# SQL Definitions

This directory stores current School SQL definition archives.

- `current/`: current or still-relevant schema/RPC definition sources.
- No file in this directory is executed automatically.
- Applying any file to a database still requires the guarded SQL/RPC workflow.

Historical one-time checks, repairs, and superseded Cash-linkage phase files
are archived under `docs/archive/sql/`.

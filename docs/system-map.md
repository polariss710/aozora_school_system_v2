# v2 System Map

Status date: 2026-06-10

Completion view: see `docs/module-status.md` for module-by-module completion, writable surfaces, readonly/preview surfaces, guards, limits, and backlog priority. For a visual static overview, open `docs/module-status-dashboard.html` locally.

Reference baseline:

- Latest stable code checkpoint reviewed before planned-only lesson batch import: `e1619e1`
- Lesson RPC SQL archive commits reviewed: `ce5ed9c` planned lesson create, `513dcd0` completed actual from planned, `ccaf087` cancelled actual from planned, and `ea8d9fb` makeup_completed actual from planned.
- This map reflects the current repository state after planned lesson creation V1, completed actual generation from planned V1, cancelled actual generation from planned V1, makeup_completed actual generation from planned V1, the planned/actual paired readonly view with expandable compact content/note text and desktop equal-height paired cards, the verified planned-only lesson batch import RPC/frontend checkpoint, the planned-only import UX/template cleanup checkpoint, the verified guarded lesson edit RPC/frontend checkpoint on list/detail, planned-only void, default paired view, cross-month makeup completion DB/RPC + API/UI + regression closure, student monthly settlement V1 closure, and the 2026-06-07 master-data create dialog UI polish checkpoint.

## Global Boundaries

- Page modules must not call Supabase `.rpc()` directly.
- Page modules must not directly `insert`, `update`, `delete`, or `upsert` database rows.
- All write operations currently exposed by pages go through `js/api/*-api.js` wrappers and verified RPCs.
- Historical records are audit data. Current write flows must preserve original records and use status, reversal fields, related records, and account transactions instead of deletion or historical rewrite.
- The 2026-05 teacher wage duplicate reconciliation was completed through service-role-only one-time RPC `school_fix_202605_teacher_wage_duplicate_cong_qirun`; it is not a page/API write surface and not a general wage void/relock lifecycle. It left the older wage/payment effective, voided only the confirmed duplicate wage snapshot, cancelled only the confirmed duplicate pending payment request, and kept effective 2026-05 wage locks at `locked:9 / void:13`.
- Profit analysis is read-only and uses effective income/expense records for operating profit. Reimbursement, account adjustment, account transfer, and payment-request state changes are audit references unless they materialize as effective income/expense.

## Module Map

| Module | Page files | Current status | Main data sources | Allowed writes / RPCs | Must not affect | Relationships and notes | Remaining risks / attention |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 老师工资支付 | `index.html`, `payment-detail.html`, `js/pages/payment-page.js`, `js/pages/payment-detail-page.js`, `js/api/payment-api.js`, `js/api/payment-detail-api.js` | Payment list can confirm payment, reverse paid payment, cancel pending requests, restore cancelled requests, and reissue reversed requests. Detail page is read-only and complete. | `school_payment_requests`, `school_teacher_wage_locks`, `school_expense_records`, `school_accounts`, `school_account_transactions`, `school_business_entities` | API-layer RPCs: `school_get_payment_management_summary` for summary read, `school_confirm_payment_request`, `school_reverse_paid_payment_request`, `school_cancel_payment_request`, `school_restore_cancelled_payment_request`, `school_reissue_reversed_payment_request` | Must not delete payment requests, wage locks, expense records, or account transactions. Reversal must preserve original payment, generated expense, and original transaction. | Confirmed teacher wage payment creates/links paid expense and account transaction. Reversal restores account balance through reversal transaction. Detail page shows same-source payment chain and links to wage/expense/account evidence. | `current-status.md` explicitly records confirmation/reversal as completed write flows; cancel/restore/reissue exist in UI/API and SQL files, but should be treated as payment-status actions with the same RPC-only boundary when retested. |
| 账户管理 | `account.html`, `account-transaction-detail.html`, `js/pages/account-page.js`, `js/pages/account-transaction-detail-page.js`, `js/api/account-api.js`, `js/api/account-transaction-detail-api.js` | Account list is readable; future-use account master data can be created; account profile is editable; account adjustment and account transfer can be created; account adjustment and transfer can be reversed from transaction detail. | `school_accounts`, `school_account_transactions`, `school_account_adjustments`, `school_account_transfers`, `school_business_entities`, related source tables | API-layer RPCs: `school_create_account_profile`, `school_update_account_profile`, `school_create_account_adjustment`, `school_reverse_account_adjustment`, `school_create_account_transfer`, `school_reverse_account_transfer` | Account create must keep opening/current balances at 0 and must not create account transactions. Account profile edit must not alter account code, business ownership, currency, opening balance, current balance, historical income/expense/reimbursement/payment/transfer/adjustment/settlement/wage data. Adjustment/transfer must not use non-whitelisted real data for tests. | Account transaction detail summarizes sources for income, expense, payment request, reimbursement, adjustment, transfer, and account origin. Transfer and adjustment writes are reflected in account balances and audit transactions. | Account transfer has no standalone detail page; source summary appears in account transaction detail. Balance changes remain under verified income, expense, reimbursement, payment, adjustment, and transfer flows. |
| 收入记录 | `income.html`, `income-detail.html`, `js/pages/income-page.js`, `js/pages/income-detail-page.js`, `js/api/income-api.js`, `js/api/income-detail-api.js` | List/detail are complete. New tuition income can be created. Received tuition income can be reversed from detail when guards pass. | `school_income_records`, `school_students`, `school_business_entities`, `school_accounts`, `school_student_monthly_settlements`, `school_account_transactions` | API-layer RPCs: `school_create_income_record`, `school_reverse_income_record` | Must not delete income, recalculate locked student settlements, edit student payment chains, or infer income from account transactions. Reversal must preserve original income and original transaction. | New income writes income record, account balance, and account transaction. Detail links settlement references and account transaction references for audit only. | First version is limited to paid tuition income. Detail guard blocks reversal for locked settlement targets and student-payment-chain-linked income. |
| 支出记录 | `expense.html`, `expense-detail.html`, `js/pages/expense-page.js`, `js/pages/expense-detail-page.js`, `js/api/expense-api.js`, `js/api/expense-detail-api.js` | List/detail are complete. Ordinary paid expenses can be created. Ordinary paid expenses can be reversed from detail when guards pass. Ordinary non-teacher-wage expense attachment metadata can be added from detail. | `school_expense_records`, `school_payment_requests`, `school_business_entities`, `school_accounts`, `school_teachers`, `school_students`, `school_reimbursement_items`, `school_reimbursements`, `school_expense_attachments`, `school_account_transactions` | API-layer RPCs: `school_create_expense_record`, `school_reverse_expense_record`, `school_create_expense_attachment_metadata` | Must not use ordinary expense reversal for teacher wage expenses. Must not add ordinary attachment metadata to teacher wage expenses. Must not reverse reimbursed expenses before reimbursement is reversed. Must not delete expense records, attachments, payment requests, or original transactions. | Teacher wage paid expenses are audit evidence for payment requests. Reimbursement links to ordinary expenses through reimbursement items. Profit summary includes effective paid expenses and excludes reversed ones. Attachment metadata is audit-only and does not affect expense amount/status/account/reimbursement state or account transactions. | Attachment V1 is metadata-only: no Supabase Storage, upload, download, preview, replace, delete, public URL, extracted text, or OCR. First version does not handle attachments in create flow and does not support reimbursement or partial reimbursement through ordinary expense create/reverse. |
| 报销管理 | `reimbursement.html`, `reimbursement-detail.html`, `js/pages/reimbursement-page.js`, `js/pages/reimbursement-detail-page.js`, `js/api/reimbursement-api.js`, `js/api/reimbursement-detail-api.js` | Reimbursement list/detail are complete. Reimbursement confirmation is available from candidate paid non-teacher-wage expenses. Reimbursement can be reversed from detail. | `school_reimbursements`, `school_reimbursement_items`, `school_expense_records`, `school_accounts`, `school_account_transactions`, `school_business_entities` | API-layer RPCs: `school_create_reimbursement_record`, `school_reverse_reimbursement_record` | Must not count reimbursement as operating expense. Must not delete reimbursement records/items or original account transactions. Must not modify unrelated expenses. | Confirmation creates reimbursement header, reimbursement items, two account transactions, account balance updates, and expense reimbursement status updates. Reversal creates opposite account transactions, restores balances, and moves linked expenses back to pending reimbursement. | First version does not support edit, partial reimbursement, attachments, or statistics from the reimbursement page. |
| 业务归属 | `business-entity.html`, `js/pages/business-entity-page.js`, `js/api/business-entity-api.js` | Readable list with future-use basic profile creation and editable basic profile. | `school_business_entities`; referenced by accounts, teachers, students, income, expense, settlements, wages, payments, profit analysis | API-layer RPCs: `school_create_business_entity_profile`, `school_update_business_entity_profile` | Create must not automatically create accounts. Edit must not modify code or company-report inclusion. Both must not rewrite historical income, expense, accounts, settlements, wages, payments, account balances, or account transactions. | Business entity is the main ownership/filter dimension across finance, settlement, wage, account, and profit modules. Creation is future-use master data only. | No delete flow is documented as complete. Editing default currency can affect future defaults/display and must not imply historical currency rewrite. |
| 老师管理 | `teacher.html`, `js/pages/teacher-page.js`, `js/api/teacher-api.js` | Teacher list is readable. Teacher profile can be created and edited for future use. | `school_teachers`, `school_business_entities` | API-layer RPCs: `school_create_teacher_profile`, `school_update_teacher_profile` | Must not edit wage rules, wage locks, payment requests, expenses, account transactions, rates, currencies, payment method, bank/contact fields, teacher code on edit, or historical lesson data. | Teachers are lookup entities for lessons, wage rules, wage locks, expenses, and payment requests. Creation is future-use master data only. | No delete flow. Existing edit scope is intentionally narrow: display name, status, default business entity, and note. |
| 学生管理 | `student.html`, `js/pages/student-page.js`, `js/api/student-api.js` | Student list is readable. Student profile can be created and edited for future use, including course/target/default fields. | `school_students`, `school_business_entities` | API-layer RPCs: `school_create_student_profile`, `school_update_student_profile` | Must not edit balances, monthly settlements, carryovers, income records, payment requests, lesson records, wage data, account transactions, tuition rules, contact fields, parent fields, birthday, or historical financial data. | Students are lookup entities for lessons, income, monthly settlements, wage rules, and profit drilldowns. Default business entity/currency supports future records only. | No delete flow. Existing update/create scope excludes tuition rules and contact/parent data. |
| 科目管理 | `subject.html`, `js/pages/subject-page.js`, `js/api/subject-api.js` | Subject list is readable. Subject profile can be created and edited for future use. | `school_subjects` | API-layer RPCs: `school_create_subject_profile`, `school_update_subject_profile` | Must not delete subjects or edit historical lessons, student settlements, teacher wages, payment requests, income, expenses, or account transactions. Edit flow must not alter category/color/sort/history fields unless a future phase explicitly expands scope. | Subjects connect lessons, wage rules, wage lock details, and settlement/wage detail references. | `school_subjects` has no `display_name/status`; UI status maps to `is_active`, display name maps to `name`. |
| 老师工资规则 | `wage-rule.html`, `wage-rule-detail.html`, `js/pages/wage-rule-page.js`, `js/pages/wage-rule-detail-page.js`, `js/api/wage-rule-api.js` | Wage rule list is readable, detail page is complete, future-use rule config can be created, existing config can be edited, and rules can be soft-disabled/restored instead of deleted. | `school_teacher_wage_rules`, `school_teachers`, `school_students`, `school_subjects`, `school_business_entities` | API-layer RPCs: `school_create_teacher_wage_rule_config`, `school_update_teacher_wage_rule_config`, `school_set_teacher_wage_rule_active_state` | Create/edit/soft-disable/restore must not recalculate historical wages or modify wage locks, wage lock details, payment requests, expenses, account balances, or account transactions. Edit must not modify teacher, student, subject, or business entity linkage. Soft-disable/restore must not physically delete rules. | Wage rules are future-lock configuration only. Detail page explicitly states no historical recalculation. First create version requires explicit teacher/student/subject/business entity and does not create generic student-empty rules. Soft-disable/restore toggles `is_active` and optional `note`; restore rejects another active rule with the same match keys. | No physical delete flow exists by design. Config edit is limited to settlement type, rates, exchange rate, transport/classroom fees, and note; active-state changes use the dedicated soft-disable/restore action. |
| 老师工资结算 | `wage.html`, `wage-detail.html`, `js/pages/wage-page.js`, `js/pages/wage-detail-page.js`, `js/api/wage-api.js`, `js/api/wage-detail-api.js` | Wage snapshot list, saved detail, teacher wage generation MVP, and wage-snapshot -> payment-request generation are complete. `wage.html` can generate wage snapshots through API wrapper `generateTeacherMonthlyWage`; the list hides `status = void` by default and exposes an explicit `已作废` filter. `wage-detail.html` shows the saved snapshot, preserves list filters on return, and can create one pending payment request for a wage snapshot that has no existing teacher_wage payment request. | `school_teacher_wage_locks`, `school_teacher_wage_lock_details`, `school_payment_requests`, `school_teachers`, `school_business_entities`, `school_lesson_records`, `school_teacher_wage_rules` | API-layer RPCs: `school_generate_teacher_monthly_wage` writes only wage locks/details from actual completed/makeup_completed lessons; `school_create_teacher_wage_payment_request` writes only one pending `school_payment_requests` row from one non-voided generated wage snapshot. Payment confirmation/status actions remain in 老师工资支付. | Page modules must not call `.rpc()` directly or directly insert/update/delete/upsert rows. Wage generation must not modify lessons and must not write payment requests, expenses, accounts, account transactions, income, or student settlements. Payment-request generation must not confirm payment and must not write expenses, accounts, account transactions, income, student settlements, lessons, wage locks, or wage details. | Wage detail shows saved wage snapshot and related payment requests, hiding the generation entry once a request exists. Payment request detail handles payment chain display. Cross-month makeup actuals enter by target `teacher_settlement_month` even when student `is_billable = false`. Wage generation creates `status = locked` snapshots directly in the current table structure, excludes planned/cancelled/voided lessons, rejects existing same-teacher/month snapshots before missing-field validation, uses `lesson_wage_jpy = round(actual_minutes / 60 * hourly_rate_jpy)`, and fixes CNY/exchange/transport/classroom fields to 0 for this MVP. Payment-request generation uses `currency = JPY`, `amount = amount_jpy = total_jpy`, and `amount_cny = total_cny`. | Multi-business same-teacher-month handling, preview UI, expanded wage void/relock lifecycle, payment confirmation follow-up, CNY/FX, transport/classroom fees, history/backfill, and cleanup remain separate guarded backlog. |
| 学生月度结算 | `settlement.html`, `settlement-detail.html`, `js/pages/settlement-page.js`, `js/pages/settlement-detail-page.js`, `js/api/settlement-api.js`, `js/api/settlement-detail-api.js` | V1 is complete for current/future v2 operations. List/detail, realtime preview, preview -> locked snapshot, soft unlock, same-row relock, guard documentation, and no-write closure self-check are complete. | `school_student_monthly_settlements`, `school_student_settlement_carryovers`, `school_students`, `school_business_entities`, `school_lesson_records`, `school_income_records`, `school_teachers`, `school_subjects`, `school_accounts` | API-layer RPCs: read-only `school_get_student_monthly_settlement_summary`, `school_lock_student_monthly_settlement` from preview rows, `school_unlock_student_monthly_settlement`, and `school_relock_student_monthly_settlement`. | Must not physically delete settlement snapshots, change `unique(student_id, year_month)`, create multi-version history in V1, auto-create/revoke/rebuild carryovers, edit adjustments, migrate/repair historical data, clean whitelist test data, or mutate lesson/income/account/wage/payment/expense rows from unlock/relock. | Lock inserts one snapshot at `student_id + year_month`. Unlock preserves the same row and records `unlocked_at` / `unlock_reason`; relock recalculates the same row from `school_get_student_monthly_settlement_summary` while preserving `id`, `student_id`, `year_month`, `created_at`, `unlocked_at`, and `unlock_reason`. Active carryovers sourced from a settlement block unlock/relock. List/detail label `unlocked` as `锁定已撤销`; successful state changes refresh the current month/detail. Locked settlements block lesson/income writes through the existing lesson/income RPC guards; unlocked settlements release those guards until relock. Historical settlement maintenance remains in v1; v2 settlement V1 is for current/future operations only. Detail shows saved settlement snapshot plus matching lesson and income references for audit. Income detail may link back to settlement references. | Future adjustment editing, multi-version settlement history, carryover revoke/rebuild, historical migration/repair, and whitelist test data cleanup require separate guarded workflows and are not part of completed V1. |
| 课时管理 | `lesson.html`, `lesson-detail.html`, `js/pages/lesson-page.js`, `js/pages/lesson-detail-page.js`, `js/api/lesson-api.js`, `js/api/lesson-detail-api.js` | Ordinary list, planned/actual paired view, detail, planned lesson manual creation V1, completed actual generation from planned V1, cancelled actual generation from planned V1, makeup_completed actual generation from planned V1, lesson import preview, planned-only template export, planned-only batch import submit, guarded lesson edit RPC/frontend on `lesson.html` / `lesson-detail.html`, planned-only lesson void DB/RPC/frontend first stage, voided planned status filtering, and small navigation polish are complete. Detail page includes source-chain tracking, settlement/wage references, guarded edit entry, month/view-aware return links, and readonly voided planned display. `lesson.html` defaults to planned/actual paired view; ordinary list remains an auxiliary view. Cross-month makeup completion DB/RPC + API/UI V1 is complete; target-month entry, source candidate lookup, guarded API wrapper, and source/target paired-view reference display are implemented. | `school_lesson_records`, `school_students`, `school_teachers`, `school_subjects`, `school_business_entities`, `school_student_monthly_settlements`, `school_teacher_wage_lock_details`, `school_teacher_wage_locks` | API-layer RPCs: `school_create_planned_lesson_record`, `school_create_actual_lesson_from_planned`, `school_create_cancelled_actual_lesson_from_planned`, `school_create_makeup_completed_actual_lesson_from_planned`, `school_import_lesson_records_batch`, `school_update_lesson_record_guarded`, `school_void_planned_lesson`. API-layer RPCs also include `school_create_cross_month_makeup_completed_actual_from_planned` for the target-month cross-month makeup entry. Import submit, edit save, and planned void call RPCs only through `js/api/lesson-api.js`; page modules do not call `.rpc()` directly. | Planned creation V1 may insert only `lesson_type = planned` rows with `planned` or `pending_makeup` status. Completed actual-from-planned V1 may insert only one linked `lesson_type = actual`, `status = completed` row from one planned row. Cancelled actual-from-planned V1 may insert only one linked `lesson_type = actual`, `status = cancelled` row from one planned row and fixes it to `is_billable = false`, `lesson_fee = 0`, and `actual_minutes = 0`. Makeup_completed actual-from-planned V1 may insert only one linked `lesson_type = actual`, `status = makeup_completed` row from one planned row, supports `is_billable = true / false`, fixes non-billable rows to `lesson_fee = 0`, and derives `actual_minutes` from duration. Cross-month makeup_completed RPC may insert only one linked target-month `lesson_type = actual`, `status = makeup_completed` row from an earlier-month `pending_makeup` planned row, defaults `is_billable = false`, requires explicit `p_is_billable = true` for billing, checks target-month settlement and wage locks, and must not modify the source planned row. All actual-from-planned flows must set `planned_lesson_id`, reject soft-voided planned sources, reject duplicate linked actuals, reject locked student settlement months and locked teacher wage months, and must not modify the source planned row. Batch import V1 is planned-only: it accepts only `lesson_type = planned` and status `planned` / `pending_makeup`, rejects actual rows, rejects planned ID links, guards locked student settlement month by lesson date, and rolls back the whole batch on any row error. It does not use teacher wage lock protection because planned rows do not set actual teacher settlement month, matching the single planned create RPC口径. Guarded edit V1 updates only `school_lesson_records`, preserves `lesson_type` and `planned_lesson_id`, uses `updated_at` optimistic locking, rejects voided planned rows, rejects planned rows with linked actuals, rejects actual status changes, rejects linked actual master-data changes, blocks locked student settlement months and locked teacher wage months, rejects wage detail snapshots, and has no settlement/wage/payment/income/expense/account/account-transaction side effects. Planned void V1 updates only one planned-only `school_lesson_records` row by setting `voided_at` and `void_reason`, preserves status/import metadata, requires a non-empty reason, rejects linked actuals, locked student settlement months, stale updated_at, already-voided rows, non-planned rows, and planned statuses outside `planned` / `pending_makeup`. | Lessons feed student settlement references and teacher wage lock details. Soft-voided planned lessons are excluded by default from lesson management stats, student monthly settlement summary planned totals, and normal lesson list/paired view results; selecting lesson status filter `已作废` loads `lesson_type = planned and voided_at is not null` for readonly review/detail navigation without treating it as `status = voided`. Paired view groups loaded records in memory by `actual.planned_lesson_id = planned.id`, displays missing planned/actual sides conservatively, exposes completed/cancelled/makeup_completed actual generation only from missing-actual planned cards, keeps long content/note text compact with a click/tap expand toggle, stretches desktop planned/actual cards to a shared row height, and refreshes both ordinary and paired views after writes. Import preview parses generic CSV/Excel rows and v1-style planned/actual paired columns into preview rows with row/field errors and warnings; supported aliases include business entity, student, teacher, subject, generic and planned/actual date/time/duration/fee/status, billable, content, note, and planned association columns. Planned-only template export creates `lesson_planned_import_template_v2.xlsx` with Chinese user-facing headers and only planned examples in the main sheet, otherwise CSV fallback; actual completed/cancelled/makeup_completed examples are retained only in the guide sheet as future/history-migration backlog. Preview normalizes Chinese/Japanese/English display values such as `预定`/`实际`, `待上课`/`待补课`, `已上课`/`取消`/`已补课`, and legacy English values to the existing canonical lesson values. Actual rows can still be previewed and planned-ID-prechecked for future design, but planned-only submit blocks before RPC when actual rows exist. Planned import success exposes both `查看 YYYY-MM` and detail links for created rows. Lesson list/paired detail links carry return month/view parameters; `lesson.html` can open directly with `?year=YYYY&month=MM&view=pair`. Lesson edit uses a shared dialog controller across `lesson.html` and `lesson-detail.html`; it keeps type/link/import metadata and actual status read-only, explains disabled linked-actual master fields as source-planned consistency protection, shows field/RPC errors in-dialog, refreshes the active month after list saves, and reloads detail data after detail saves. Lesson detail cross-links settlement and wage evidence. Cross-month source/target reference display loads linked records outside the current month: source months show completed target-month actual references, and target months show original pending_makeup planned references. The 2026-06-10 closure regression verified source-month settlement locks are allowed when the target month is unlocked, target-month settlement and wage locks are rejected, linked sources disappear from candidates, source actions hide after linking, and non-billable cross-month actuals do not enter student settlement actual fee/hours. | Future full lesson import for actual rows is demoted to history-migration backlog and would need a separate guarded phase. Cross-month makeup completion V1 is implemented through a dedicated DB/RPC plus API wrapper, target-month UI entry, source selection, and original/target month paired-view reference display. Lesson delete, void restore, voided_by, expanded wage-lock lifecycle, and settlement generation/adjustment still need separate guarded phases because they can affect both student settlement and teacher wage boundaries. Auto-matching by student/teacher/subject/date/time and same-file planned/actual linking remain unsupported. |
| 利润分析 | `profit-summary.html`, `js/pages/profit-summary-page.js`, `js/api/profit-summary-api.js` | Read-only summary and drilldown are complete. Supports month, business entity, and currency filters, plus income/expense detail links. | `school_income_records`, `school_expense_records`, `school_reimbursements`, `school_payment_requests`, `school_account_transactions`, `school_business_entities` | None. | Must not write DB, adjust source records, recalculate payments/settlements/wages, or count audit-only transactions as operating profit. | Effective received income and paid expense drive operating profit by currency. Reversed income/expense are excluded. Teacher wage expense is included through paid expense records. Reimbursements, account adjustments, transfers, and payment requests are audit references. | Real browser filters/drilldown verification is recorded in `current-status.md`; no write surface exists in this module. |

## Cross-Module Write Scope Summary

| Write scope | Entry module | Allowed tables / effects | RPC boundary |
| --- | --- | --- | --- |
| Tuition income create/reverse | 收入记录 | `school_income_records`, `school_accounts`, `school_account_transactions`; read/guard against `school_student_monthly_settlements` | `school_create_income_record`, `school_reverse_income_record` |
| Ordinary expense create/reverse | 支出记录 | `school_expense_records`, `school_accounts`, `school_account_transactions`; references teacher/student/business lookups | `school_create_expense_record`, `school_reverse_expense_record` |
| Ordinary expense attachment metadata create | 支出记录 | `school_expense_attachments` metadata-only rows for ordinary non-teacher-wage expenses; no expense/account/reimbursement/account-transaction mutation | `school_create_expense_attachment_metadata` |
| Reimbursement confirm/reverse | 报销管理 | `school_reimbursements`, `school_reimbursement_items`, `school_expense_records`, `school_accounts`, `school_account_transactions` | `school_create_reimbursement_record`, `school_reverse_reimbursement_record` |
| Teacher wage payment actions | 老师工资支付 | `school_payment_requests`, `school_expense_records`, `school_accounts`, `school_account_transactions`; read source wage locks | `school_confirm_payment_request`, `school_reverse_paid_payment_request`, `school_cancel_payment_request`, `school_restore_cancelled_payment_request`, `school_reissue_reversed_payment_request` |
| Account adjustment/transfer | 账户管理 | `school_account_adjustments`, `school_account_transfers`, `school_accounts`, `school_account_transactions` | `school_create_account_adjustment`, `school_reverse_account_adjustment`, `school_create_account_transfer`, `school_reverse_account_transfer` |
| Master data profile create/update | 老师/学生/科目/业务归属/账户/工资规则 | Narrow master/profile/config columns only; no historical financial chain writes; wage rule delete alternative is soft-disable/restore only | `school_create_teacher_profile`, `school_update_teacher_profile`, `school_create_student_profile`, `school_update_student_profile`, `school_create_subject_profile`, `school_update_subject_profile`, `school_create_business_entity_profile`, `school_update_business_entity_profile`, `school_create_account_profile`, `school_update_account_profile`, `school_create_teacher_wage_rule_config`, `school_update_teacher_wage_rule_config`, `school_set_teacher_wage_rule_active_state` |
| Planned lesson create V1 | 课时管理 | Insert one future/planned `school_lesson_records` row only after student/teacher/subject/business-entity and locked-student-settlement guards pass; no actual/wage/payment/income/expense/account side effects | `school_create_planned_lesson_record` |
| Completed actual from planned V1 | 课时管理 | Insert one linked completed actual `school_lesson_records` row only after planned-source, duplicate-actual, locked-student-settlement, and locked-teacher-wage guards pass; no source planned mutation and no wage/payment/income/expense/account side effects | `school_create_actual_lesson_from_planned` |
| Cancelled actual from planned V1 | 课时管理 | Insert one linked cancelled actual `school_lesson_records` row only after planned-source, duplicate-actual, locked-student-settlement, and locked-teacher-wage guards pass; fixed non-billable with fee/minutes zero; no source planned mutation and no wage/payment/income/expense/account side effects | `school_create_cancelled_actual_lesson_from_planned` |
| Makeup_completed actual from planned V1 | 课时管理 | Insert one linked makeup_completed actual `school_lesson_records` row only after planned-source, duplicate-actual, locked-student-settlement, and locked-teacher-wage guards pass; supports billable/non-billable, fixes non-billable fee to zero, records actual minutes from duration; no source planned mutation and no wage/payment/income/expense/account side effects | `school_create_makeup_completed_actual_lesson_from_planned` |
| Cross-month makeup_completed actual from planned V1 | 课时管理 | Insert one linked target-month makeup_completed actual `school_lesson_records` row from an earlier-month `pending_makeup` planned row through the API-layer target-month entry; UI fixes non-billable amount to zero, RPC defaults non-billable and allows billable only by explicit parameter, checks target-month student settlement and teacher wage locks, and has no source planned mutation or settlement/wage/payment/income/expense/account side effects | `school_create_cross_month_makeup_completed_actual_from_planned` |
| Teacher wage generation MVP | 老师工资结算 | Insert teacher wage snapshot rows in `school_teacher_wage_locks` and matching rows in `school_teacher_wage_lock_details` from actual `completed` / `makeup_completed` lessons only. It uses `teacher_settlement_month` / `year_month`, ignores `is_billable`, excludes planned/cancelled/voided lessons, rejects existing same-teacher same-month wage records before missing-field actual validation, rejects already-detailed actuals, rejects missing/duplicate active wage rules, and has no lesson/payment/expense/account/account-transaction/income/student-settlement side effects. The exposed page entry goes through `js/api/wage-api.js`; page modules do not call `.rpc()` directly. | `school_generate_teacher_monthly_wage` |
| Teacher wage payment request generation | 老师工资结算 | Insert one pending `school_payment_requests` row from one locked, non-voided teacher wage lock. It rejects wage locks with existing teacher_wage payment requests, uses the wage lock teacher/business/month/amount snapshot, and has no payment confirmation, expense, account, account-transaction, income, student-settlement, lesson, wage-lock, or wage-detail side effects. The exposed page entry goes through `js/api/wage-detail-api.js`; page modules do not call `.rpc()` directly. | `school_create_teacher_wage_payment_request` |
| Planned-only lesson batch import V1 | 课时管理 | Insert a validated all-or-nothing batch of planned `school_lesson_records` rows only. It derives import metadata and planned date/month fields, accepts status `planned` / `pending_makeup`, rejects actual rows and planned links, guards locked student settlement months, and has no settlement/wage/payment/income/expense/account/account-transaction side effects | `school_import_lesson_records_batch` |
| Guarded lesson edit V1 | 课时管理 | Update one `school_lesson_records` row only. It preserves `lesson_type` and `planned_lesson_id`, uses `updated_at` optimistic locking, limits planned status to `planned` / `pending_makeup`, keeps actual status unchanged, blocks linked planned edits, linked actual master-data changes, locked student settlement months, locked teacher wage months, and wage detail snapshots, and has no settlement/wage/payment/income/expense/account/account-transaction side effects | `school_update_lesson_record_guarded` |
| Planned-only lesson void V1 | 课时管理 | Update one planned-only `school_lesson_records` row only by setting `voided_at` and `void_reason`. It preserves original status, lesson_type, planned_lesson_id, import_batch_id, import_source, and imported_at; rejects non-planned rows, statuses outside `planned` / `pending_makeup`, already-voided rows, linked actuals, locked student settlement months, stale updated_at, and blank reasons; no settlement/wage/payment/income/expense/account/account-transaction side effects | `school_void_planned_lesson` |

## Boundary Questions To Recheck Before Future Work

- Payment cancel/restore/reissue actions are present in current UI/API and SQL files, while `current-status.md` only names teacher wage payment confirmation and reversal in the completed write-flow summary. Treat any future change around these status actions as requiring explicit retest against the full write-RPC workflow.
- Account transfer/reversal and profit summary filters/drilldown now have real browser verification recorded in `current-status.md`; future changes around these surfaces should retest the same workflows.
- No standalone account transfer detail page exists; account transaction detail is the current audit surface.
- Lesson management V1, planned-only import, student monthly settlement V1, and cross-month makeup completion are complete/verified. Planned and actual lessons stay strictly separated; planned-only import does not import actual rows; same-month pending_makeup completion uses the existing same-month makeup_completed actual-from-planned flow; cross-month makeup completion uses dedicated guarded RPC `school_create_cross_month_makeup_completed_actual_from_planned`, does not copy planned/actual rows, does not modify the source planned row, defaults non-billable, and must remain behind API-layer wrappers rather than page-level `.rpc()` calls.
- No completed write flow exists for free actual creation outside planned flow, actual batch import, lesson delete, expanded teacher wage-lock lifecycle beyond the completed generation/payment-request MVP, settlement adjustment beyond completed V1, or wage rule physical deletion. Next-stage backlog candidates are payment confirmation follow-up enhancements, weekly plan image export, full actual import / history migration, whitelist/codex-test deferred cleanup, expanded wage-lock lifecycle, and DB-level linked-actual unique/index only after read-only duplicate-risk verification.

# v2 System Map

Status date: 2026-06-12

Completion view: see `docs/module-status.md` for module-by-module completion, writable surfaces, readonly/preview surfaces, guards, limits, and backlog priority. For a visual static overview, open `docs/module-status-dashboard.html` locally.

Reference baseline:

- Latest stable code checkpoint reviewed before planned-only lesson batch import: `e1619e1`
- Lesson RPC SQL archive commits reviewed: `ce5ed9c` planned lesson create, `513dcd0` completed actual from planned, `ccaf087` cancelled actual from planned, and `ea8d9fb` makeup_completed actual from planned.
- This map reflects the current repository state after planned lesson creation V1, completed actual generation from planned V1, cancelled actual generation from planned V1, makeup_completed actual generation from planned V1, the planned/actual paired readonly view with expandable compact content/note text and desktop equal-height paired cards, the parent-facing student lesson PDF print layout and popup-blocking fix, the verified planned-only lesson batch import RPC/frontend checkpoint, the planned-only import UX/template cleanup checkpoint, the verified guarded lesson edit RPC/frontend checkpoint on list/detail, planned-only void, default paired view, cross-month makeup completion DB/RPC + API/UI + regression closure, student monthly settlement V1 closure with pre-lock difference adjustment, the 2026-06-10 student-line release-candidate self-check, and the 2026-06-07 master-data create dialog UI polish checkpoint.

## Global Boundaries

- Page modules must not call Supabase `.rpc()` directly.
- Page modules must not directly `insert`, `update`, `delete`, or `upsert` database rows.
- All write operations currently exposed by pages go through `js/api/*-api.js` wrappers and verified RPCs.
- Master-data create/edit surfaces for accounts, teachers, students, subjects, and teacher wage rules now use a shared dialog behavior standard: explicit small/medium/large sizing, cancel/save action order, loading state, inline errors, failure keeps the dialog and typed input, and success refreshes while preserving current filters and viewport position as far as possible. The 2026-06-12 editable-field pass opened safe displayed master-data fields while keeping IDs, generated codes, timestamps, balances, account transactions, settlement/wage/payment/audit chains, contact/payment-account data, and lock-like derived fields readonly. Delete remains closed for these surfaces unless a future guarded workflow explicitly opens it.
- Historical records are audit data. Current write flows must preserve original records and use status, reversal fields, related records, and account transactions instead of deletion or historical rewrite.
- The 2026-05 teacher wage duplicate reconciliation was completed through service-role-only one-time RPC `school_fix_202605_teacher_wage_duplicate_cong_qirun`; it is not a page/API write surface and not a general wage void/relock lifecycle. It left the older wage/payment effective, voided only the confirmed duplicate wage snapshot, cancelled only the confirmed duplicate pending payment request, and kept effective 2026-05 wage locks at `locked:9 / void:13`.
- Current or unclosed real business months must not be used for real wage generation, settlement generation, closing, or lock-style write validation. Wage generation validation must use codex-test whitelist data or transaction rollback unless the user explicitly authorizes a formal business operation. The 2026-06 teacher wage early-generation rollback was a one-time user-authorized repair that deleted only the mistaken 2026-06 wage locks/details after proving there were no payment, expense, account-transaction, or adjustment dependencies; it is not a general wage lifecycle.
- Teacher wage settlement user flow is snapshot-based: actual lessons -> confirm matching student monthly settlements are `locked` -> generate teacher wage settlement snapshot -> generate payment request from that snapshot -> confirm payment with a selected account -> create teacher_wage expense and account transaction. Company-account wage payments set `reimbursement_status = not_required`; advance/personal-account wage payments set `reimbursement_status = pending`; reimbursement repays advance accounts and must not create another teacher_wage expense. The underlying table/status names remain `school_teacher_wage_locks`, `locked_at`, and `status = locked`, but the UI treats them as generated wage snapshots rather than a separate user operation.
- Student monthly settlements become downstream-locked once any same-student/month actual lesson is referenced by a non-void teacher wage snapshot. Settlement unlock, relock, and draft adjustment RPCs must reject active wage snapshots, any teacher_wage payment request, paid teacher_wage expenses, and account transactions; void wage snapshots are not permanent blockers.
- Profit analysis is read-only and uses effective income/expense records for operating profit. Reimbursement, account adjustment, account transfer, and payment-request state changes are audit references unless they materialize as effective income/expense.

## Module Map

| Module | Page files | Current status | Main data sources | Allowed writes / RPCs | Must not affect | Relationships and notes | Remaining risks / attention |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 老师工资支付 | `index.html`, `payment-detail.html`, `js/pages/payment-page.js`, `js/pages/payment-detail-page.js`, `js/api/payment-api.js`, `js/api/payment-detail-api.js` | Payment list can confirm payment with a selected account, reverse paid payment, cancel pending requests, restore cancelled requests, and reissue reversed requests. Detail page is read-only and complete. | `school_payment_requests`, `school_teacher_wage_locks`, `school_expense_records`, `school_accounts`, `school_account_transactions`, `school_business_entities` | API-layer RPCs: `school_get_payment_management_summary` for summary read, `school_confirm_payment_request`, `school_reverse_paid_payment_request`, `school_cancel_payment_request`, `school_restore_cancelled_payment_request`, `school_reissue_reversed_payment_request` | Must not delete payment requests, wage locks, expense records, or account transactions. Reversal must preserve original payment, generated expense, and original transaction. Confirm payment must not create reimbursements, reimbursement items, income, student settlements, wage locks/details, or lesson rows. | Confirmed teacher wage payment creates one teacher_wage expense, one account transaction, updates the selected account balance, and links both ids back to the payment request. Company-account payments set the wage expense `reimbursement_status = not_required`; advance/personal-account payments set `reimbursement_status = pending`, but reimbursement flows still reject `expense_category = teacher_wage`, so reimbursement only repays advance accounts and never generates a second teacher wage expense. Reversal restores account balance through `payment_reversal`, marks the payment request `reversed`, and marks the generated teacher_wage expense `reversed`; the approved 2026-02/2026-03 historical sync repaired the same expense status linkage without adding cash transactions. Detail page shows same-source payment chain and links to wage/expense/account evidence. | `current-status.md` records confirmation/reversal and the 2026-06-10 account-type retest as completed write flows; cancel/restore/reissue exist in UI/API and SQL files, but should be treated as payment-status actions with the same RPC-only boundary when retested. |
| 账户管理 | `account.html`, `account-transaction-detail.html`, `js/pages/account-page.js`, `js/pages/account-transaction-detail-page.js`, `js/api/account-api.js`, `js/api/account-transaction-detail-api.js` | Account list is readable; future-use account master data can be created; account profile is editable; account adjustment and account transfer can be created; account adjustment and transfer can be reversed from transaction detail. Create/edit dialogs preserve filters and viewport on refresh, keep input on failure, and use the shared master-data dialog sizing/actions. | `school_accounts`, `school_account_transactions`, `school_account_adjustments`, `school_account_transfers`, `school_business_entities`, related source tables | API-layer RPCs: `school_create_account_profile`, `school_update_account_profile`, `school_create_account_adjustment`, `school_reverse_account_adjustment`, `school_create_account_transfer`, `school_reverse_account_transfer` | Account create must keep opening/current balances at 0 and must not create account transactions. Account profile edit must not alter account code, business ownership, currency, opening balance, current balance, historical income/expense/reimbursement/payment/transfer/adjustment/settlement/wage data. Adjustment/transfer must not use non-whitelisted real data for tests. | Account transaction detail summarizes sources for income, expense, payment request, reimbursement, adjustment, transfer, and account origin. Transfer and adjustment writes are reflected in account balances and audit transactions. | Account transfer has no standalone detail page; source summary appears in account transaction detail. Balance changes remain under verified income, expense, reimbursement, payment, adjustment, and transfer flows. |
| 收入记录 | `income.html`, `income-detail.html`, `js/pages/income-page.js`, `js/pages/income-detail-page.js`, `js/api/income-api.js`, `js/api/income-detail-api.js` | List/detail are complete. New tuition income can be created. Received tuition income can be reversed from detail when guards pass. | `school_income_records`, `school_students`, `school_business_entities`, `school_accounts`, `school_student_monthly_settlements`, `school_account_transactions` | API-layer RPCs: `school_create_income_record`, `school_reverse_income_record` | Must not delete income, recalculate locked student settlements, edit student payment chains, or infer income from account transactions. Reversal must preserve original income and original transaction. | New income writes income record, account balance, and account transaction. Detail links settlement references and account transaction references for audit only. | First version is limited to paid tuition income. Detail guard blocks reversal for locked settlement targets and student-payment-chain-linked income. |
| 支出记录 | `expense.html`, `expense-detail.html`, `js/pages/expense-page.js`, `js/pages/expense-detail-page.js`, `js/api/expense-api.js`, `js/api/expense-detail-api.js` | List/detail are complete. Ordinary paid expenses can be created. Ordinary paid expenses can be reversed from detail when guards pass. Ordinary non-teacher-wage expense attachment metadata can be added from detail. | `school_expense_records`, `school_payment_requests`, `school_business_entities`, `school_accounts`, `school_teachers`, `school_students`, `school_reimbursement_items`, `school_reimbursements`, `school_expense_attachments`, `school_account_transactions` | API-layer RPCs: `school_create_expense_record`, `school_reverse_expense_record`, `school_create_expense_attachment_metadata` | Must not use ordinary expense reversal for teacher wage expenses. Must not add ordinary attachment metadata to teacher wage expenses. Must not reverse reimbursed expenses before reimbursement is reversed. Must not delete expense records, attachments, payment requests, or original transactions. | Teacher wage paid expenses are audit evidence for payment requests. Reimbursement links to ordinary expenses through reimbursement items. Profit summary includes effective paid expenses and excludes reversed ones. Attachment metadata is audit-only and does not affect expense amount/status/account/reimbursement state or account transactions. | Attachment V1 is metadata-only: no Supabase Storage, upload, download, preview, replace, delete, public URL, extracted text, or OCR. First version does not handle attachments in create flow and does not support reimbursement or partial reimbursement through ordinary expense create/reverse. |
| 报销管理 | `reimbursement.html`, `reimbursement-detail.html`, `js/pages/reimbursement-page.js`, `js/pages/reimbursement-detail-page.js`, `js/api/reimbursement-api.js`, `js/api/reimbursement-detail-api.js` | Reimbursement list/detail are complete. Reimbursement confirmation is available from candidate paid non-teacher-wage expenses. Reimbursement can be reversed from detail. | `school_reimbursements`, `school_reimbursement_items`, `school_expense_records`, `school_accounts`, `school_account_transactions`, `school_business_entities` | API-layer RPCs: `school_create_reimbursement_record`, `school_reverse_reimbursement_record` | Must not count reimbursement as operating expense. Must not delete reimbursement records/items or original account transactions. Must not modify unrelated expenses. | Confirmation creates reimbursement header, reimbursement items, two account transactions, account balance updates, and expense reimbursement status updates. Reversal creates opposite account transactions, restores balances, and moves linked expenses back to pending reimbursement. | First version does not support edit, partial reimbursement, attachments, or statistics from the reimbursement page. |
| 业务归属 | `business-entity.html`, `js/pages/business-entity-page.js`, `js/api/business-entity-api.js` | Readable list with future-use basic profile creation and editable basic profile. | `school_business_entities`; referenced by accounts, teachers, students, income, expense, settlements, wages, payments, profit analysis | API-layer RPCs: `school_create_business_entity_profile`, `school_update_business_entity_profile` | Create must not automatically create accounts. Edit must not modify code or company-report inclusion. Both must not rewrite historical income, expense, accounts, settlements, wages, payments, account balances, or account transactions. | Business entity is the main ownership/filter dimension across finance, settlement, wage, account, and profit modules. Creation is future-use master data only. | No delete flow is documented as complete. Editing default currency can affect future defaults/display and must not imply historical currency rewrite. |
| 老师管理 | `teacher.html`, `js/pages/teacher-page.js`, `js/api/teacher-api.js` | Teacher list is readable. Teacher profile can be created and edited for future use through a narrowed, unified create/edit dialog. Dialogs preserve filters/viewport on refresh and keep typed input on validation/RPC failure. | `school_teachers`, `school_subjects`, `school_business_entities` | API-layer RPCs: `school_create_teacher_profile`, `school_update_teacher_profile` | Must not edit teacher code, default hourly rate, wage/default currency, payment currency, default payment method, wage rules, wage locks, payment requests, expenses, account transactions, lesson history, or non-scoped contact/payment legacy fields from the teacher profile dialog. | Editable teacher dialog fields are `name`, `department` as 老师分类 (`常勤老师`, `バイト老师`, `事务老师`), `default_subject_id` as 默认科目, `default_business_entity_id` as 默认业务归属, `status` as 在职/暂停/离职, `note`, CNY payment fields `alipay_account` / `wechat_account`, and JPY payment fields `bank_name` / `bank_branch_code` / `bank_branch_name` / `bank_account_number`. `display_name` is set internally to `name` for lookup compatibility but is not exposed separately. Teachers are lookup entities for lessons, wage rules, wage locks, expenses, and payment requests. Creation/edit is future-use master data only. | No delete flow. Separate display name, kana/reading, teacher code, default hourly rate, wage/default currency, payment currency, default payment method, bank account holder/default account name, phone/email/contact wechat, China bank account, and historical chain editing remain closed; teacher wage payment confirmation already chooses the account/currency in the payment flow. |
| 学生管理 | `student.html`, `js/pages/student-page.js`, `js/api/student-api.js` | Student list is readable. Student profile can be created and edited for future use through a narrowed, unified create/edit dialog. Dialogs preserve filters/viewport on refresh and keep typed input on validation/RPC failure. | `school_students`, `school_business_entities` | API-layer RPCs: `school_create_student_profile`, `school_update_student_profile` | Must not edit student code, balances, monthly settlements, carryovers, tuition/billing rules, income records, payment requests, lesson records, wage data, account transactions, parent fields, birthday, gender, or historical financial data from the student profile dialog. | Editable student dialog fields are `name`, `business_entity_id` as default business entity, `course_track` as 文理区分, `preset_exchange_rate`, `wechat`, `phone`, `entrance_date`, `target_schools` limited to 3 newline-separated schools, and `note`. `display_name` is set internally to `name` for lookup compatibility but is not exposed separately; new students default to `status = active` and `default_currency = CNY`. Students are lookup entities for lessons, income, monthly settlements, wage rules, and profit drilldowns. | No delete flow. Separate display name, kana/reading, target type, default currency, status, parent fields, birthday, gender, and tuition-rule editing remain closed; if needed later, they require dedicated design rather than ordinary profile-field drift. |
| 科目管理 | `subject.html`, `js/pages/subject-page.js`, `js/api/subject-api.js` | Subject list is readable. Subject profile can be created and edited for future use, including classification/display fields. Dialogs preserve filters/viewport on refresh, keep typed input on validation/RPC failure, and snapshot filters before rebuilding category options. | `school_subjects` | API-layer RPCs: `school_create_subject_profile`, `school_update_subject_profile` | Must not delete subjects or edit historical lessons, student settlements, teacher wages, payment requests, income, expenses, account transactions, system id, created_at, or updated_at directly. | Editable subject fields are `name`, status mapped to `is_active`, `primary_category`, `category`, `tertiary_category`, `color`, `sort_order`, and `note`. Subjects connect lessons, wage rules, wage lock details, and settlement/wage detail references. | `school_subjects` has no `display_name/status`; UI status maps to `is_active`, display name maps to `name`. |
| 老师工资规则 | `wage-rule.html`, `wage-rule-detail.html`, `js/pages/wage-rule-page.js`, `js/pages/wage-rule-detail-page.js`, `js/api/wage-rule-api.js` | Wage rule list is readable, detail page is complete, future-use rule config can be created, existing config including matching keys can be edited, and rules can be soft-disabled/restored instead of deleted. Dialogs preserve filters/viewport on refresh, keep typed input on validation/RPC failure, and snapshot filters before rebuilding lookup/filter options. | `school_teacher_wage_rules`, `school_teachers`, `school_students`, `school_subjects`, `school_business_entities` | API-layer RPCs: `school_create_teacher_wage_rule_config`, `school_update_teacher_wage_rule_config`, `school_set_teacher_wage_rule_active_state` | Create/edit/soft-disable/restore must not recalculate historical wages or modify wage locks, wage lock details, payment requests, expenses, account balances, or account transactions. Soft-disable/restore must not physically delete rules. | Editable wage-rule fields are `teacher_id`, `student_id`, `subject_id`, `business_entity_id`, `settlement_type`, rates, exchange rate, transport/classroom fees, and `note`; active state remains on the dedicated soft-disable/restore flow. Changing matching keys requires usable target master data, keeping the current inactive historical association is allowed, and duplicate active matches are rejected. | No physical delete flow exists by design. Generic student-empty rules and historical recalculation remain backlog. |
| 老师工资结算 | `wage.html`, `wage-detail.html`, `js/pages/wage-page.js`, `js/pages/wage-detail-page.js`, `js/api/wage-api.js`, `js/api/wage-detail-api.js` | Wage snapshot list, candidate actual preview with generated/void/student-settlement status, saved detail, teacher wage generation MVP, detail adjustment, wage-snapshot -> payment-request generation, and guarded unpaid snapshot void are complete. `wage.html` can generate wage snapshots through API wrapper `generateTeacherMonthlyWage`; generation itself freezes the wage settlement snapshot and there is no separate user-facing wage-lock action. The list hides `status = void` by default, exposes an explicit `已作废` filter, and can show read-only actual completed/makeup_completed candidate lessons alongside existing effective snapshots; void snapshots no longer hide the candidate section and candidate rows show whether each source actual is ungenerated, covered by an effective wage snapshot, linked to a void snapshot that can be regenerated, and whether the matching student monthly settlement is completed. `wage-detail.html` shows the saved snapshot, adjustment state/read-only reason, preserves list filters on return, can create one pending payment request for a wage snapshot that has no existing teacher_wage payment request, and can void a locked snapshot before any payment request or payment-chain dependency exists. | `school_teacher_wage_locks`, `school_teacher_wage_lock_details`, `school_teacher_wage_detail_adjustments`, `school_payment_requests`, `school_teachers`, `school_business_entities`, `school_lesson_records`, `school_teacher_wage_rules`, `school_student_monthly_settlements` | API-layer RPCs: `school_generate_teacher_monthly_wage` requires matching locked student monthly settlements, then writes wage locks/details from actual completed/makeup_completed lessons and groups snapshots by teacher + business entity + month; `school_adjust_teacher_wage_detail` adjusts one saved unpaid/unrequested wage detail and writes audit; `school_create_teacher_wage_payment_request` writes only one pending `school_payment_requests` row from one non-voided generated wage snapshot; `school_void_teacher_wage_lock` marks one unpaid/unrequested generated snapshot `void` with reason/operator/source audit after verifying no payment request, expense, or account transaction dependency. Read-only API helpers on `wage.html` can load actual candidate lessons, wage-lock blocker state, and student monthly settlement state before generation; they do not write DB. DB utility `school_backfill_actual_minutes_from_duration` is service-role-only and exists for guarded actual-minute synchronization, not page use. Payment confirmation/status actions remain in 老师工资支付. | Page modules must not call `.rpc()` directly or directly insert/update/delete/upsert rows. Wage generation must not modify lessons and must not write payment requests, expenses, accounts, account transactions, income, or student settlements; it only reads `school_student_monthly_settlements` to require `settlement_status = locked` for each candidate `student_id + year_month + business_entity_id`. Actual candidate preview must remain read-only and must not calculate final wage amounts as a replacement for the generation RPC. Business-entity filtering on `wage.html` is display-only for the current generation RPC; the RPC still accepts month + optional teacher and groups by teacher + business entity. Actual-minute sync/backfill may update only guarded `school_lesson_records.actual_minutes` from `duration_hours`, rejecting locked settlement/wage/payment-linked rows. Detail adjustment must not modify lesson source rows, wage rules, payment requests, expenses, accounts, account transactions, income, or student settlements, and must reject snapshots with existing teacher_wage payment requests. Payment-request generation must not confirm payment and must not write expenses, accounts, account transactions, income, student settlements, lessons, wage locks, or wage details. Snapshot void must not delete wage detail rows or source lessons, must not change `actual_minutes`, and must reject any snapshot with a payment request, expense, account transaction, or mismatched detail/header totals. | Wage detail shows saved wage snapshot and related payment requests, hiding the payment-request entry once a request exists and rendering detail adjustment/void actions as read-only with reasons when locked by payment state. Cross-month makeup actuals enter by target `teacher_settlement_month` even when student `is_billable = false`. Candidate preview reads source lessons by `teacher_settlement_month` with `year_month` fallback, displays blocker/void/effective snapshot state and student settlement completion state, and carries a wage return link into lesson detail. `trg_school_lesson_actual_minutes_sync` keeps future completed/makeup_completed actual minutes synchronized from duration. Wage generation creates `status = locked` snapshots directly in the current table structure, excludes planned/cancelled/voided lessons, ignores void wage locks/details as generation blockers, rejects existing same-teacher/business/month active locked snapshots before missing-field validation, rejects missing actual minutes, rejects missing/unlocked/mismatched-business student monthly settlements, uses `lesson_wage_jpy = round(actual_minutes / 60 * hourly_rate_jpy)`, and fixes CNY/exchange/transport/classroom fields to 0 for this MVP. Guarded lesson edit likewise ignores detail references whose parent wage lock is already void, so source actual lessons can be corrected before regenerating wages. Payment-request generation uses `currency = JPY`, `amount = amount_jpy = total_jpy`, and `amount_cny = total_cny`. | Wage amount realtime preview, business-entity-scoped generation RPC, snapshot restore/reissue beyond void-then-regenerate, CNY/FX, generation-time transport/classroom fees, broader history/backfill, and cleanup remain separate guarded backlog. |
| 学生月度结算 | `settlement.html`, `settlement-detail.html`, `js/pages/settlement-page.js`, `js/pages/settlement-detail-page.js`, `js/api/settlement-api.js`, `js/api/settlement-detail-api.js` | V1 is complete for current/future v2 operations. List/detail, realtime preview, pre-lock difference adjustment draft, preview -> locked snapshot, lock/relock draft consumption, locked read-only adjustment audit display, soft unlock, same-row relock, previous locked snapshot carryover reading, guard documentation, and closure self-check are complete. | `school_student_monthly_settlements`, `school_student_settlement_adjustment_drafts`, `school_student_settlement_adjustments`, `school_student_settlement_carryovers`, `school_students`, `school_business_entities`, `school_lesson_records`, `school_income_records`, `school_teachers`, `school_subjects`, `school_accounts` | API-layer RPCs: read-only `school_get_student_monthly_settlement_preview`, write `school_set_student_monthly_settlement_draft_adjustment` for unlocked previews, `school_lock_student_monthly_settlement`, `school_unlock_student_monthly_settlement`, and `school_relock_student_monthly_settlement`. Legacy `school_apply_student_monthly_settlement_adjustment` is retained only as a DB guard and rejects post-lock adjustment writes. | Must not physically delete settlement snapshots, change `unique(student_id, year_month)`, create multi-version history in V1, auto-create/revoke/rebuild carryovers, reverse/void adjustments without a future guarded phase, migrate/repair historical data, clean whitelist test data, or mutate lesson/income/account/wage/payment/expense rows from settlement status/adjustment flows. | Lock inserts one snapshot at `student_id + year_month`. Unlocked preview rows can save one active draft per student/month/app. Preview reads DB/RPC lesson, income, previous carryover, and active draft values; page JS does not calculate core settlement amounts. Lock consumes the active draft, posts one `school_student_settlement_adjustments` audit row, freezes `adjustment_amount_cny` and adjusted `carryover_amount_cny`, and marks the draft consumed. Unlock preserves the same row and records `unlocked_at` / `unlock_reason`; relock recalculates the same row from preview including any active draft while preserving `id`, `student_id`, `year_month`, `created_at`, `unlocked_at`, and `unlock_reason`. Locked adjustment values and posted audit records are read-only in UI/API. Active carryovers sourced from a settlement block unlock/relock. Summary carryover priority remains explicit active carryover row, previous locked monthly settlement carryover, then student fallback balance. Locked settlements block lesson/income writes through existing guards; unlocked settlements release those guards until relock. Detail shows saved settlement snapshot plus matching lesson, income, and adjustment references for audit. Historical settlement maintenance remains in v1. | Future adjustment reversal/void, multi-version settlement history, carryover revoke/rebuild, historical migration/repair, and whitelist test data cleanup require separate guarded workflows. |
| 课时管理 | `lesson.html`, `lesson-detail.html`, `js/pages/lesson-page.js`, `js/pages/lesson-detail-page.js`, `js/api/lesson-api.js`, `js/api/lesson-detail-api.js` | Ordinary list, planned/actual paired view, DB-sourced top stats, parent-facing student lesson PDF print export, detail, planned lesson manual creation V1, completed actual generation from planned V1, cancelled actual generation from planned V1, makeup_completed actual generation from planned V1, lesson import preview, planned-only template export, planned-only batch import submit, guarded lesson edit RPC/frontend on `lesson.html` / `lesson-detail.html`, planned-only lesson void DB/RPC/frontend first stage, voided planned status filtering, startup/stats DOM cleanup, and navigation polish are complete. Detail page includes source-chain tracking, settlement/wage references, guarded edit entry, filter-aware return links, and readonly voided planned display. `lesson.html` defaults to planned/actual paired view; ordinary list remains an auxiliary view. Cross-month makeup completion DB/RPC + API/UI V1 is complete. | `school_lesson_records`, `school_students`, `school_teachers`, `school_subjects`, `school_business_entities`, `school_student_monthly_settlements`, `school_teacher_wage_lock_details`, `school_teacher_wage_locks` | API-layer RPCs: `school_get_lesson_management_stats_filtered`, `school_create_planned_lesson_record`, `school_create_actual_lesson_from_planned`, `school_create_cancelled_actual_lesson_from_planned`, `school_create_makeup_completed_actual_lesson_from_planned`, `school_import_lesson_records_batch`, `school_update_lesson_record_guarded`, `school_void_planned_lesson`, and `school_create_cross_month_makeup_completed_actual_from_planned`. Page modules do not call `.rpc()` directly. | Write guards remain unchanged: planned creation is planned-only; actual/cancelled/makeup_completed/cross-month actual flows insert only linked actual rows from guarded planned sources; all actual-from-planned flows reject duplicate linked actuals, locked student settlement months, locked teacher wage months, and source planned mutation. Batch import is planned-only and rejects actual rows. Guarded edit and planned void remain narrow `school_lesson_records` updates only. PDF export is read-only/print-only and must not write DB. | Stats and PDF summaries are DB/RPC sourced; PDF export uses API-sourced lesson rows and DB/RPC stats, renders four summary cards, current-month planned/actual side-by-side cards for actual exports, and a compact six-column next-month planned table with an explicit empty state. `lessonStatsRecordCount` is the record-count surface and the obsolete `#lessonCount` dependency has been removed. Soft-voided planned lessons are excluded by default from stats, settlement summary planned totals, and normal lesson list/paired view results; `status=voided` loads only voided planned rows for review. Paired view groups loaded records by `actual.planned_lesson_id = planned.id` and cross-month references still load records outside the current month. Detail/return links and create/actual/cancelled/makeup/cross-month success refresh preserve formal filters including month, view, teacher/student/subject/business, status, `lesson_type`, `is_billable`, and keyword. | Future full lesson import for actual rows remains history-migration backlog. Lesson delete, void restore, `voided_by`, expanded wage-lock lifecycle, and DB-level linked-actual unique/index review remain separate guarded phases. |
| 利润分析 | `profit-summary.html`, `js/pages/profit-summary-page.js`, `js/api/profit-summary-api.js` | Read-only summary and drilldown are complete. Supports month, business entity, and currency filters, plus income/expense detail links. | `school_income_records`, `school_expense_records`, `school_reimbursements`, `school_payment_requests`, `school_account_transactions`, `school_business_entities` | None. | Must not write DB, adjust source records, recalculate payments/settlements/wages, or count audit-only transactions as operating profit. | Effective received income and paid expense drive operating profit by currency. Reversed income/expense are excluded. Teacher wage expense is included through paid expense records. Reimbursements, account adjustments, transfers, and payment requests are audit references. | Real browser filters/drilldown verification is recorded in `current-status.md`; no write surface exists in this module. |

## Cross-Module Write Scope Summary

| Write scope | Entry module | Allowed tables / effects | RPC boundary |
| --- | --- | --- | --- |
| Tuition income create/reverse | 收入记录 | `school_income_records`, `school_accounts`, `school_account_transactions`; read/guard against `school_student_monthly_settlements` | `school_create_income_record`, `school_reverse_income_record` |
| Ordinary expense create/reverse | 支出记录 | `school_expense_records`, `school_accounts`, `school_account_transactions`; references teacher/student/business lookups | `school_create_expense_record`, `school_reverse_expense_record` |
| Ordinary expense attachment metadata create | 支出记录 | `school_expense_attachments` metadata-only rows for ordinary non-teacher-wage expenses; no expense/account/reimbursement/account-transaction mutation | `school_create_expense_attachment_metadata` |
| Reimbursement confirm/reverse | 报销管理 | `school_reimbursements`, `school_reimbursement_items`, `school_expense_records`, `school_accounts`, `school_account_transactions`; rejects `teacher_wage` expenses so reimbursement repays advance accounts and does not recreate wage expense | `school_create_reimbursement_record`, `school_reverse_reimbursement_record` |
| Teacher wage payment actions | 老师工资支付 | `school_payment_requests`, `school_expense_records`, `school_accounts`, `school_account_transactions`; read source wage locks | `school_confirm_payment_request`, `school_reverse_paid_payment_request`, `school_cancel_payment_request`, `school_restore_cancelled_payment_request`, `school_reissue_reversed_payment_request` |
| Account adjustment/transfer | 账户管理 | `school_account_adjustments`, `school_account_transfers`, `school_accounts`, `school_account_transactions` | `school_create_account_adjustment`, `school_reverse_account_adjustment`, `school_create_account_transfer`, `school_reverse_account_transfer` |
| Master data profile create/update | 老师/学生/科目/业务归属/账户/工资规则 | Safe master/profile/config columns only; no historical financial chain writes; wage rule delete alternative is soft-disable/restore only | `school_create_teacher_profile`, `school_update_teacher_profile`, `school_create_student_profile`, `school_update_student_profile`, `school_create_subject_profile`, `school_update_subject_profile`, `school_create_business_entity_profile`, `school_update_business_entity_profile`, `school_create_account_profile`, `school_update_account_profile`, `school_create_teacher_wage_rule_config`, `school_update_teacher_wage_rule_config`, `school_set_teacher_wage_rule_active_state` |
| Planned lesson create V1 | 课时管理 | Insert one future/planned `school_lesson_records` row only after student/teacher/subject/business-entity and locked-student-settlement guards pass; no actual/wage/payment/income/expense/account side effects | `school_create_planned_lesson_record` |
| Completed actual from planned V1 | 课时管理 | Insert one linked completed actual `school_lesson_records` row only after planned-source, duplicate-actual, locked-student-settlement, and locked-teacher-wage guards pass; no source planned mutation and no wage/payment/income/expense/account side effects | `school_create_actual_lesson_from_planned` |
| Cancelled actual from planned V1 | 课时管理 | Insert one linked cancelled actual `school_lesson_records` row only after planned-source, duplicate-actual, locked-student-settlement, and locked-teacher-wage guards pass; fixed non-billable with fee/minutes zero; no source planned mutation and no wage/payment/income/expense/account side effects | `school_create_cancelled_actual_lesson_from_planned` |
| Makeup_completed actual from planned V1 | 课时管理 | Insert one linked makeup_completed actual `school_lesson_records` row only after planned-source, duplicate-actual, locked-student-settlement, and locked-teacher-wage guards pass; supports billable/non-billable, fixes non-billable fee to zero, records actual minutes from duration; no source planned mutation and no wage/payment/income/expense/account side effects | `school_create_makeup_completed_actual_lesson_from_planned` |
| Cross-month makeup_completed actual from planned V1 | 课时管理 | Insert one linked target-month makeup_completed actual `school_lesson_records` row from an earlier-month `pending_makeup` planned row through the API-layer target-month entry; UI fixes non-billable amount to zero, RPC defaults non-billable and allows billable only by explicit parameter, checks target-month student settlement and teacher wage locks, and has no source planned mutation or settlement/wage/payment/income/expense/account side effects | `school_create_cross_month_makeup_completed_actual_from_planned` |
| Teacher wage generation MVP | 老师工资结算 | Insert teacher wage snapshot rows in `school_teacher_wage_locks` and matching rows in `school_teacher_wage_lock_details` from actual `completed` / `makeup_completed` lessons only after every candidate `student_id + year_month + business_entity_id` has a matching `school_student_monthly_settlements.settlement_status = locked` row. It uses `teacher_settlement_month` / `year_month`, ignores `is_billable`, excludes planned/cancelled/voided lessons, rejects existing same-teacher same-month active non-void wage records before missing-field actual validation, rejects already-detailed actuals only when the parent wage lock is active, rejects missing/duplicate active wage rules, and has no lesson/payment/expense/account/account-transaction/income/student-settlement write side effects. The exposed page entry goes through `js/api/wage-api.js`; page modules do not call `.rpc()` directly. | `school_generate_teacher_monthly_wage` |
| Teacher wage payment request generation | 老师工资结算 | Insert one pending `school_payment_requests` row from one generated, non-voided teacher wage snapshot. It rejects snapshots with existing teacher_wage payment requests, uses the snapshot teacher/business/month/amount, and has no payment confirmation, expense, account, account-transaction, income, student-settlement, lesson, wage-snapshot, or wage-detail side effects. The exposed page entry goes through `js/api/wage-detail-api.js`; page modules do not call `.rpc()` directly. | `school_create_teacher_wage_payment_request` |
| Teacher wage unpaid snapshot void | 老师工资结算 | Mark one generated teacher wage snapshot `void` with required reason/operator/source audit only when it has no payment request, no expense, no account transaction, and matching header/detail totals. It preserves wage details as audit rows, does not delete or update source actual lessons, and releases generation/edit blockers because active guards ignore details whose parent lock is void. | `school_void_teacher_wage_lock` |
| Student settlement difference adjustment | 学生月度结算 | Save or update one pre-lock draft in `school_student_settlement_adjustment_drafts` for an unlocked preview; lock/relock consumes that draft, appends one posted `school_student_settlement_adjustments` audit row, and freezes adjustment/carryover fields on `school_student_monthly_settlements`. Locked snapshots are read-only for adjustment changes. The flow has no lesson/income/teacher-wage/payment/expense/account/account-transaction side effects. If the same student/month actual lessons are referenced by non-void teacher wage snapshots, payment requests, paid expenses, or account transactions, draft adjustment/unlock/relock are rejected by DB guard. | `school_set_student_monthly_settlement_draft_adjustment`, `school_get_student_monthly_settlement_preview`, `school_get_student_monthly_settlement_wage_blockers`, `school_lock_student_monthly_settlement`, `school_unlock_student_monthly_settlement`, `school_relock_student_monthly_settlement`; legacy `school_apply_student_monthly_settlement_adjustment` rejects post-lock writes |
| Planned-only lesson batch import V1 | 课时管理 | Insert a validated all-or-nothing batch of planned `school_lesson_records` rows only. It derives import metadata and planned date/month fields, accepts status `planned` / `pending_makeup`, rejects actual rows and planned links, guards locked student settlement months, and has no settlement/wage/payment/income/expense/account/account-transaction side effects | `school_import_lesson_records_batch` |
| Guarded lesson edit V1 | 课时管理 | Update one `school_lesson_records` row only. It preserves `lesson_type` and `planned_lesson_id`, uses `updated_at` optimistic locking, limits planned status to `planned` / `pending_makeup`, keeps actual status unchanged, blocks linked planned edits, linked actual master-data changes, locked student settlement months, locked teacher wage months, and wage detail snapshots, and has no settlement/wage/payment/income/expense/account/account-transaction side effects | `school_update_lesson_record_guarded` |
| Planned-only lesson void V1 | 课时管理 | Update one planned-only `school_lesson_records` row only by setting `voided_at` and `void_reason`. It preserves original status, lesson_type, planned_lesson_id, import_batch_id, import_source, and imported_at; rejects non-planned rows, statuses outside `planned` / `pending_makeup`, already-voided rows, linked actuals, locked student settlement months, stale updated_at, and blank reasons; no settlement/wage/payment/income/expense/account/account-transaction side effects | `school_void_planned_lesson` |

## Boundary Questions To Recheck Before Future Work

- Payment cancel/restore/reissue actions are present in current UI/API and SQL files, while `current-status.md` only names teacher wage payment confirmation and reversal in the completed write-flow summary. Treat any future change around these status actions as requiring explicit retest against the full write-RPC workflow.
- Account transfer/reversal and profit summary filters/drilldown now have real browser verification recorded in `current-status.md`; future changes around these surfaces should retest the same workflows.
- No standalone account transfer detail page exists; account transaction detail is the current audit surface.
- Lesson management V1, planned-only import, student monthly settlement V1, and cross-month makeup completion are complete/verified. Planned and actual lessons stay strictly separated; planned-only import does not import actual rows; same-month pending_makeup completion uses the existing same-month makeup_completed actual-from-planned flow; cross-month makeup completion uses dedicated guarded RPC `school_create_cross_month_makeup_completed_actual_from_planned`, does not copy planned/actual rows, does not modify the source planned row, defaults non-billable, and must remain behind API-layer wrappers rather than page-level `.rpc()` calls.
- No completed write flow exists for free actual creation outside planned flow, actual batch import, lesson delete, wage snapshot restore/reissue beyond the completed guarded unpaid void + regenerate path, adjustment reversal/void beyond the completed pre-lock student-settlement adjustment MVP, or wage rule physical deletion. Next-stage backlog candidates are payment management follow-up enhancements, weekly plan image export, full actual import / history migration, whitelist/codex-test deferred cleanup, business-entity-scoped wage generation, wage snapshot restore/reissue beyond void, and DB-level linked-actual unique/index only after read-only duplicate-risk verification.

# Current Status

- Teacher wage generation API/UI MVP checkpoint: 2026-06-10 `wage.html` now exposes `生成老师工资` through API-layer wrapper `generateTeacherMonthlyWage` in `js/api/wage-api.js`; the page module does not call Supabase `.rpc()` directly and does not directly insert/update/delete/upsert rows. The dialog uses the current filter month and optional teacher filter, explains that generation only uses actual `completed` / `makeup_completed` lessons, excludes `cancelled` and `planned`, may include `is_billable = false` actual lessons, writes only wage locks/details, and does not generate payment requests, expenses, account transactions, income, or student settlements. Successful generation refreshes the current wage lock list and keeps `wage-detail.html` as a read-only saved snapshot view. Browser validation on `http://localhost:4173` used whitelist/codex-test teacher `12f6d142-b90b-4da2-be88-310414000bd1`, month `2028-11`, setup actual lessons `82000000-0000-4000-8000-000000011001` completed, `82000000-0000-4000-8000-000000011002` non-billable `makeup_completed`, and `82000000-0000-4000-8000-000000011003` cancelled. UI generation created wage lock `7c367bf0-a2c8-467f-91c8-fb1e237fac51` and detail ids `9e26d6ab-acd5-45e1-8972-8ec2d13505ab`, `dfb642ee-6b33-4621-8679-a31a629db6f1`; verified `lesson_count = 2`, `total_minutes = 210`, `pay_hours = 3.5`, `lesson_wage_jpy = total_jpy = 15400`, `fee_jpy = 0`, and cancelled detail count `0`. Browser checks covered desktop list refresh, generated row visibility, detail page completed/makeup_completed rows, duplicate generation error displayed in-dialog, 390px dialog visibility, and document/body `scrollWidth = 390`. Network audit saw only the expected `school_generate_teacher_monthly_wage` RPC write call during duplicate-error validation and no non-RPC write requests. Protected table counts stayed unchanged after UI generation: payment requests `66`, expenses `44`, accounts `12`, account transactions `235`, income `17`, and student monthly settlements `14`. No payment request, expense, account transaction, income, student settlement, history backfill, cleanup, full actual import, or wage amount RPC change was implemented.
- Teacher wage generation DB/RPC MVP checkpoint: 2026-06-10 added and executed verified guarded RPC `public.school_generate_teacher_monthly_wage(p_year_month text, p_teacher_id uuid default null)` in `school_generate_teacher_monthly_wage_rpc.sql`. The RPC generates saved teacher wage snapshots from actual `completed` / `makeup_completed` lessons only, uses `teacher_settlement_month` / `year_month` for the target wage month, ignores `is_billable` for teacher wage, excludes `planned`, `cancelled`, and voided lessons, and writes only `school_teacher_wage_locks` plus `school_teacher_wage_lock_details`. It generates `status = locked` snapshots, blocks existing same-teacher same-month wage records, blocks actual lessons already referenced by wage details, blocks missing or duplicate active wage-rule matches, and rejects same-teacher same-month candidates spanning multiple business entities because the current wage lock header has one business-entity field. User-confirmed MVP scope excludes CNY/FX and transport/classroom fees, so generated CNY/exchange/fee fields are `0`; amount formula is `lesson_wage_jpy = round(actual_minutes / 60 * hourly_rate_jpy)`, confirmed by read-only DB verification against existing 388 detail rows. Rollback test used codex-test actual lesson ids `80000000-0000-4000-8000-000000008001`, `80000000-0000-4000-8000-000000008002`, `80000000-0000-4000-8000-000000008003`, temporary wage lock `3184381f-fafe-458a-ae93-30bceda3cc6c`, and locked-guard test lock `80000000-0000-4000-8000-000000009099`, then rolled back with lessons/locks/details residue `0`. Whitelist commit test used teacher `12f6d142-b90b-4da2-be88-310414000bd1`, month `2028-10`, actual lesson ids `81000000-0000-4000-8000-000000010001` completed, `81000000-0000-4000-8000-000000010002` non-billable `makeup_completed`, and `81000000-0000-4000-8000-000000010003` cancelled; created wage lock `f5fe1fe3-f9e1-45d4-ac50-270c9b609d58` and detail ids `aad48406-c0cc-499b-b2a5-0fd7e1709688`, `1ad4f156-e869-4a36-aa47-c12edaa18da6`. Verified totals: `lesson_count = 2`, `total_minutes = 210`, `pay_hours = 3.5`, `lesson_wage_jpy = total_jpy = 15400`, `fee_jpy = total_cny = 0`; cancelled detail count `0`; duplicate generation and locked-existing generation were rejected. Protected table counts stayed unchanged for payment requests `66`, expenses `44`, accounts `12`, account transactions `235`, income `17`, and student monthly settlements `14`. At this DB/RPC checkpoint no API wrapper or UI entry was implemented yet; the later API/UI checkpoint above supersedes that page-readonly status.
- Teacher wage generation design checkpoint: 2026-06-10 completed repo-only, no-SQL/no-RPC/no-DB read design for the teacher wage generation phase in `docs/teacher-wage-generation-design.md`. Current facts: `wage.html` / `wage-detail.html` are read-only saved wage lock surfaces over `school_teacher_wage_locks` and `school_teacher_wage_lock_details`; wage rules are future-lock configuration only; lesson actual creation/edit already guards locked teacher wage months and wage detail references; payment confirmation is a later payment-module flow and is the point that can create teacher_wage expense/account transaction chain. The later DB/RPC MVP checkpoint above supersedes the design-only implementation status and fixes the initial open口径 for this MVP: cancelled actual is excluded, no draft model, no CNY/FX, no transport/classroom fees, `lesson_count` is detail count, and payment request generation remains a separate later phase. At the design checkpoint no SQL draft was written, no SQL/RPC was executed, and no DB was read or written.
- Lesson management completion/backlog checkpoint: 2026-06-10 docs-only consistency pass explicitly marks lesson management V1 as 完成 / 已验证, planned-only import as 完成 / 当前稳定源头, student monthly settlement V1 as 完成 / 已验证, and cross-month makeup completion as 完成 / 已验证. Hard boundaries are unchanged: planned and actual lessons stay strictly separated; planned-only import does not import actual lessons; same-month pending_makeup completion uses the existing same-month makeup_completed actual-from-planned flow; cross-month makeup completion uses dedicated guarded RPC `school_create_cross_month_makeup_completed_actual_from_planned`; cross-month completion does not copy planned or actual rows, does not modify the source planned lesson, and defaults to non-billable / lesson fee 0; page modules must not call Supabase `.rpc()` directly. Next-stage backlog candidates are teacher wage generation, payment management follow-up enhancements, weekly plan image export, full actual import / history migration, whitelist/codex-test deferred cleanup, and DB-level linked-actual unique/index consideration only after read-only duplicate-risk verification. This checkpoint changed docs only; no SQL/RPC was executed, no DB was written, no cleanup/backfill/full actual import/wage-generation implementation was started.
- Cross-month makeup completion closure regression checkpoint: 2026-06-10 completed rollback guard tests and read-only browser regression for the implemented API/UI V1. Rollback test 1 proved source-month student settlement locking does not block cross-month completion when the target actual month is unlocked: temporary source planned `c5e9e664-7453-4391-a915-0071bfdc61f9`, source settlement lock `76b22099-55ce-4fff-a7b8-08bf7f05ccf8`, and actual `6e5f9cd6-ad4b-4560-8cad-fbb5ceb0be45` were created inside the transaction, linked once, then rolled back with residue `0`. Rollback test 2 proved target-month student settlement lock rejection: temporary source planned `a862982b-0294-4efe-8eba-e3c42f8f1b88`, target setup planned `ed4af32f-c3f2-436d-934b-827808d78feb`, and target settlement lock `91f0224d-b7e9-4931-910c-3e78d8ed22df` caused `补课月份学生月度结算已锁定，不能登记跨月补课完成。`, wrote no linked actual, and rolled back with residue `0`. Rollback test 3 proved target-month teacher wage lock rejection: temporary source planned `10d4113f-5ef2-4b34-9570-4bb2bfb35121` plus temporary wage lock `e6feb09e-e2eb-45bd-bdf0-909aa67f65e6` caused `补课月份老师工资已锁定，不能登记跨月补课完成。`, wrote no linked actual, and rolled back with residue `0`. Persistent UI test chain remained source planned `d97acf8d-47f1-4b06-9eef-459521aaecb1` -> actual `50ea35b7-c746-4ffb-88e7-f2c11758258c`; read-only DB verification confirmed source still `pending_makeup planned`, actual `year_month = 2027-12`, `planned_lesson_id` points to source, `is_billable = false`, `lesson_fee = 0`, `actual_minutes = 120`, and linked actual count `1`. `school_get_student_monthly_settlement_summary` for the test student/month `2027-12` returned `actual_hours = 0` and `actual_fee_jpy = 0`, so the non-billable cross-month actual does not enter student settlement actual fee/hours. Browser read-only regression on `http://localhost:4173` confirmed linked source is absent from the cross-month candidate dialog, target month shows `来源：2027-11 待补课`, source month shows `已于 2027-12 完成`, source planned `补课完成` action count is `0`, 390px mobile `scrollWidth = 390`, and no non-GET or `/rpc/` requests occurred. Final protected counts stayed at lessons `369`, settlements `14`, wage locks `70`, wage lock details `388`, income `17`, expenses `44`, accounts `12`, account transactions `235`, and payment requests `66`. No SQL/schema/RPC file was added or executed, no DB-level unique/index constraint was added, no historical backfill or cleanup was performed.
- Cross-month makeup completed API/UI checkpoint: 2026-06-10 `lesson.html` now exposes an independent `登记跨月补课完成` target-month entry backed by API-layer wrappers in `js/api/lesson-api.js`; page modules still do not call `.rpc()` directly. The dialog reads an original-month range and lists only earlier-month `pending_makeup` planned lessons that are not voided and have no linked actual, defaults to non-billable, fixes lesson fee to `0`, and states that it will not copy planned, will not modify the source planned, and will only create one target-month `makeup_completed` actual. Same-month pending_makeup planned lessons still use the existing paired-card `补课完成` flow. Paired view now loads read-only cross-month references: source months display `已于 YYYY-MM 完成` on the actual side and hide the source planned `补课完成` action once a linked actual exists; target months display `来源：YYYY-MM 待补课` on the planned side for cross-month makeup actuals. Browser validation on `http://localhost:4173` used whitelisted source planned `d97acf8d-47f1-4b06-9eef-459521aaecb1` and UI-created cross-month actual `50ea35b7-c746-4ffb-88e7-f2c11758258c` for target month `2027-12`; actual `planned_lesson_id` points to the source, `is_billable = false`, `lesson_fee = 0`, `actual_minutes = 120`, and `teacher_settlement_month = 2027-12`. Source candidate reread after success confirmed the linked source no longer appears in the dialog; target-month paired view showed `来源：2027-11 待补课`, source-month paired view showed `已于 2027-12 完成`, source planned makeup action count was `0`, and 390px mobile `scrollWidth = 390`. Protection counts changed only for lessons `367 -> 369` because the test setup created one whitelisted planned source and the UI created one linked actual; settlements `14`, wage locks `70`, wage lock details `388`, income `17`, expenses `44`, accounts `12`, account transactions `235`, and payment requests `66` stayed unchanged.
- Cross-month makeup completed RPC checkpoint: 2026-06-10 added and executed verified RPC `public.school_create_cross_month_makeup_completed_actual_from_planned` in `school_create_cross_month_makeup_completed_actual_from_planned_rpc.sql`. This RPC is dedicated to cross-month makeup completion and does not reuse `school_create_makeup_completed_actual_lesson_from_planned`: source must be `lesson_type = planned`, `status = pending_makeup`, `voided_at is null`, and have no linked actual; target actual is fixed to `lesson_type = actual`, `status = makeup_completed`, `year_month = to_char(p_lesson_date, 'YYYY-MM')`, `planned_lesson_id = source.id`, and target month must be later than source `year_month`. It defaults `p_is_billable` to `false`; callers must explicitly pass `true` to bill the cross-month makeup result. It inserts only one actual lesson row, does not copy planned/actual rows, does not modify the source planned row, and does not write student settlements, teacher wage locks/details, income, expense, accounts, account transactions, or payment requests. Read-only preflight found one existing historical linked-actual duplicate group, so no DB-level unique/index constraint was added; existing cross-month linked actual count was `0`. Rollback tests created only temporary codex-test planned/actual rows and left no residue, covering successful default non-billable insertion, duplicate linked-actual guard, same-month guard, target student settlement lock guard, and target teacher wage lock guard. Whitelist commit test created source planned lesson `19b574d5-78e0-43fb-8248-e2cd9c2c68af` and actual lesson `8baa4f13-f290-4332-819c-d8ba20906df4`; actual `year_month = 2027-10`, `planned_lesson_id` points to the source, `is_billable = false`, `lesson_fee = 0`, `actual_minutes = 120`, and `teacher_settlement_month = 2027-10`. Protection counts changed only for lessons `365 -> 367`; settlements `14`, wage locks `70`, wage lock details `388`, income `17`, expenses `44`, accounts `12`, account transactions `235`, and payment requests `66` stayed unchanged. No API wrapper or UI entry was implemented in this checkpoint.
- Lesson default paired view and cross-month makeup design checkpoint: 2026-06-10 `lesson.html` now defaults to the planned/actual `左右对应` view, with `普通列表` retained as the auxiliary explicit view. Invalid or missing `view` query values now fall back to `pair`, while `view=list` still opens the ordinary table view; reset returns to the default paired view. Browser read-only validation on `http://localhost:4173` confirmed default paired visibility, ordinary-list toggle, reset back to paired view, explicit `view=list` support, 390px document/body `scrollWidth = 390`, and no write/network mutation beyond GET reads. At that checkpoint cross-month makeup completion was still design-only; the API/UI checkpoint above supersedes that implementation status while preserving the same no-copy and no-source-mutation boundary. This checkpoint changed frontend view defaults and docs only; no SQL/RPC/schema was added or executed, and the DB was not written.
- Lesson planned-only import template cache-bust checkpoint: 2026-06-09 synchronized the standard `导出预定课时模板` Excel download after user reported the downloaded file still showed old fields. The template generation logic in `js/pages/lesson-page.js` already uses the planned-only headers `学生 / 老师 / 科目 / 业务归属 / 日期 / 开始时间 / 结束时间 / 课时类型 / 状态 / 课时 / 回数 / 课时费总额 JPY / 是否计费 / 内容 / 备注`, two planned-only sample rows, and a guide sheet stating that `课时费总额 JPY` is the total lesson fee rather than unit price. This checkpoint updates `lesson.html` CSS/module cache-bust to `v2.78.0-lesson-import-template-sync-20260609` so browsers load the corrected template code instead of cached old JS. Browser no-write validation downloaded `lesson_planned_import_template_v2.xlsx`, confirmed headers include `回数` and `课时费总额 JPY`, confirmed headers exclude `金额` and `关联预定ID`, confirmed the guide mentions total-fee-not-unit-price, re-uploaded the downloaded template with current page lookup labels, and confirmed preview recognizes `回数` values `3` / `4` plus total fees `5000` / `7500`; the preview had no row errors, only inactive-master warnings from the selected test lookup data, and 390px document/body overflow stayed false. No SQL/RPC/schema changed and the DB was not written.
- Lesson planned-only import preview/template/dialog fix checkpoint: 2026-06-09 fixed the planned-only lesson import flow after user testing. Student, teacher, subject, and business-entity lookup in `lesson.html` preview is now exact-only after trim/Unicode/whitespace normalization; fuzzy `includes` matching was removed, so short names such as `测试学生` / `测试老师` are not guessed into codex-test master data. Preview now flags same-file duplicate planned rows with a normalized key of student, teacher, subject, business, date, start, end, status, duration, and total amount, and reports the first duplicated data row. `回数` is parsed into `lesson_count` and sent through the existing API/RPC payload; planned rows ignore legacy `关联预定ID` values and never submit a link. The template now includes `回数`, labels amount as `课时费总额 JPY`, removes `关联预定ID` from the planned-only template, and documents that amount is the total fee, not unit price. The import dialog now keeps errors inside the dialog, disables submit when preview has errors, keeps the selected file/preview after error and dismiss attempts, prevents backdrop/Esc close, and keeps the bottom actions reachable with a scrollable content body. Browser no-write validation at 390px covered exact-only student/teacher rejection, subject/business lookup errors, invalid date error, completed/cancelled actual rows blocked in preview, row 10 duplicate row 1, disabled submit on errors, template export headers, legacy planned ID ignored, footer visibility, no horizontal overflow, and no write requests/RPC. No SQL/RPC/schema changed and the DB was not written.
- Student settlement V1 closure checkpoint: 2026-06-09 completed a no-write self-check after user real-device validation. Student monthly settlement V1 is now marked complete for current/future v2 operations: realtime preview, preview -> lock, soft unlock, same-row relock, locked guard behavior, and active-carryover unlock/relock protection are all implemented and documented. Code review confirmed `preview` / `locked` / `unlocked` list/detail status labels are clear (`未锁定 / 预览`, `已锁定`, `锁定已撤销`), lock/unlock/relock calls are centralized in `js/api/settlement-api.js`, and `settlement.html` / `settlement-detail.html` page modules contain no direct `.rpc()` or direct insert/update/delete/upsert. Existing docs already record that locked settlements block lesson edit, planned void, actual generation, tuition income create, and tuition income reverse through verified RPC guards; they also record that active `school_student_settlement_carryovers.source_settlement_id` blocks unlock/relock. V1 intentionally does not include historical versions, multi-version snapshots, adjustment editing, automatic carryover revoke/rebuild, historical data migration, historical data repair, or whitelist test data cleanup. Historical maintenance remains in the existing v1 system; v2 settlement V1 is scoped to current and future operations only. This checkpoint changed docs only, did not execute SQL/RPC, did not write DB, and did not clean test data.
- Student settlement unlock/relock frontend checkpoint: 2026-06-09 `settlement.html` and `settlement-detail.html` now expose verified V1 soft unlock and same-row relock through API-layer wrappers `unlockStudentMonthlySettlement` and `relockStudentMonthlySettlement` in `js/api/settlement-api.js`; page modules still contain no direct `.rpc()` or direct insert/update/delete/upsert. List and detail now label `unlocked` as `锁定已撤销`; locked saved rows/detail show `撤销锁定`, unlocked rows/detail show `重新锁定`, and preview rows keep the existing preview -> lock flow. Unlock dialog requires a nonblank reason plus confirmation checkbox and keeps validation/RPC errors inside the dialog. Relock dialog accepts optional note plus confirmation checkbox and keeps validation/RPC errors inside the dialog. Success refreshes the current list month or current detail page. Browser validation used whitelisted codex-test snapshot `15975c66-0272-4343-8cac-d3953c8f6ba2`: list locked row unlock wrote `unlock_reason = codex-test / v2-test / sandbox / v2.76.0 frontend unlock browser verification`, list then showed `锁定已撤销` and `重新锁定`, detail showed unlocked status and relock action, detail relock wrote `note = codex-test / v2-test / sandbox / v2.76.0 frontend relock browser verification`, and final detail/list state returned to `locked`. Active carryover guard validation used source settlement `41e018bd-bbf6-4673-83d9-56b8c71c49c4` / carryover `6487e0e6-f23a-4f4b-a581-ae2fa70ef07b`; RPC rejected unlock with `该结算已生成有效结转，不能撤销锁定。` inside the dialog and left the settlement locked with no unlock audit. 390px checks passed for list and detail with document/body `scrollWidth = 390`; only expected RPC calls were read-only summary RPCs plus `school_unlock_student_monthly_settlement` and `school_relock_student_monthly_settlement`. Protection counts stayed stable: settlements `14` all locked, carryovers `8`, lessons `364`, income `17`, expenses `44`, account transactions `235`, wage locks `70`, wage lock details `388`, payment requests `66`. No SQL/RPC/schema, history versioning, adjustment, carryover rebuild, or historical-data maintenance was changed.
- Student settlement unlock/relock DB/RPC checkpoint: 2026-06-09 `school_student_monthly_settlement_unlock_fields_schema.sql` added nullable `unlocked_at` and `unlock_reason` to `school_student_monthly_settlements` without backfill, status rewrite, unique-constraint change, or carryover mutation. `school_unlock_relock_student_monthly_settlement_rpc.sql` added verified RPCs `public.school_unlock_student_monthly_settlement(p_settlement_id uuid, p_reason text)` and `public.school_relock_student_monthly_settlement(p_settlement_id uuid, p_note text default null)`. Unlock changes only one locked snapshot row to `settlement_status = unlocked`, records required reason plus `unlocked_at`, and rejects non-locked rows, blank reasons, and active `school_student_settlement_carryovers.source_settlement_id` references. Relock only allows `unlocked` rows, preserves `id`, `student_id`, `year_month`, `created_at`, `unlocked_at`, and `unlock_reason`, recalculates snapshot amounts from `school_get_student_monthly_settlement_summary`, overwrites business entity, exchange rate, planned/actual/received/final-due fields, fixes `adjustment_amount_cny = 0`, clears `adjustment_reason`, sets `settlement_status = locked`, `locked_at`, note, and `updated_at`, and rejects active carryover references. `school_lock_student_monthly_settlement` remains insert-only and `unique(student_id, year_month)` is unchanged. Rollback tests left no residue and covered invalid relock while locked, blank unlock reason, successful unlock, lesson edit and tuition income allowed while unlocked, successful same-row relock, lesson edit and tuition income rejected after relock, active carryover rejecting unlock, and active carryover rejecting relock. Commit test wrote only whitelisted codex-test snapshot `15975c66-0272-4343-8cac-d3953c8f6ba2`, preserving its row identity and final `locked` status while setting `unlocked_at = 2026-06-09 07:53:57.793323+00`, `unlock_reason = codex-test / v2-test / sandbox / v2.75.0 commit unlock reason`, and `note = codex-test / v2-test / sandbox / v2.75.0 commit relock note`; relock snapshot totals matched the summary RPC. Protection counts after commit stayed stable: settlements `14` all locked, carryovers `8`, lessons `364`, income `17`, expenses `44`, account transactions `235`, wage locks `70`, wage lock details `388`, payment requests `66`. No frontend/API wrapper or unlock/relock UI was implemented in this checkpoint.
- Student settlement lock frontend checkpoint: 2026-06-09 `settlement.html` now exposes V1 lock from realtime preview rows only. `js/api/settlement-api.js` adds the API-layer wrapper for verified RPC `school_lock_student_monthly_settlement`, while `js/pages/settlement-page.js` keeps the page module free of direct `.rpc()` / insert / update / delete / upsert calls. Preview rows show `预览` plus `锁定`; locked snapshot rows still show the existing `详情` link. The lock dialog shows the current preview summary, optional note, required confirmation checkbox, dialog-local validation/errors, and refreshes the current month after success so the row becomes a saved snapshot/detail row. No SQL/RPC/schema definition changed. Browser validation used whitelisted test student `b48cd784-62e2-4232-a07c-65414e842b89`: created test planned sources `abdd9db7-3b56-4cf1-b1df-1be3212f551e` for `2026-09` and `33fdae7d-6f72-4e31-813e-cf0fd99c064c` for `2026-11`; UI lock created snapshot `15975c66-0272-4343-8cac-d3953c8f6ba2`, and prelock duplicate setup created snapshot `02339a5d-c365-4c86-9696-a2305bb9f5ca`. Duplicate lock retry showed `该学生月份已存在结算快照，不能重复锁定。` inside the dialog and kept the dialog open. Post-lock guard smoke tests rejected lesson edit, planned void, actual generation, and tuition income create for the locked test month. Request audit showed only expected read requests, read-only summary RPC calls, and expected `school_lock_student_monthly_settlement` calls; 390px mobile `scrollWidth = 390`.
- Student settlement lock DB/RPC checkpoint: 2026-06-09 `school_lock_student_monthly_settlement_rpc.sql` added verified guarded RPC `public.school_lock_student_monthly_settlement(p_student_id uuid, p_year_month text, p_note text default null)`. The RPC locks one realtime preview into `school_student_monthly_settlements` at existing DB granularity `student_id + year_month`, rejects invalid month format, non-school or missing students, students without default `business_entity_id`, duplicate snapshots, and student/months with no effective source. It maps the student's default business entity plus `school_get_student_monthly_settlement_summary` output into the saved snapshot: exchange rate, planned/actual fee, received amounts, previous balance, final due, `adjustment_amount_cny = 0`, `carryover_amount_cny = final_due_cny`, `settlement_status = locked`, `locked_at`, note, created_at, and updated_at. It does not write next-month carryovers, lessons, income, teacher wage, accounts, payments, expenses, account transactions, unlock/relock state, adjustment edits, or snapshot detail rows. Rollback tests covered successful temporary lock plus duplicate, no-source, and missing-default-business guards with no residue. Commit test wrote only whitelisted codex-test source lesson `dea88720-b4dd-4395-9f9b-937d01940192` and locked snapshot `b15a50fa-9437-461e-9e44-e51f8747640b`; post-lock guards rejected lesson edit, planned void, completed/cancelled/makeup actual generation, tuition income create, and tuition income reverse. Protection counts after commit: settlements `11`, lessons `362`, income `17`, expenses `44`, account transactions `235`, wage locks `70`, wage lock details `388`, payment requests `66`.
- Student settlement preview candidate granularity checkpoint: 2026-06-09 `settlement.html` realtime preview V1 now generates preview rows at `student_id + year_month` granularity instead of `student_id + year_month + business_entity_id`. If any saved snapshot already exists for the same student/month, no additional preview row is generated, even when lesson or tuition-income source rows exist under another business entity. Preview rows still call the existing read-only `school_get_student_monthly_settlement_summary(p_student_id, p_year_month)` and display the student's default business entity as the row business entity, with source-row business entity only as a fallback. This avoids misleading split business-entity preview rows while the summary RPC remains student/month scoped. The 2026-05 李天伦 / 青空进学塾 preview row is therefore suppressed by the existing 李天伦 / 个人名义 locked snapshot; 2026-06 unlocked preview rows remain available, with at most one preview row per student/month. No SQL/RPC/schema definition changed and the DB was not written.
- Student settlement realtime preview checkpoint: 2026-06-09 `settlement.html` now merges saved student monthly settlement snapshots with read-only realtime preview rows for months that have lesson or tuition-income candidates but no saved snapshot for that student/business pair. `js/api/settlement-api.js` remains the only layer calling Supabase and now builds candidates from `school_lesson_records` plus tuition `school_income_records`, then calls read-only RPC `school_get_student_monthly_settlement_summary(p_student_id, p_year_month)` for unsaved preview rows. Page modules still do not call `.rpc()` directly and no insert/update/delete/upsert path was added. Preview rows are marked `未锁定 / 预览`, show a `预览` badge instead of a detail link, and state that they are not saved snapshots; existing snapshot rows keep their `详情` link and snapshot status. `settlement-detail.html` was not changed and continues to read saved snapshots only. Browser no-write validation showed 2026-06 now renders 7 preview rows, 2026-05 keeps 5 locked snapshot rows with detail links and adds 1 preview row, preview planned JPY total equals the active planned total `1,196,500` and excludes the voided planned `20,000`, no unsafe write request was observed, and 390px mobile `scrollWidth = 390`. No SQL/RPC/schema definition was changed and the DB was not written.
- Lesson detail return query checkpoint: 2026-06-09 `lesson.html` detail links now include a sanitized `returnQuery` carrying the formalized lesson filter URL state (`year`/`month`, `view`, `teacher_id`, `student_id`, `subject_id`, `business_entity_id`, and `status`) in addition to the older `returnYear` / `returnMonth` / `returnView` fallback parameters. `lesson-detail.html` now prefers `returnQuery` for the top return link, so clicking `返回 YYYY-MM 课时管理 / 对应视图` preserves the same filters as browser Back, including `status=voided`; old detail URLs that only provide year/month/view still return normally. This did not change auth/login flow, stats/RPC口径, edit/create/void behavior, dialog sizing, SQL/RPC/schema, or DB data. Browser no-write validation covered filtered list -> detail -> top return with teacher/student/subject/business/status preserved, browser Back preservation, `status=voided` detail round-trip, old `returnYear` / `returnMonth` / `returnView=pair` fallback, and 390px mobile `scrollWidth = 390`; no write RPC or non-GET business request was observed.
- Lesson edit dialog size checkpoint: 2026-06-09 `lesson.html` and `lesson-detail.html` now apply a new complex-form dialog size class (`app-dialog--xl`) plus scoped density class (`lesson-edit-dialog-panel`) only to the shared lesson edit dialog. Standard lesson create / actual generation / planned void dialogs keep the existing `lesson-create-dialog-panel` sizing, small master dialogs keep the base 520px dialog, and standard business dialogs such as account and income remain at the existing 820px width. The edit dialog summary, warning/error blocks, four-column desktop field grid, and content/note textarea default height were tightened to reduce vertical scrolling without changing guarded edit behavior, API/RPC paths, void behavior, actual generation dialogs, or stats/settlement口径. Browser no-write validation on `lesson.html` and `lesson-detail.html` showed desktop edit dialog width `1120px` with `scrollDelta = 0`, 390px mobile document/body `scrollWidth = 390`, dirty-cancel confirmation still requiring a second cancel click, lesson create/actual/void panels without `app-dialog--xl`, and subject/account/income dialogs unaffected. No SQL/RPC/schema was added or executed, and the DB was not written in this checkpoint.
- Lesson planned void entry hierarchy checkpoint: 2026-06-09 `误录作废` is now detail-only. `lesson.html` ordinary list and paired view cards no longer render the planned void action, so the list/card surfaces keep only lower-risk navigation/edit/actual-generation actions and users must open `lesson-detail.html` before reaching the data-correction void flow. `lesson-detail.html` still shows `误录作废` only when the shared guarded void controller says the planned-only row is eligible, and the detail button now uses the danger button style to distinguish it from normal edit. Existing dialog copy, required reason, confirmation checkbox, backdrop/Esc blocking, dialog-local errors, void guards, readonly voided detail display, stats/settlement口径, SQL/RPC/API behavior, import undo, and void restore behavior were not changed. No DB write was performed in this checkpoint.
- Lesson planned void copy checkpoint: 2026-06-09 planned lesson void remains the same guarded DB/RPC/API flow, but the user-facing positioning now labels the operation as `误录作废` rather than an ordinary cancellation action. `lesson.html` list/paired actions and `lesson-detail.html` detail action show `误录作废`; the shared void dialog title is `误录作废预定课时`, the reason field and submit state use `误录作废原因` / `确认误录作废`, and the dialog now states that this operation is only for duplicate import, manual mis-entry, or other data correction. It explicitly tells users not to use void for student/teacher leave, cancellation, makeup handling, or settlement adjustment, and to preserve real planned lessons then handle them through actual lessons or monthly settlement. Status filter `已作废` and readonly detail state remain unchanged. No SQL/RPC/schema, guard, stats, settlement, import undo, or void restore behavior changed, and the DB was not written in this checkpoint.
- Lesson URL filter checkpoint: 2026-06-09 `lesson.html` now formalizes the main lesson-management filter state in URL query parameters. The page restores `year`/`month` or `year_month`, `view`, `teacher_id`, `student_id`, `subject_id`, `business_entity_id`, and `status` during initialization before the first lesson query, including the special `status=voided` mode for readonly planned-voided review. Clicking `查询` writes the current main filter state to the URL without changing stats/RPC口径 or triggering requests from ordinary filter changes; clicking `重置` clears the filter query and returns to the default current-month list view. `lesson.html` module cache bust was updated to `v2.66.0-lesson-query-filters-20260609`. Browser no-write validation covered query restore through `year/month` and `year_month`, `已作废` restore, all main master-data id filters, query URL sync, refresh persistence, reset URL cleanup/default filters, and 390px no-overflow; no `/rpc` or non-GET requests were observed. No SQL/RPC/schema was added or executed, and the DB was not written in this checkpoint.
- Lesson JS idempotent init checkpoint: 2026-06-09 `initLessonPage()` and `initLessonDetailPage()` now return early after the first successful module-level initialization, preventing duplicate page event binding and duplicate data loading when the exported init functions are called again. Shared `lesson-edit-dialog` and `lesson-void-dialog` controller `init()` functions now have per-controller idempotent guards; edit controller still auto-initializes on creation for compatibility and now also exposes `init()` so page setup can call it safely. No business behavior, UI text, SQL/RPC, or API write path changed. Browser no-write validation on `lesson.html` / `lesson-detail.html` repeatedly called the page init exports and confirmed `0` extra Supabase requests; direct component checks confirmed edit and void controllers each bound only one document `keydown` listener across repeated `init()` calls. Lesson edit, lesson void, actual generation, detail edit, and detail void dialogs opened and closed normally, with no write/RPC requests observed. No SQL file was created or executed, and the DB was not written in this checkpoint.
- Lesson void status filter checkpoint: 2026-06-09 `lesson.html` status filter now uses the fixed business options `全部 / 待上课 / 待补课 / 已上课 / 取消课 / 已补课 / 已作废`. The `已作废` option is a frontend/API query mode for `lesson_type = planned and voided_at is not null`, not `status = voided`; normal `全部` and all non-voided status filters still exclude voided planned rows. `js/api/lesson-api.js` keeps the default monthly query excluding voided planned rows and only loads voided planned rows when the status filter is `voided`; `js/pages/lesson-page.js` now treats the status filter as business state, keeps paired/list default views clean, and lets voided planned rows link to `lesson-detail.html`. Browser no-write validation covered the dropdown options, default exclusion, `已作废` listing/detail readonly display for `bce0af13-7452-46c1-90e0-641b1dc78d3b`, and filters for `待上课`, `待补课`, `已上课`, `取消课`, and `已补课`; no write RPC/non-GET RPC requests were observed. No SQL file was created or executed, and the DB was not written in this checkpoint.
- Lesson planned-only void frontend checkpoint: 2026-06-09 `lesson.html` / `lesson-detail.html` now expose planned-only `作废` through shared `js/components/lesson-void-dialog.js` and API-layer wrapper `voidPlannedLesson`, with page modules still containing no direct `.rpc()` or direct insert/update/delete/upsert. Lesson API reads `voided_at` / `void_reason`; monthly lesson list queries default-exclude voided planned rows at query layer, so ordinary list and paired view hide voided planned records by default. The shared edit dialog now blocks voided planned edits. Detail direct-open for a voided planned row displays `已作废`, `作废时间`, and `void_reason`, disables edit, hides the `作废` entry, and exposes no actual-generation actions. The 作废 dialog requires a nonblank `void_reason`, a second confirmation checkbox, blocks backdrop/Esc dismissal, calls guarded RPC `school_void_planned_lesson`, and keeps validation/RPC errors inside the dialog. Browser validation wrote only whitelisted codex-test planned lesson `bce0af13-7452-46c1-90e0-641b1dc78d3b`, setting `voided_at` and `void_reason = codex-test / v2-test / sandbox / v2.63.0 frontend planned void browser verification`; follow-up checks confirmed reason-required dialog error, remaining planned-only row `aeb9beac-5dc3-478a-b4bf-8660fa86c43d` shows `作废`, linked actual planned row `9d33f158-1781-46be-b6b4-d1cee837dc9e` shows no `作废`, voided row is absent from list and paired view, and direct detail is read-only. No SQL file was created or executed in this checkpoint.
- Lesson planned-only void DB/RPC checkpoint: 2026-06-09 `school_lesson_records_void_fields_schema.sql` added nullable `voided_at` and `void_reason` to `school_lesson_records` with no historical backfill and no status rewrite. New verified RPC `public.school_void_planned_lesson(p_lesson_id uuid, p_expected_updated_at timestamptz, p_void_reason text)` soft-voids only planned-only rows by setting `voided_at` / `void_reason`, preserves original `status`, `lesson_type`, `planned_lesson_id`, `import_batch_id`, `import_source`, and `imported_at`, and rejects non-planned rows, statuses outside `planned` / `pending_makeup`, already-voided rows, linked actuals, locked student settlement months, stale `updated_at`, and blank reasons. Existing RPC archives now reject voided planned sources in `school_create_actual_lesson_from_planned`, `school_create_cancelled_actual_lesson_from_planned`, and `school_create_makeup_completed_actual_lesson_from_planned`; `school_update_lesson_record_guarded` rejects voided planned edits; `school_get_lesson_management_stats` and `school_get_student_monthly_settlement_summary` exclude voided planned rows from planned totals by default. Rollback tests covered successful void with no residue, all three actual-generation guards, guarded edit rejection, stats/settlement exclusion, blank reason, stale updated_at, linked planned, actual row, and repeat-void guards. Persistent commit test was limited to whitelisted codex-test planned lesson `62288630-2005-4ace-be9d-34343d715bc2`, setting only `voided_at` / `void_reason = codex-test / v2-test / sandbox / v2.63.0 commit planned void`. Protection counts stayed unchanged at student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, account transactions `235`, and actual lesson records `151`. No frontend/API wrapper or void button was implemented in this checkpoint.
- Lesson high-priority feedback checkpoint: 2026-06-09 `lesson.html` and shared lesson edit dialog now protect lesson business input dialogs from accidental data loss: backdrop clicks and Esc no longer close create/edit lesson dialogs, and changed forms require a second Cancel click before closing. Start/end time changes now calculate duration automatically without rounding, require `end_time > start_time`, require a 15-minute multiple, and keep errors inside the active dialog with field highlights. Lesson list ordering now uses date ASC, fixed subject rank (`日语/JLPT` -> `数学` -> `物理` -> `化学` -> `文综` -> other), subject name, lesson_count ASC nulls last, start_time ASC nulls last, created_at ASC, and id as a final stable tie-breaker; API fetch includes `created_at` and applies the query-layer sortable parts. Read-only DB verification confirmed actual subject values in use include `EJU日语`, `JLPT`, `EJU数学`, `EJU物理`, `EJU化学`, `EJU文综`, and other subjects. No SQL/RPC/schema was added or executed, no DB writes were performed, page modules still have no direct `.rpc()` or direct insert/update/delete/upsert, and browser/CDP no-write validation saw no RPC or non-GET requests while covering actual-generation dialog close protection, 15-minute duration errors, auto duration `1.25`, `end_time` guard, full 2026-06 rendered order matching DB-computed target order for 76 rows, and 390px no-overflow.
- Lesson navigation polish checkpoint: 2026-06-08 `lesson.html` planned-only import success results now keep the existing `查看 YYYY-MM` action and add a low-risk `查看首条详情` action plus row-level `已导入 shortId` detail links. The links carry `returnYear` / `returnMonth` / optional `returnView=pair`, so `lesson-detail.html` can show `返回 YYYY-MM 课时管理` or `返回 YYYY-MM 对应视图` and return to the same month/view without introducing global state. `lesson.html` now also reads `?year=YYYY&month=MM&view=pair` on load, and list / paired view detail links use clearer `查看详情` text. Browser no-write validation intercepted the planned import RPC and returned a mocked success result, confirming success summary, month action, first-detail action, row detail link, pair-view detail/return navigation, edit dialog smoke, and 390px no-overflow on both lesson list and detail. No SQL/RPC/schema was added, no RPC rule changed, and read-only DB verification confirmed no persistent lesson row changes; protection counts stayed unchanged at student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`.
- Lesson edit checkpoint + UX polish: 2026-06-08 guarded lesson edit is stable end-to-end across verified RPC `school_update_lesson_record_guarded`, API wrapper `updateLessonRecordGuarded`, shared `js/components/lesson-edit-dialog.js`, `lesson.html` list/paired view, and `lesson-detail.html`. This checkpoint did not change RPC rules, SQL, schema, or API write boundaries. The shared dialog warning now gives clearer no-hover reasons for readonly/disabled fields: linked actual master fields are locked because they must stay consistent with the source planned row; actual status is readonly because status changes require a separate guarded flow; `lesson_type`, `planned_lesson_id`, and import metadata are readonly audit/source fields; planned rows describe the allowed V1 edit fields and still rely on RPC for linked/locked guards. Static checks, page-boundary scan, no-write browser smoke, locked-guard rejection smoke, and 390px overflow checks passed. The only business RPC observed in browser validation was one expected rejected `school_update_lesson_record_guarded` call for locked historical actual `41e82fe1-fa31-4eaa-a87f-9125d01aecfc`; read-only DB verification confirmed no persistent lesson row change from this checkpoint and protection counts stayed unchanged at student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`.
- Lesson detail edit frontend checkpoint: 2026-06-08 `lesson-detail.html` now exposes guarded lesson edit through the same shared dialog controller used by `lesson.html`; page modules still do not call `.rpc()` directly and the only write path remains API-layer wrapper `updateLessonRecordGuarded` in `js/api/lesson-api.js`. The shared controller owns edit-button rendering/block reasons, readonly `lesson_type` / `planned_lesson_id` / import metadata, planned-only status switching (`planned` / `pending_makeup`), actual status readonly mode, linked-actual master-field disabling, field validation, RPC error mapping, submit duplicate protection, fee preview behavior, and dialog-only errors. Detail saves use `updated_at` optimistic lock and reload the current detail data after success so `updated_at`, note, status, amount, source chain, settlement references, and wage references are refreshed.
- Lesson detail edit browser regression: local Chrome/Puppeteer validation on `http://localhost:8000/lesson-detail.html` covered real white-list writes through the detail page: planned edit on `bce0af13-7452-46c1-90e0-641b1dc78d3b` and actual edit on `dfb01d63-f404-420d-9c2f-1d1ab370291e`, both limited to `note` / `updated_at`. The dialog confirmed `lesson_type`, `planned_lesson_id`, and import source are readonly, and actual status is disabled/read-only. Locked historical actual `41e82fe1-fa31-4eaa-a87f-9125d01aecfc` returned the dialog RPC error `原学生月度结算已锁定，不能编辑课时。` without persistent change. A 390px reload of detail kept document/body `scrollWidth = 390`. A no-write smoke regression on `lesson.html` confirmed the refactored shared edit dialog still opens from the list and canceling triggers `0` write RPC calls. Browser console had only the expected local favicon 404 and expected failed RPC 400 for the locked guard case. Protected counts stayed unchanged at student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`.
- Lesson edit frontend checkpoint: 2026-06-08 `lesson.html` exposes the verified `school_update_lesson_record_guarded` RPC through API-layer wrapper `updateLessonRecordGuarded` in `js/api/lesson-api.js`; the page module still does not call `.rpc()` directly. The ordinary list and planned/actual paired view show `编辑` for records that are clearly editable under V1 and show disabled `不可编辑` with a reason for obvious blocked cases such as linked planned rows or legacy planned statuses. The edit dialog keeps `lesson_type`, `planned_lesson_id`, import metadata, and actual status read-only; planned status can switch only between `planned` and `pending_makeup`; linked actual rows disable student/teacher/subject/business-entity fields with an inline warning; validation highlights field-level errors and RPC guard errors inside the dialog without `alert/prompt/confirm`. Save uses `updated_at` as `p_expected_updated_at` and refreshes the active month/list/paired view after success.
- Lesson edit frontend browser regression: local Chrome/Puppeteer validation on `http://localhost:8000/lesson.html` covered real white-list writes through the page: planned edit on `bce0af13-7452-46c1-90e0-641b1dc78d3b`, planned status switch on `b0c29b0f-2865-4439-bfb4-4bf81a0a2b7d`, and actual edit on `dfb01d63-f404-420d-9c2f-1d1ab370291e`. It also confirmed actual status is disabled/read-only, linked actual `e5fb18d1-ccf3-4cda-83c3-a9aedd123152` disables student/teacher/subject/business-entity fields with a warning, linked planned rows display disabled edit reasons, and locked historical actual `41e82fe1-fa31-4eaa-a87f-9125d01aecfc` returns the dialog RPC error `原学生月度结算已锁定，不能编辑课时。` without a persistent write. A 390px reload kept document/body `scrollWidth = 390`. Browser console had only expected local favicon 404 and the expected failed RPC 400 for the locked guard case. Persistent DB writes were limited to the three whitelisted `school_lesson_records` above; protected counts stayed unchanged at student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`.
- Lesson edit guarded RPC checkpoint: 2026-06-08 verified RPC `public.school_update_lesson_record_guarded` is implemented in `school_update_lesson_record_guarded_rpc.sql`. V1 updates one `school_lesson_records` row only, uses `updated_at` optimistic locking, does not expose editable `lesson_type` or `planned_lesson_id`, and writes no settlement, wage, payment, income, expense, account, or account transaction rows. Planned edit is limited to current `status in ('planned','pending_makeup')`, permits status switching only within that pair, rejects planned rows with linked actuals, and checks old/new student settlement months for `locked`. Actual edit is limited to current `status in ('completed','cancelled','makeup_completed')`, rejects status changes, checks old/new student settlement months and old/new teacher wage months for `locked`, rejects any `school_teacher_wage_lock_details.lesson_record_id` usage, and prevents linked actual rows from changing student/teacher/subject/business entity. `year_month` is derived from edited `lesson_date`; actual rows derive `actual_minutes` and `teacher_settlement_month`; planned rows keep `actual_minutes`, `teacher_settlement_month`, and `planned_lesson_id` null. If `lesson_fee` is omitted, billable rows derive it as `round(duration_hours * unit_price)`; non-billable actual rows use `0`, and cancelled actual rows are fixed to `is_billable = false`, `lesson_fee = 0`, and `actual_minutes = 0`.
- Lesson edit guarded RPC tests: SQL creation succeeded on Supabase. Rollback tests covered editable planned update, planned `planned`/`pending_makeup` status switch, and editable actual update with no residue. Guard tests covered linked planned rejection, rollback-only legacy planned status rejection, actual status-change rejection, linked actual master-data rejection, rollback-only wage-detail usage rejection, rollback-only locked student settlement rejection, rollback-only locked teacher wage rejection, and stale `updated_at` rejection. Persistent commit test was limited to whitelisted codex-test planned lesson `bce0af13-7452-46c1-90e0-641b1dc78d3b`, updating only its note/updated_at through the new RPC. Protection-chain counts after tests stayed unchanged: student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`. No schema/index was added and no frontend entry was implemented in this checkpoint.
- Lesson planned import duplicate-submit UX checkpoint: 2026-06-08 `lesson.html` planned-only import now has front-end duplicate-submit protection without changing RPC rules. Current paths are: submit-in-progress still returns early and disables the button; successful submit now locks the same preview by disabling the submit button with label `已导入`; the page remembers successfully imported file hashes for the current browser session and blocks re-submitting the same file with a dialog message; RPC result rows containing duplicate batch signals such as `import_batch_id 已存在` now show a user-facing dialog message that the duplicate batch was rejected and no new planned lessons were written. Browser validation used local Playwright/Chrome with the `school_import_lesson_records_batch` network request intercepted, so no write RPC reached Supabase. Validation covered actual-row submit blocked before RPC (`0` RPC), rapid double click producing one intercepted RPC while the button showed `导入中...`, success state `成功导入 1 行` / `导入月份 2027-03` / submit button `已导入` disabled / `查看 2027-03`, same-file reselect blocked with `0` RPC, mocked duplicate batch result showing the clearer dialog error plus row-level `import_batch_id 已存在`, 390px document/body `scrollWidth = 390`, and no console/page errors. Read-only DB verification confirmed no `v2.60.3 duplicate ux` lesson records were created; protection-chain counts remained student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`. This checkpoint changed `js/pages/lesson-page.js`, `lesson.html`, and `docs/current-status.md`; it did not add SQL/RPC or write DB.
- Lesson planned import success regression checkpoint: 2026-06-08 browser write regression covered the planned-only import success UI branch on `lesson.html`. The test used whitelisted codex master data (`codex-test v2.45.0 browser 学生 20260606`, `codex-test v2.45.0 browser 老师 20260606`, `codex-test v2.44.0 browser 科目 20260606`, `codex-test v2.47.0 browser2 测试业务归属`) and 2027-02 unlocked planned lessons only. It first confirmed actual rows are still blocked before RPC with `当前批量导入第一版仅支持预定课时` and `0` RPC / `0` non-GET requests. A UI bug was found and fixed in `js/pages/lesson-page.js`: after submit completed, the single-month view button became visible but still said `查看导入月份`; `setLessonImportSubmitting` now refreshes the text to `查看 YYYY-MM`. Post-fix browser validation confirmed summary `成功导入 1 行`, `导入月份 2027-02`, toast `已导入预定课时 1 行（2027-02）。`, visible `查看 2027-02`, click-through to filters `2027 / 02`, imported row visible, and 390px document/body `scrollWidth = 390`. Persistent DB writes were limited to four whitelisted planned `school_lesson_records`: `63029e0a-fb14-4041-90a6-3490ca4843cd` (`planned`, 2027-02-03), `b0c29b0f-2865-4439-bfb4-4bf81a0a2b7d` (`pending_makeup`, 2027-02-10), `aeb9beac-5dc3-478a-b4bf-8660fa86c43d` (`planned`, 2027-02-17), and `bce0af13-7452-46c1-90e0-641b1dc78d3b` (`planned`, 2027-02-18). Protection-chain counts stayed unchanged before/after at student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`. This checkpoint changed `js/pages/lesson-page.js`, `lesson.html`, and `docs/current-status.md`; it did not add SQL/RPC or change RPC rules.
- Lesson planned import UX polish checkpoint: 2026-06-08 `lesson.html` planned-only batch import UI is now consistently framed as `导入预定课时`. The entry button, dialog title, template export button, empty state, and guide copy no longer imply that full actual batch import can be submitted. The exported `.xlsx` main `lesson_template` sheet now contains only two planned examples: `预定 / 待上课` and `预定 / 待补课`; actual completed/cancelled/makeup_completed examples were moved to the `说明` sheet as `future/history migration backlog` notes and remain unavailable for current submit. Preview still recognizes actual rows and keeps the existing pre-submit block `当前批量导入第一版仅支持预定课时`. Submit success handling now records successful row count and imported month range in the preview summary, and exposes a single-month `查看 YYYY-MM` action after a successful import. Browser no-write validation on `http://localhost:8000` confirmed `lesson_planned_import_template_v2.xlsx`, sheets `lesson_template` and `说明`, 2 main data rows with types `[预定, 预定]` and statuses `[待上课, 待补课]`, actual backlog notes in the guide sheet, actual-row submit block with `0` RPC calls and `0` non-GET requests, hidden view-month action before success, and 390px document/body `scrollWidth = 390`. This checkpoint changed `lesson.html`, `js/pages/lesson-page.js`, `docs/current-status.md`, and `docs/system-map.md`; it did not execute SQL/RPC or write DB.
- Lesson planned-only batch import checkpoint: 2026-06-08 verified RPC `public.school_import_lesson_records_batch(p_import_batch_id uuid, p_source_file_name text, p_source_file_hash text, p_rows jsonb, p_note text default null)` is now implemented as planned-only V1 in `school_import_lesson_records_batch_rpc.sql`. It keeps the general RPC name for possible future history-migration expansion, but the current executed function only accepts `lesson_type = planned` with status `planned` or `pending_makeup`. It writes only `school_lesson_records`, casts `p_import_batch_id::text` into `school_lesson_records.import_batch_id`, derives `year_month`, `app_type = school`, `import_source`, and `imported_at`, and writes `actual_minutes = null`, `teacher_settlement_month = null`, and `planned_lesson_id = null`. Any actual row returns `当前批量导入第一版仅支持预定课时。`; no completed/cancelled/makeup_completed batch write, planned ID linking, or auto-matching is implemented. Validation is whole-batch: any row error returns row errors and writes no lesson rows.
- Lesson planned-only batch import protections and tests: the RPC validates active school student/teacher/subject/business entity records using the same口径 as planned lesson manual create, rejects student default business-entity mismatch, positive-duration/amount/time errors, existing `import_batch_id`, and locked student settlement month based on imported `lesson_date` year-month. Teacher wage lock protection is intentionally not used in planned-only V1 because planned creation does not set actual teacher settlement month and the verified single planned RPC also only guards student settlement locks. SQL creation succeeded on Supabase after transaction-wrapped syntax loading. Rollback covered `planned` and `pending_makeup`; guards covered actual-row rejection, locked student settlement month, and import batch re-entry. Whitelisted SQL commit wrote only planned lessons `62288630-2005-4ace-be9d-34343d715bc2` (`planned`) and `b04ca01d-ed3d-4a06-8e63-17a29d3e2a0d` (`pending_makeup`) for 2027-01.
- Lesson planned-only import frontend checkpoint: `lesson.html` import dialog now exposes `导入预定课时` through API-layer wrapper `importPlannedLessonRecordsBatch` in `js/api/lesson-api.js`; page modules still do not call `.rpc()` directly. The existing preview/template/中文归一化 remains in place and actual rows can still be parsed and displayed, but clicking submit with actual rows marks those rows and blocks before RPC with `当前批量导入第一版仅支持预定课时`. Browser validation on `http://localhost:8000` covered actual-row blocked submit with `0` RPC calls, planned-only submit with exactly one `school_import_lesson_records_batch` RPC and no other RPCs, success message `已导入预定课时 2 行`, created planned lessons `5e61e63f-a7ba-48e2-9939-cbb3501f8b69` (`planned`) and `f711e2ff-05aa-4181-b7f6-1fd3a3bffe6d` (`pending_makeup`), preview result display, and 390px document/body `scrollWidth = 390`. The only browser console error was the local static server favicon 404. Protected counts stayed unchanged after SQL and browser commits: student settlements `10`, teacher wage locks `70`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, accounts `11`, and account transactions `235`.
- Lesson import planned ID precheck checkpoint: 2026-06-08 `lesson.html` import preview now performs read-only `关联预定ID` / `planned ID` prechecks for actual rows before any batch import write design. The confirmed current lesson status口径 is: `lesson_type = planned` uses internal status `planned` for 待上课 and `pending_makeup` for 待补课; `lesson_type = actual` uses `completed`, `cancelled`, and `makeup_completed`. Actual preview rows with a planned ID now first require UUID format, then check whether the ID exists, whether it points to a `lesson_type = planned` row, whether the source planned status is `planned` or `pending_makeup`, whether student/teacher/subject/business entity match the import row, whether an existing linked actual already uses that planned ID, whether the same import file reuses the planned ID across multiple actual rows, whether the source planned's student settlement month is locked, and whether the imported actual teacher settlement month is locked. Actual rows without planned ID now get a warning that preview will not auto-match planned and that batch import must separately decide whether matching by student/teacher/subject/date/time is allowed. Planned rows do not require planned ID. The precheck uses API-layer read-only Supabase selects only; it adds no write RPC, no import submit action, and no DB writes. Browser read-only validation on `http://localhost:8000` covered missing planned ID warning, valid planned ID with 1 row / 0 errors / 0 warnings, placeholder non-UUID planned ID as a row-level format error without dialog-level failure, existing linked actual duplicate error, non-planned ID error, missing UUID error, student mismatch error, same-file duplicate planned ID error, locked student settlement month error, locked teacher wage month error, 390px document/body `scrollWidth = 390`, no `/rpc`, no non-GET requests, and no console/page errors. This checkpoint changed `js/api/lesson-api.js`, `js/pages/lesson-page.js`, `lesson.html`, `docs/current-status.md`, and `docs/system-map.md`; it did not execute SQL/RPC or write DB.
- Lesson import template human-readable values checkpoint: 2026-06-08 `lesson.html` standard template export now uses user-facing Chinese headers and sample values for the type/status fields: `课时类型`, `状态`, and `关联预定ID`, with examples covering `预定 / 待上课`, `预定 / 待补课`, `实际 / 已上课`, `实际 / 取消`, and `实际 / 已补课`; `是否计费` examples use `是 / 否`. Preview parsing continues to normalize these display values to the existing canonical values (`planned`, `actual`, `completed`, `cancelled`, `makeup_completed`, and existing planned-status canonical `planned`) and keeps English legacy values compatible (`planned`, `actual`, `pending`, `pending_makeup`, `completed`, `cancelled`, `makeup_completed`). The template guide and import dialog now show Chinese/English value mapping and the legal combinations `预定：待上课 / 待补课` and `实际：已上课 / 取消 / 已补课`. SheetJS data-validation dropdowns were not added in this checkpoint because the browser-side SheetJS community export path does not provide a low-risk, consistently supported dropdown workflow across Excel/Numbers/Google Sheets; the guide sheet records the legal values instead. Browser read-only validation on `http://localhost:8000` confirmed the downloaded `lesson_import_template_v2.xlsx`, sheets `lesson_template` and `说明`, 15-column Chinese header, 5 Chinese sample rows, guide mapping content, re-upload of the exported Chinese template rendering 5 rows with no type/status/duration missing errors, a separate Chinese-value CSV preview rendering 5 rows with 0 errors, a legacy English-value CSV preview rendering 4 rows with 0 errors, 390px document/body `scrollWidth = 390`, no `/rpc`, no non-GET requests, and no console/page errors. This preview/template-only checkpoint changed `js/pages/lesson-page.js`, `lesson.html`, `docs/current-status.md`, and `docs/system-map.md`; it did not execute SQL/RPC or write DB.
- Lesson import standard template export checkpoint: 2026-06-08 `lesson.html` import preview now includes `导出标准模板`. The browser uses SheetJS when available to export `lesson_import_template_v2.xlsx` with two sheets: `lesson_template` and `说明`; if SheetJS is unavailable it falls back to `lesson_import_template_v2.csv`. The first row uses the v2 preview standard fields `学生`, `老师`, `科目`, `业务归属`, `日期`, `开始时间`, `结束时间`, `lesson_type`, `status`, `课时`, `金额`, `是否计费`, `内容`, `备注`, and `planned ID`. Example rows cover planned (`pending`, normalized in preview as 待上课), actual completed, actual cancelled, and actual makeup_completed. The guide sheet records legal values for `lesson_type`, planned/actual statuses, billable values, and that example master-data names must be replaced with current system lookup values. Browser read-only validation on `http://localhost:8000` confirmed the downloaded `.xlsx` suggested filename, both sheets, 4 sample rows, statuses `pending/completed/cancelled/makeup_completed`, 15-column header, guide sheet content, re-upload preview rendering 4 rows, desktop dialog `1248 x 860`, 390px document/body `scrollWidth = 390`, no `/rpc`, no non-GET requests, and no console/page errors. This checkpoint changed `css/app.css`, `js/pages/lesson-page.js`, `lesson.html`, `docs/current-status.md`, and `docs/system-map.md`; it did not execute SQL/RPC or write DB.
- Lesson import preview usability checkpoint: 2026-06-08 `lesson.html` import preview now has broader v1-template-compatible header aliases, including business entity, student, teacher, subject, generic and planned/actual date/time/duration/fee/status, billable, content, note, and planned association columns. The parser no longer treats a generic `日期` column as a planned/actual paired template; only explicit planned/actual columns imply paired preview rows. If a generic template lacks `lesson_type` or `status`, preview now reports clear missing-column messages instead of silently guessing. The dialog now includes a template field guide, displays billable/content/planned ID preview columns, and uses a wider/taller tool layout on desktop with internal table scrolling. Browser read-only validation on `http://localhost:8000` covered a v1-style old template with `時間数`, `単価`, `应收课时费`, `是否计费`, `上课内容及作业`, and `关联标识`, expanding 1 source row into 2 preview rows with 0 errors; a generic template missing `lesson_type/status` produced explicit补列 messages; desktop dialog measured `1248 x 860`; 390px layout kept document/body `scrollWidth = 390`; no `/rpc` requests, no non-GET requests, and no console/page errors. This checkpoint changed `css/app.css`, `js/pages/lesson-page.js`, `lesson.html`, `docs/current-status.md`, and `docs/system-map.md`; it did not execute SQL/RPC or write DB.
- Lesson import preview checkpoint: 2026-06-08 `lesson.html` now has a front-end-only `导入预览` dialog for local `.xlsx/.xls/.csv/.tsv/.txt` files. The preview parses generic lesson rows and v1-style planned/actual paired columns, expands paired rows into planned/actual preview rows, maps against already-loaded read-only student/teacher/subject/business-entity lookups, and displays original row number, student, teacher, subject, business entity, date, time, `lesson_type`, `status`, duration, amount, note, and row/field-level errors or warnings. The dialog exposes only file select, clear, and close actions; it has no submit/import button, does not call write RPCs, does not generate `school_lesson_records`, and does not change planned/actual write flows. Browser read-only validation on `http://localhost:8000` covered CSV preview with 2 rows and 1 error row, Excel paired preview expanding 1 source row into 2 preview rows, loaded SheetJS, 390px layout with document/body `scrollWidth = 390` and table overflow contained inside the dialog, no `/rpc` requests, no non-GET requests, and no console/page errors. This checkpoint changed `css/app.css`, `js/pages/lesson-page.js`, `lesson.html`, `docs/current-status.md`, and `docs/system-map.md`; it did not execute SQL/RPC or write DB.
- Lesson paired-row card height alignment checkpoint: 2026-06-08 `lesson.html` planned/actual paired view now treats each paired row as one visual unit by stretching the paired columns, card/placeholder area, and actual stack through CSS grid. Desktop collapsed rows keep the planned card and actual card/actual stack equal height, including rows where one side has less text and needs blank fill. Expand/collapse remains the existing click/tap behavior and still keeps the row aligned. Browser read-only validation on `http://localhost:8000` covered 60 paired rows, 15 single-actual rows, 45 missing-actual rows, desktop max planned-vs-actual height diff `0`, expanded-row height diff `0`, collapsed-row height diff `0`, unchanged generation entry counts (`actual/cancelled/makeup = 45/45/45`), 390px stacked layout with document/body `scrollWidth = 390`, `lesson-detail.html` 390px with document/body `scrollWidth = 390`, no `/rpc` requests, no non-GET requests, and no console/page errors. This UI-only checkpoint changed `css/app.css`, `lesson.html`, and `lesson-detail.html`; it did not change lesson business rules, dialogs, API/RPC wiring, SQL, or DB data.
- Lesson paired-view note UI polish checkpoint: 2026-06-08 `lesson.html` planned/actual paired view now renders content and note as labeled compact text rows. Long content/note values are line-clamped by default with a click/tap `展开` / `收起` toggle, so mobile users do not depend on hover and long notes no longer force large card height expansion by default. Existing planned creation and completed/cancelled/makeup_completed actual generation entry buttons and dialogs were left unchanged. Local browser validation on `http://localhost:8000` covered paired view visibility, 75 cards, 4 collapsible text blocks, expand/collapse state changes, unchanged generation entry counts, `lesson.html` at 390px with document/body `scrollWidth = 390`, `lesson-detail.html` at 390px with document/body `scrollWidth = 390`, no `/rpc` requests, no non-GET requests, and no console/page errors. This UI-only checkpoint changed `css/app.css`, `js/pages/lesson-page.js`, `lesson.html`, and `lesson-detail.html`; it did not execute SQL/RPC or write DB.
- Lesson write-chain checkpoint: 2026-06-08 read-only checkpoint confirmed the completed lesson writing surface remains the same scope: planned lesson manual creation V1, planned -> completed actual, planned -> cancelled actual, planned/pending_makeup -> makeup_completed actual, and the planned/actual paired readonly view. No new feature, SQL/RPC execution, DB write, or browser write test was performed in this checkpoint. The four lesson RPC SQL archives are committed and marked executed / rollback-tested / guard-tested / commit-tested: `school_create_planned_lesson_record_rpc.sql`, `school_create_actual_lesson_from_planned_rpc.sql`, `school_create_cancelled_actual_lesson_from_planned_rpc.sql`, and `school_create_makeup_completed_actual_lesson_from_planned_rpc.sql`.
- Lesson protection-chain checkpoint: the lesson V1 write chain records that source planned rows are preserved, duplicate actual generation is guarded, locked student settlement months and locked teacher wage months are rejected where relevant, and no student settlement snapshots, teacher wage locks/details, payment requests, income records, expense records, accounts, or account transactions are generated or modified by these lesson writes. Page-layer scan for `lesson.html`, `lesson-detail.html`, `js/pages/lesson-page.js`, and `js/pages/lesson-detail-page.js` showed no direct `.rpc()` or direct `insert`/`update`/`delete`/`upsert`; lesson RPC calls remain in `js/api/lesson-api.js`.
- System map checkpoint: `docs/system-map.md` added on 2026-06-06 to record completed v2 modules, page/API boundaries, main data source tables, allowed write RPCs, protected historical links, cross-module relationships, and remaining boundary risks. This documentation-only task did not execute SQL/RPC or write DB.
- Browser verification archive checkpoint: 2026-06-07 archived the completed real browser verification results for `profit-summary.html` filters/drilldown and account transfer/reversal. This archive-only checkpoint changed `docs/current-status.md` only; it did not execute SQL/RPC, run new browser tests, or write DB.
- Master-data create dialog UI polish checkpoint: on 2026-06-07, `account.html`, `student.html`, and `wage-rule.html` create dialogs were widened to match the income/expense create dialog width and use a desktop two-column layout while preserving mobile single-column behavior. Browser validation covered account create, student create, and wage-rule create dialogs opening, empty-submit validation, field highlighting, desktop `820px` panel width with two columns, 390px single-column layout with no horizontal overflow, and no `/rpc` requests. This UI-only checkpoint did not change APIs/RPCs, saving logic, field meaning, or DB data.
- Master-data create/edit phase checkpoint: student, teacher, subject, business entity, account, and wage rule create/update flows are complete through verified RPCs, rollback tests, whitelisted commit tests, frontend/API-layer integration, real browser checks, DB comparison, and documentation checkpoints. Full autopilot workflow remains active and ordinary phase-transition confirmation is no longer required.
- Latest stable code checkpoint before lesson import planned ID precheck: `ad369ce fix: polish lesson import template values`.
- Completed write flows: income creation, income reversal, expense creation, expense reversal, ordinary expense attachment metadata creation, reimbursement confirmation, reimbursement reversal, account creation, account adjustment, account adjustment reversal, account transfer, account transfer reversal, account profile update, student profile update, student creation, teacher profile update, teacher creation, subject profile update, subject creation, business entity profile update, business entity creation, wage rule creation, wage rule soft-disable/restore, planned lesson creation V1, completed actual generation from planned V1, cancelled actual generation from planned V1, makeup_completed actual generation from planned V1, teacher wage payment confirmation, and teacher wage payment reversal.
- Write flows use API-layer wrappers and verified RPCs; page modules must not call Supabase `.rpc()` directly or directly insert, update, delete, or upsert rows.
- Ordinary expense attachment metadata RPC checkpoint completed on 2026-06-07. Verified RPC `school_create_expense_attachment_metadata` inserts metadata-only rows into `school_expense_attachments` for ordinary school expenses using existing schema fields `file_name`, `file_type`, `file_size`, `source_type`, and `note`; it generates a `metadata-only/...` storage_path placeholder only because the existing table requires `storage_path`, and it does not integrate Supabase Storage, upload, download, preview, replace, delete, OCR, public URL, or extracted text handling. This checkpoint did not modify frontend/API/page files and did not change `school_create_expense_record`.
- Ordinary expense attachment metadata RPC tests used whitelisted ordinary test expense `536b9c01-b728-4e7a-a5bb-96173a83656d` (`other`, JPY 500, reversed, note `v2.25.17 expense reversal frontend test`). Rollback test inserted attachment `1dfa484f-8813-4b85-a481-4a5e013c2f77` inside a transaction and left no residue. Commit test inserted attachment `ab22a5eb-96f8-4505-a873-1893bafecdbd` with `file_name = codex-test-v2.51.0-commit-receipt.pdf`, `file_type = application/pdf`, `file_size = 12345`, `source_type = receipt`, and note `codex-test / v2-test / sandbox / v2.51.0 commit expense attachment metadata`. DB comparison confirmed expense status/amount/account/reimbursement status unchanged, expense count stayed `44`, account transaction count stayed `235`, and account `1b41c695-9176-465c-bcb1-27e3c803037e` current balance stayed JPY `700.00`.
- Ordinary expense attachment metadata frontend V1 completed on 2026-06-07. `expense-detail.html` now exposes a metadata-only add dialog for ordinary non-`teacher_wage` expenses through the API-layer RPC wrapper `school_create_expense_attachment_metadata`; the dialog validates required file name, file size, and source type, highlights invalid fields, locks buttons during save, and does not include file inputs, Supabase Storage, upload, download, preview, replace, delete, OCR, public URL, or extracted text handling. `expense-detail.html` continues to display attachment metadata, and `expense.html` now displays attachment counts in the attachment column.
- Ordinary expense attachment metadata frontend browser validation used test expense `536b9c01-b728-4e7a-a5bb-96173a83656d`. Browser-created attachments: `80c676aa-1c23-439f-82dc-98db0fde877b` (`codex-test-v2.51.1-browser-receipt-1780809981013.pdf`, `application/pdf`, `23456`, `receipt`) and `d8d91ce9-8941-4b28-b23e-90fe9d340794` (`codex-test-v2.51.1-browser-double-submit-1780810085422.pdf`, `application/pdf`, `34567`, `receipt`). Browser checks covered `expense-detail.html` load, add dialog open, empty-file-name validation and field highlight, double-click save duplicate guard with one expected RPC call, success refresh and detail metadata display, `expense.html` attachment count display (`3 个` for the test expense), 390px narrow detail/dialog layout with no page-level horizontal overflow, and absence of file inputs or Storage requests. Console/pageerror had no critical app errors; only the local static server `favicon.ico` 404 was observed. DB comparison confirmed expense status/amount/account/reimbursement status unchanged, expense count stayed `44`, account transaction count stayed `235`, and account `1b41c695-9176-465c-bcb1-27e3c803037e` current balance stayed JPY `700.00`.
- Account transaction linkage and account transaction detail source summaries cover income, expense, reimbursement, payment request, account adjustment, account adjustment reversal, account transfer, and account transfer reversal sources.
- Account transaction detail readonly coverage verification completed on 2026-06-07. Browser validation covered `account.html` display of newly created codex accounts, empty transaction state for zero-transaction account `fd6d7e1e-b8b2-43c3-babb-b8d57e6fd8a2`, `account-transaction-detail.html` source summaries, source drilldown, browser-back return, explicit return-to-account link, 390px narrow layout, and fallback rendering for no related source / unsupported related table through browser-only network stubs. The browser run made no `/rpc` calls and no non-read requests; console/pageerror stayed clean.
- Account transaction detail readonly coverage IDs: income transaction `6adc8a15-e0f9-4b1c-8293-70d52aa62b27` -> `school_income_records`; expense transaction `066db4ae-806a-4bbd-b30d-aea284e19b87` -> `school_expense_records`; reimbursement transaction `cf71e1c6-c489-4fc1-a451-cc36e21ead78` -> `school_reimbursements`; teacher wage payment transaction `a470c61a-ddde-4950-8d8b-4918bc3a0c12` -> `school_payment_requests`; account adjustment transaction `23eb9b46-74df-45e5-ac16-37dc6594c8d6` -> `school_account_adjustments`; account transfer transactions `c0c8c3af-3f8c-4217-a630-8d8270bf6d72` and `7d210f1b-9c12-4b71-b9cf-e899823f1ed3` -> `school_account_transfers`; account-origin transaction `9e9fe594-b10e-4355-bca9-f375857b00c0` -> `school_accounts`. DB comparison stayed read-only with accounts `11`, account transactions `235`, null-source transactions `0`, and unsupported-source transactions `0`.
- Lesson planned/actual paired readonly view completed on 2026-06-07. `lesson.html` keeps the existing ordinary list and adds a readonly view toggle for `普通列表` / `左右对应`; the paired view uses the already-loaded `school_lesson_records` data in memory, pairs `actual.planned_lesson_id = planned.id`, shows planned rows without actual as missing actual, and shows actual rows whose planned record is absent from the filtered result as `未关联实际课时`. The flow does not add SQL/RPC, does not write DB, and does not modify lesson-detail business rules, student settlements, teacher wage locks, payment requests, expenses, accounts, or account transactions.
- Lesson paired-view browser validation completed with local Playwright/Chrome. Default `2026-06` loaded `69 条`, ordinary list was default, paired view rendered `59` paired rows and `69` readonly cards, detail links opened `lesson-detail.html` and loaded the planned/actual/makeup source chain, type filtering kept the selected view, switching back to ordinary list worked, and 390px narrow layout stacked planned/actual with no page-level horizontal overflow. Extra readonly validation on `2026-11` covered cancelled and makeup states: cancelled actual rendered as `不计费（取消课）`, makeup_completed rendered with billable / non-billable distinction, pending_makeup and missing actual states were visible, and filtered actual rows without their planned side were conservatively shown as `未关联实际课时`. Network audit showed no `/rpc` and no non-read requests; console/pageerror had no critical errors.
- Lesson planned manual creation V1 completed on 2026-06-07 with verified RPC `school_create_planned_lesson_record`, API-layer integration on `lesson.html`, and no page-layer direct `.rpc()` or direct insert/update/delete/upsert. V1 only creates `lesson_type = planned` rows with status `planned` or `pending_makeup`; it does not create actual lessons, cancelled lessons, makeup_completed lessons, planned-to-actual conversion, edits, deletes, copies, imports, batch operations, student settlement snapshots, teacher wage locks/details, payment requests, income/expense records, account transactions, or account balance changes.
- Lesson planned creation RPC guard and SQL tests: the RPC validates active/effective student, teacher, subject, and business entity; derives `year_month` from `lesson_date`; forces planned-only insert; validates positive duration and non-negative unit/fee values; keeps `planned_lesson_id`, `actual_minutes`, `teacher_settlement_month`, and import fields empty; and rejects locked student settlement months using `school_student_monthly_settlements.student_id + year_month + business_entity_id + settlement_status = locked`. Rollback insert test left no residue; locked-month rollback guard rejected the insert as expected and left no residue; SQL commit test inserted whitelisted planned lesson `85ab1365-7929-4818-a369-4a7c935c2368` for `2026-12-11`, student `92981b7a-7fec-4982-9684-21b1f5a96eeb`, teacher `21d53107-abd8-442d-a63c-e099ec0f42ba`, subject `3c36adcf-ecf6-49e7-8645-7cb912d4ddfe`, business entity `648fc486-bb10-47fa-9734-6ca41dd04a47`, time `09:30-11:30`, duration `2`, unit JPY `1600`, fee JPY `3200`, status `planned`, lesson_count `4`, and note `codex-test / v2-test / sandbox / v2.54.0 commit planned lesson`.
- Lesson planned creation browser validation used local Playwright/Chrome and created whitelisted lesson `850f1874-09fc-45ad-9404-5ca6a0312ef7` for `2026-12-12`, student `92981b7a-7fec-4982-9684-21b1f5a96eeb`, teacher `21d53107-abd8-442d-a63c-e099ec0f42ba`, subject `3c36adcf-ecf6-49e7-8645-7cb912d4ddfe`, business entity `648fc486-bb10-47fa-9734-6ca41dd04a47`, time `13:00-15:00`, duration `2`, unit JPY `1700`, fee JPY `3400`, status `pending_makeup`, lesson_count `5`, content `codex-test v2.54.0 browser planned lesson`, and note `codex-test / v2-test / sandbox / v2.54.0 browser planned lesson 20260607`. Browser checks covered `lesson.html` load, cache bust `v2.54.0-lesson-planned-create-20260607`, empty required-field validation and field highlights, fee preview, submit-button duplicate protection with one expected RPC call, post-save list refresh, ordinary list display, paired view display with missing actual, detail link to `lesson-detail.html`, browser-back return to lesson page, and 390px narrow dialog/paired-view layout with no page-level horizontal overflow. Console/pageerror had no critical app errors; the only console error was local static server `/favicon.ico` 404.
- Lesson planned creation DB comparison confirmed browser-created lesson `850f1874-09fc-45ad-9404-5ca6a0312ef7` has `lesson_type = planned`, `year_month = 2026-12`, `status = pending_makeup`, `is_billable = true`, `planned_lesson_id = null`, `actual_minutes = null`, `teacher_settlement_month = null`, and import fields empty. The browser test note count was `1`; total lesson records became `334`, actual lessons stayed `140`, teacher wage lock details stayed `388`, payment requests stayed `66`, income records stayed `17`, expense records stayed `44`, account transactions stayed `235`, and no actual record was generated for either the SQL commit-test planned lesson or the browser-created planned lesson.
- Lesson planned status display copy was corrected on 2026-06-07: lesson status code `planned` now displays as `待上课` in the planned lesson create dialog, lesson list/status filter, planned/actual paired view, lesson detail, and settlement detail lesson references. The underlying value/code remains `planned`; no `cancelled` option was added to planned lesson creation, no RPC was changed, and no DB write was performed for this copy-only checkpoint.
- Lesson completed actual generation from planned V1 completed on 2026-06-07 with verified RPC `school_create_actual_lesson_from_planned` and API-layer integration on `lesson.html`. V1 only creates one linked `lesson_type = actual`, `status = completed` row from one existing `lesson_type = planned` row, requires `planned_lesson_id`, does not support free actual creation, cancelled actual, makeup_completed actual, edit, delete, copy, import, or batch generation, and does not modify the original planned row.
- Lesson actual-from-planned RPC guard and SQL tests: the RPC locks the source planned row, rejects non-planned sources, rejects source planned statuses outside `planned` / `pending_makeup`, rejects duplicate linked actuals, inherits the planned row's student settlement month (`year_month`), derives `teacher_settlement_month` from the actual lesson date, rejects locked student settlement months, and rejects locked teacher wage months. Rollback insert test left no residue; duplicate guard, locked-student-settlement guard, and locked-teacher-wage guard were all tested inside rollback transactions and left no residue. SQL commit test generated actual lesson `52a4a8be-6575-4de0-a1a2-0c261055cbf2` from planned lesson `85ab1365-7929-4818-a369-4a7c935c2368` with `lesson_date = 2026-12-11`, `year_month = 2026-12`, `teacher_settlement_month = 2026-12`, `start_time = 09:45`, `end_time = 11:45`, `duration_hours = 2`, `actual_minutes = 120`, unit JPY `1600`, fee JPY `3200`, lesson_count `4`, and note `codex-test / v2-test / sandbox / v2.55.0 commit actual from planned`.
- Lesson actual-from-planned browser validation used local Playwright/Chrome and generated actual lesson `35c07a40-9bac-4aee-a518-27f55b0d1582` from planned lesson `850f1874-09fc-45ad-9404-5ca6a0312ef7`. Browser checks covered `lesson.html` load with cache bust `v2.55.0-lesson-actual-from-planned-20260607`, 2026-12/test-student filtering, paired-view action visibility, hidden generate action for planned `85ab1365-7929-4818-a369-4a7c935c2368` after its SQL-test actual exists, visible generate action for planned `850f1874-09fc-45ad-9404-5ca6a0312ef7` before browser generation, dialog open, negative-duration validation and field highlight, submit-button duplicate protection with one expected RPC call, post-save paired-view refresh with linked actual displayed, generate action hidden after success, direct `lesson-detail.html` verification for the new actual and linked planned source, and 390px paired-view layout with no page-level horizontal overflow. Console/pageerror had no critical app errors; the only console error was local static server `/favicon.ico` 404.
- Lesson actual-from-planned DB comparison confirmed browser actual `35c07a40-9bac-4aee-a518-27f55b0d1582` has `lesson_type = actual`, `status = completed`, `lesson_date = 2026-12-12`, `year_month = 2026-12`, `teacher_settlement_month = 2026-12`, `planned_lesson_id = 850f1874-09fc-45ad-9404-5ca6a0312ef7`, `start_time = 13:10`, `end_time = 15:10`, `duration_hours = 2`, `actual_minutes = 120`, unit JPY `1700`, fee JPY `3400`, lesson_count `5`, content `codex-test v2.55.0 browser completed actual from planned`, and note `codex-test / v2-test / sandbox / v2.55.0 browser actual from planned`. Source planned `850f1874-09fc-45ad-9404-5ca6a0312ef7` remained `pending_makeup`, and SQL commit source planned `85ab1365-7929-4818-a369-4a7c935c2368` remained `planned`. After browser verification, lesson records were `337`, actual lessons `142`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, and account transactions `235`.
- Lesson cancelled actual generation from planned V1 completed on 2026-06-07 with verified RPC `school_create_cancelled_actual_lesson_from_planned` and API-layer integration on `lesson.html`. V1 only creates one linked `lesson_type = actual`, `status = cancelled` row from one existing `lesson_type = planned` row, requires `planned_lesson_id`, fixes cancelled actual rows to `is_billable = false`, `lesson_fee = 0`, and `actual_minutes = 0`, does not support free actual creation, completed actual through this flow, makeup_completed actual, edit, delete, copy, import, or batch generation, and does not modify the original planned row.
- Lesson cancelled-actual-from-planned RPC guard and SQL tests: the RPC locks the source planned row, rejects non-planned sources, rejects source planned statuses outside `planned` / `pending_makeup`, rejects duplicate linked actuals, inherits the planned row's student settlement month (`year_month`), derives `teacher_settlement_month` from the cancelled actual lesson date, rejects locked student settlement months, and rejects locked teacher wage months. Rollback insert test left no residue; duplicate guard, locked-student-settlement guard, and locked-teacher-wage guard were all tested inside rollback transactions and left no residue. SQL commit test created whitelisted planned lesson `557214bb-0826-42e1-b3cb-52953045f4b5` and cancelled actual lesson `8ac46deb-259b-4967-b33d-411d3b40fd8c` with `lesson_date = 2026-12-24`, `year_month = 2026-12`, `teacher_settlement_month = 2026-12`, `start_time = 10:35`, `end_time = 12:35`, `duration_hours = 2`, `actual_minutes = 0`, unit JPY `1800`, fee JPY `0`, lesson_count `6`, and note `codex-test / v2-test / sandbox / v2.56.0 commit cancelled actual from planned`.
- Lesson cancelled-actual-from-planned browser validation used local Playwright/Chrome and created whitelisted planned lesson `5c3005fe-d1f0-4fbf-a6a2-4883d77f07ab`, then generated cancelled actual lesson `33da07ec-8674-4292-9226-c69160f4dcb7`. Browser checks covered `lesson.html` load with cache bust `v2.56.0-lesson-cancelled-actual-from-planned-20260607`, 2026-12/test-student filtering, paired-view `标记取消` action visibility, cancelled dialog open, negative-duration validation and field highlight, submit-button duplicate protection with one expected cancelled RPC call, post-save paired-view refresh with linked cancelled actual displayed as `已取消` / `不计费（取消课）`, cancelled action hidden after success, direct `lesson-detail.html` verification for the new actual and linked planned source, browser-back return, and 390px paired-view layout with no page-level horizontal overflow. Network writes were limited to expected RPCs `school_create_planned_lesson_record` and `school_create_cancelled_actual_lesson_from_planned`; pageerror stayed clean, and the only console error was the local static server 404.
- Lesson cancelled-actual-from-planned DB comparison confirmed browser planned `5c3005fe-d1f0-4fbf-a6a2-4883d77f07ab` stayed `lesson_type = planned`, `status = planned`, `lesson_date = 2026-12-25`, `year_month = 2026-12`, unit JPY `1900`, fee JPY `3800`, `is_billable = true`, and no actual fields were written to the source row. Browser actual `33da07ec-8674-4292-9226-c69160f4dcb7` has `lesson_type = actual`, `status = cancelled`, `lesson_date = 2026-12-25`, `year_month = 2026-12`, `teacher_settlement_month = 2026-12`, `planned_lesson_id = 5c3005fe-d1f0-4fbf-a6a2-4883d77f07ab`, `start_time = 14:05`, `end_time = 16:05`, `duration_hours = 2`, `actual_minutes = 0`, unit JPY `1900`, fee JPY `0`, `is_billable = false`, lesson_count `7`, content `codex-test v2.56.0 browser cancelled actual from planned`, and note `codex-test / v2-test / sandbox / v2.56.0 browser cancelled actual from planned`. After browser verification, lesson records were `341`, actual lessons `144`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, and account transactions `235`.
- Lesson makeup_completed actual generation from planned V1 completed on 2026-06-07 with verified RPC `school_create_makeup_completed_actual_lesson_from_planned` and API-layer integration on `lesson.html`. V1 only creates one linked `lesson_type = actual`, `status = makeup_completed` row from one existing `lesson_type = planned` row whose status is `planned` or `pending_makeup`, requires `planned_lesson_id`, supports explicit `is_billable = true / false`, fixes non-billable makeup_completed rows to `lesson_fee = 0`, derives `actual_minutes` from duration because the makeup lesson was completed, and does not modify the original planned row.
- Lesson makeup-completed-from-planned RPC guard and SQL tests: the RPC locks the source planned row, rejects non-planned sources, rejects source planned statuses outside `planned` / `pending_makeup`, rejects duplicate linked actuals, inherits the planned row's student settlement month (`year_month`), derives `teacher_settlement_month` from the makeup actual lesson date, rejects locked student settlement months, and rejects locked teacher wage months. Rollback tests covered both billable and non-billable inserts and left no residue; duplicate guard, locked-student-settlement guard, and locked-teacher-wage guard were all tested inside rollback transactions and left no residue. SQL commit test created whitelisted planned lesson `b729ae81-3bed-4070-8c49-27efe8d6f797` and billable makeup_completed actual lesson `b057546d-ee07-4b63-ada8-c33544bd9c44` with `lesson_date = 2026-12-31`, `year_month = 2026-12`, `teacher_settlement_month = 2026-12`, `start_time = 10:15`, `end_time = 12:15`, `duration_hours = 2`, `actual_minutes = 120`, unit JPY `2200`, fee JPY `4400`, `is_billable = true`, lesson_count `10`, and note `codex-test / v2-test / sandbox / v2.57.0 commit billable makeup_completed actual from planned`.
- Lesson makeup-completed-from-planned browser validation used local Playwright/Chrome and created whitelisted planned lesson `d3dfc9a5-22d6-446f-823d-9c0da95676df`, then generated non-billable makeup_completed actual lesson `5fe36b6e-eb89-46c7-a3fa-c5a9bb4fd1b0`. Browser checks covered `lesson.html` load with cache bust `v2.57.0-lesson-makeup-completed-actual-from-planned-20260607`, 2026-12/test-student filtering, paired-view `补课完成` action visibility, makeup dialog open, negative-duration validation and field highlight, non-billable select forcing fee `0` and read-only fee field, submit-button duplicate protection with one expected makeup RPC call, post-save paired-view refresh with linked actual displayed as `补课完成` / `不计费（补课完成）`, makeup action hidden after success, direct `lesson-detail.html` verification for the new actual and linked planned source, browser-back return, and 390px paired-view layout with no page-level horizontal overflow. Network writes were limited to expected RPCs `school_create_planned_lesson_record` and `school_create_makeup_completed_actual_lesson_from_planned`; console/pageerror stayed clean.
- Lesson makeup-completed-from-planned DB comparison confirmed browser planned `d3dfc9a5-22d6-446f-823d-9c0da95676df` stayed `lesson_type = planned`, `status = pending_makeup`, `lesson_date = 2026-12-31`, `year_month = 2026-12`, unit JPY `2300`, fee JPY `4600`, `is_billable = true`, and no actual fields were written to the source row. Browser actual `5fe36b6e-eb89-46c7-a3fa-c5a9bb4fd1b0` has `lesson_type = actual`, `status = makeup_completed`, `lesson_date = 2026-12-31`, `year_month = 2026-12`, `teacher_settlement_month = 2026-12`, `planned_lesson_id = d3dfc9a5-22d6-446f-823d-9c0da95676df`, `start_time = 15:10`, `end_time = 17:10`, `duration_hours = 2`, `actual_minutes = 120`, unit JPY `2300`, fee JPY `0`, `is_billable = false`, lesson_count `11`, content `codex-test v2.57.0 browser nonbillable makeup_completed actual from planned`, and note `codex-test / v2-test / sandbox / v2.57.0 browser nonbillable makeup_completed actual from planned`. After browser verification, lesson records were `346`, actual lessons `147`, teacher wage lock details `388`, payment requests `66`, income records `17`, expense records `44`, and account transactions `235`.
- Teacher wage payment cancel/restore/reissue browser retest completed on 2026-06-07 using clearly marked `codex-test / v2-test / sandbox / v2.50.0 / payment-status-retest` payment requests. Browser validation covered `index.html` default `2026-06` load, cancel dialog, restore dialog, reissue dialog, reissue empty-reason validation with field highlight, submit-button duplicate protection, list refresh, hidden reissue entry after replacement creation, `payment-detail.html` drilldown for original and replacement requests, and 390px narrow layouts. Static checks passed for `js/api/payment-api.js`, `js/pages/payment-page.js`, `js/api/payment-detail-api.js`, and `js/pages/payment-detail-page.js`; page modules still have no direct `.rpc()` or direct insert/update/delete/upsert calls.
- Teacher wage payment status-action test data: cancel/restore target `e8d5ff57-8a16-4902-b67f-2d7e5f9b8a9a` ended as `pending` after browser-driven `pending -> cancelled -> pending`; reissue original `01b912c6-289c-4fa9-912a-cedb1406bf45` remains `reversed` with `replacement_payment_request_id = a8fe5921-b3a3-425b-b032-9da7c6fa67df`; replacement request `a8fe5921-b3a3-425b-b032-9da7c6fa67df` is `pending`, `reissued_from_payment_request_id = 01b912c6-289c-4fa9-912a-cedb1406bf45`, and `reissue_reason = codex-test v2.50.0 browser reissue retest`. The retest used only existing RPCs `school_cancel_payment_request`, `school_restore_cancelled_payment_request`, and `school_reissue_reversed_payment_request`; no SQL/RPC files were added or executed.
- Teacher wage payment status-action DB comparison: the retest inserted two setup payment requests and browser reissue inserted one replacement payment request, all with codex test markers. No paid expense, paid account transaction, reversal transaction, or account id was linked to these test requests. Expense count stayed `44`, account transaction count stayed `235`, and aggregate account current balance stayed `856036.00`; no manual correction was performed. Browser console/pageerror had no critical errors; the only console error observed was the local static server `favicon.ico` 404. Network writes were limited to the expected status-action RPCs.
- Account transfer completed schema, RPC SQL draft/static review, schema/RPC execution, rollback test, commit test, verified SQL archive, frontend minimum implementation, frontend static checkpoint/push, and feature checkpoint.
- Account transfer commit test used whitelisted test data only: dedicated `codex-test / v2.33.10 / 账户转账测试` accounts.
- Account transfer reversal completed with no schema change, verified RPC SQL, rollback test, whitelisted commit test, frontend minimum implementation, static checkpoint, online file checks, and feature checkpoint.
- Account transfer reversal commit test used whitelisted test data only: transfer `372fe08d-0c9e-4b96-8e76-31a4e09ca9ac` on dedicated `codex-test / v2.33.10 / 账户转账测试` accounts.
- Account transfer and account transfer reversal completed real local Playwright/Chrome browser interaction verification on 2026-06-07 using whitelisted JPY test accounts `ac4150b1-658a-44ce-b1f6-f27e005e267b` -> `426ef08b-b864-47d2-975a-aae91fabeb63`, business entity `886a8f7c-0fea-45ac-97d2-15c976ede996`, amount JPY 12. Browser validation covered transfer dialog field errors, preview balances, submit button duplicate protection, list refresh, account transaction drilldown, transfer reversal dialog field errors/confirmation, submit button duplicate protection, post-reversal hidden reversal entry, and narrow 390px layouts for transfer dialog, reversal dialog, and account transaction detail. Only expected RPCs appeared: `school_create_account_transfer` and `school_reverse_account_transfer`; no critical console/page errors were observed.
- Account transfer browser verification test chain is preserved as audit data: transfer `41be4047-5ccd-4db2-8a86-dd14160c5bc9`; original transactions `c0c8c3af-3f8c-4217-a630-8d8270bf6d72` (`transfer_out`, balance after JPY 988) and `2698eb58-4840-4c3f-8915-5bd8853439e4` (`transfer_in`, balance after JPY 12); reversal transactions `7d210f1b-9c12-4b71-b9cf-e899823f1ed3` (`transfer_reverse_in`, balance after JPY 1000) and `e9e7544c-fcb4-4c85-9316-c72a25031db7` (`transfer_reverse_out`, balance after JPY 0). Final account balances returned to the pre-test snapshot: from account JPY 1000, to account JPY 0. No manual balance correction or deletion was performed.
- Student profile update completed with no schema change, verified RPC SQL, rollback test, whitelisted commit test, frontend minimum implementation, static checkpoint, online file checks, and feature checkpoint.
- Student profile update is limited to `display_name`, `status`, `course_track`, `target_type`, and `note`; it does not edit balances, settlement fields, tuition rules, contact fields, parent fields, birthday, lessons, income, settlements, or payments.
- Student profile update commit test used whitelisted test student `b48cd784-62e2-4232-a07c-65414e842b89` with `codex-test / v2-test / sandbox / v2.36.0 / 测试学生` markers.
- Student course/target/default edit completed with no schema change, verified RPC SQL `school_update_student_profile`, rollback test, whitelisted commit test, frontend existing-dialog implementation, local Chrome browser test, SQL checkpoint `a19ea9f`, frontend checkpoint `db02d4a`, and cache-bust checkpoint `d3857c0`.
- Student course/target/default edit extends the existing student edit dialog with a `课程/目标信息` section and is limited to `course_track`, `target_type`, `business_entity_id` as default business entity, `default_currency`, and `note` in addition to the existing `display_name/status` fields; it does not edit monthly settlements, carryovers, income records, payment requests, lessons, wages, account transactions, balances, tuition rules, contact fields, parent fields, birthday, or historical financial data.
- Student course/target/default edit tests used whitelisted test student `b48cd784-62e2-4232-a07c-65414e842b89` with `codex-test / v2-test / sandbox / v2.43.0` markers; rollback left no residue, commit/browser tests changed only the test student's main-data fields, and historical counts stayed unchanged at settlements `0`, carryovers `0`, incomes `0`, payment requests `0`, lesson records `0`, wage details `0`, and account transactions `231`.
- Student creation completed with no schema change, verified RPC SQL `school_create_student_profile`, rollback test, whitelisted commit test, frontend/API-layer implementation on `student.html`, local Playwright browser test, SQL checkpoint `3a6f94c`, frontend checkpoint `20c2535`, cache-bust checkpoint `a94c319`, and fallback DB/code backup.
- Student creation is limited to future-use student master data in `school_students`: `student_code`, `name`, `kana_name`, `display_name`, `status`, `course_track`, `target_type`, `target_schools`, `business_entity_id`, `default_currency`, and `note`; it uses existing defaults for balances, exchange rate, contact/parent fields, birthday, timestamps, and id.
- Student creation does not delete students, edit monthly settlements, carryovers, income records, lesson records, wage data, account transactions, balances, tuition rules, contact fields, parent fields, birthday, or recalculate historical data.
- Student creation tests used whitelisted students `d2dcd249-5d01-4857-b12d-293e6a2fa092` and `92981b7a-7fec-4982-9684-21b1f5a96eeb` with `codex-test / v2-test / sandbox / v2.45.0` markers; rollback left no residue, duplicate student code was rejected, and historical counts stayed unchanged at settlements `10`, carryovers `8`, income records `17`, lesson records `326`, wage lock details `388`, and account transactions `231`.
- Teacher profile update completed with no schema change, verified RPC SQL, rollback test, whitelisted commit test, frontend minimum implementation, static checkpoint, online file checks, and feature checkpoint.
- Teacher profile update is limited to `display_name`, `status`, `default_business_entity_id`, and `note`; it does not edit wage rules, wage locks, payment data, lesson data, rates, currencies, payment method, bank fields, phone, email, wechat, department, subject, teacher code, or system name.
- Teacher profile update commit test used whitelisted test teacher `12f6d142-b90b-4da2-be88-310414000bd1` with `codex-test / v2-test / sandbox / v2.37.0 / 测试老师` markers.
- Teacher profile edit completed real local Playwright/Chrome browser verification on 2026-06-07 using whitelisted test teacher `12f6d142-b90b-4da2-be88-310414000bd1`. The existing implementation already met the requested v2 basic edit scope, so no business JS/CSS/HTML or SQL/RPC changes were needed. Browser validation covered `teacher.html` load, edit dialog open, immutable-field summary, empty display-name validation with dialog error and field highlight, save button duplicate protection, successful refresh of the teacher card, and 390px mobile dialog layout. The only write RPC observed was `school_update_teacher_profile`; no page-layer `.rpc()` or direct insert/update/delete/upsert was present in the page module.
- Teacher profile edit browser test changed only allowed test-teacher fields: before `display_name = codex-test v2.37.0 commit 老师`, `status = inactive`, `default_business_entity_id = 886a8f7c-0fea-45ac-97d2-15c976ede996`, `note = codex-test / v2-test / sandbox / v2.37.0 / commit teacher profile update`; after `display_name = codex-test v2.46.0 browser 老师基础编辑`, `status = paused`, `default_business_entity_id = 2cf7b72f-6e3c-4d09-80f7-7c58593cd466`, `note = codex-test / v2-test / sandbox / v2.46.0 / browser teacher profile edit`. DB verification confirmed protected historical counts unchanged for that teacher: wage rules `1`, wage locks `0`, wage lock details `0`, payment requests `0`, expenses `0`, lessons `0`, and account transactions total `235`.
- Teacher creation completed with no schema change, verified RPC SQL `school_create_teacher_profile`, rollback test, whitelisted commit test, frontend/API-layer implementation on `teacher.html`, local Playwright browser test, SQL checkpoint `3a6f94c`, frontend checkpoint `20c2535`, and fallback DB/code backup.
- Teacher creation is limited to future-use teacher master data in `school_teachers`: `teacher_code`, `name`, `kana_name`, `display_name`, `department`, `status`, `default_business_entity_id`, and `note`; it uses existing defaults for rates, currencies, payment method, subject, bank/contact fields, timestamps, and id.
- Teacher creation does not delete teachers, create or edit wage rules, wage locks, payment requests, expenses, account transactions, sensitive/contact/payment fields, or recalculate historical data.
- Teacher creation tests used whitelisted teachers `efeafbbe-6d89-4f46-9d48-2970d2ec5a2f` and `21d53107-abd8-442d-a63c-e099ec0f42ba` with `codex-test / v2-test / sandbox / v2.45.0` markers; rollback left no residue, duplicate teacher code was rejected, and historical counts stayed unchanged at wage rules `16`, wage locks `70`, wage lock details `388`, payment requests `63`, expenses `44`, and account transactions `231`.
- Subject profile update completed with no schema change, verified RPC SQL, rollback test, whitelisted commit test, frontend minimum implementation, static checkpoint, online file checks, and feature checkpoint.
- Subject profile update is limited to `name`, `is_active` via status, and `note`; `school_subjects` has no `display_name/status` columns, so the first version maps UI status to `is_active` and does not add schema.
- Subject profile update does not delete subjects or edit category, color, sort order, historical lesson, wage, settlement, or payment data.
- Subject profile update commit test used whitelisted test subject `e87e4acf-d161-4d44-b1f1-f0e7e2253023` with `codex-test / v2-test / sandbox / v2.38.0 / 测试科目` markers.
- Subject profile edit completed real local Playwright/Chrome browser verification on 2026-06-07 using whitelisted test subject `e87e4acf-d161-4d44-b1f1-f0e7e2253023`. The existing implementation already met the requested v2 basic edit scope, so no business JS/CSS/HTML or SQL/RPC changes were needed. Browser validation covered `subject.html` load, edit dialog open, immutable-field summary, empty subject-name validation with dialog error and field highlight, save button duplicate protection, successful refresh of the subject card, and 390px mobile dialog layout. The only write RPC observed was `school_update_subject_profile`; no page-layer `.rpc()` or direct insert/update/delete/upsert was present in the page module.
- Subject profile edit browser test changed only allowed test-subject fields: before `name = codex-test v2.38.0 commit 科目6.6`, `is_active = true`, `note = codex-test / v2-test / sandbox / v2.38.0 / commit subject profile update`; after `name = codex-test v2.46.0 browser 科目基础编辑`, `is_active = false`, `note = codex-test / v2-test / sandbox / v2.46.0 / browser subject profile edit`. DB verification confirmed protected fields and history unchanged: `category = codex-test`, `primary_category = codex-test`, `tertiary_category = v2.38.0`, `color = #64748b`, `sort_order = 9999`, lesson records `0`, wage rules `1`, wage lock details `0`, settlements total `10`, payment requests total `63`, income records total `17`, expense records total `44`, and account transactions total `235`.
- Subject create completed with no schema change, verified RPC SQL `school_create_subject_profile`, rollback test, whitelisted commit test, frontend/API-layer implementation on `subject.html`, local Playwright browser test, SQL checkpoint `9f80c12`, and frontend checkpoint `febc3e1`.
- Subject create is limited to future-use subject master data in `school_subjects`: `name`, `is_active` via status, `category`, `primary_category`, `tertiary_category`, `color`, `sort_order`, and `note`; `school_subjects` has no `display_name/status` columns, so display name maps to `name` and status maps to `is_active`.
- Subject create does not delete subjects, modify existing subject history, edit lessons, student settlements, teacher wages, payment requests, income, expenses, account transactions, or recalculate historical data.
- Subject create tests used whitelisted subjects `a4147872-7b2c-4ec2-a26e-24af44311fce` and `3c36adcf-ecf6-49e7-8645-7cb912d4ddfe` with `codex-test / v2-test / sandbox / v2.44.0` markers; rollback left no residue, duplicate name was rejected, and historical counts stayed unchanged at lesson records `326`, settlements `10`, wage locks `70`, wage lock details `388`, payment requests `63`, income records `17`, expense records `44`, and account transactions `231`.
- Business entity profile update completed with no schema change, verified RPC SQL, rollback test, whitelisted commit test, frontend minimum implementation, static checkpoint, online file checks, APP_VERSION/cache bust update, and feature checkpoint.
- Business entity profile update is limited to `name`, `entity_type`, `default_currency`, `is_active`, and `note`; it does not edit code, company-report inclusion, historical income, expense, account, settlement, wage, payment, account balances, or account transactions.
- Business entity profile update commit test used whitelisted test business entity `7fdc0547-19e7-4199-9c66-3e8b832dd485` with `codex-test / v2-test / sandbox / v2.39.0 / 测试业务归属` markers.
- Business entity creation completed with no schema change, verified RPC SQL `school_create_business_entity_profile`, rollback test, whitelisted commit test, frontend/API-layer implementation on `business-entity.html`, local Playwright browser test, SQL checkpoint `a865e12`, and frontend checkpoint `b95b395`.
- Business entity creation is limited to future-use business entity master data in `school_business_entities`: `code`, `name`, `entity_type`, `default_currency`, `is_active`, and `note`; it uses existing defaults for `id`, `is_company_report`, `created_at`, and `updated_at`.
- Business entity creation does not automatically create accounts, edit company-report inclusion after creation, delete or merge business entities, reassign historical ownership, or rewrite income, expense, settlement, wage, payment request, account balance, or account transaction data.
- Business entity creation tests used whitelisted business entities `7d1f8aec-a272-49e3-8138-521884864870`, `2fa5bd72-7ba5-48f3-91dd-b56f978c56e6`, and `648fc486-bb10-47fa-9734-6ca41dd04a47` with `codex-test / v2-test / sandbox / v2.47.0` markers. Rollback left no residue, duplicate code was rejected, browser validation covered empty-code and empty-name field errors, submit-button duplicate protection, successful list refresh, keyword filter/reset, and 390px narrow layout, and the only business RPC observed in the full browser audit was `school_create_business_entity_profile`.
- Business entity creation DB verification confirmed test fields: commit test `codex-test-v2-47-commit-be` / `personal` / `CNY` / inactive, browser tests `codex-test-v2-47-browser-be` and `codex-test-v2-47-browser2-be` / `company` / `JPY` / active. Test business entities created zero accounts, and protected counts stayed unchanged from the pre-create baseline for accounts `8`, account transactions `235`, income records `17`, expense records `44`, student settlements `10`, teacher wage locks `70`, and payment requests `63`.
- Account profile update completed with no schema change, verified RPC SQL, rollback test, whitelisted commit test, frontend minimum implementation, static checkpoint, online file checks, APP_VERSION/cache bust update, and feature checkpoint.
- Account profile update is limited to `name`, `account_type`, `is_company_account`, `is_active`, and `note`; it does not edit account code, business ownership, currency, opening balance, current balance, account transactions, historical income, expense, reimbursement, payment, transfer, adjustment, settlement, or wage data.
- Account profile update commit test used whitelisted test account `ac4150b1-658a-44ce-b1f6-f27e005e267b` with `codex-test / v2.33.10 / 账户转账测试` markers; balance correction remains under the verified account adjustment flow only.
- Account profile update has completed local browser interaction test on `account.html`: edited whitelisted test account fields `name`, `account_type`, `is_company_account`, `is_active`, and `note`; the account list refreshed, `opening_balance` and `current_balance` stayed unchanged, account transaction count stayed unchanged, and balance correction still uses the verified account adjustment entry.
- Account creation completed with no schema change, verified RPC SQL `school_create_account_profile`, rollback test, whitelisted commit test, frontend/API-layer implementation on `account.html`, local Playwright browser test, SQL checkpoint `b6993c7`, and frontend checkpoint `d044017`.
- Account creation is limited to future-use account master data in `school_accounts`: `account_code`, `name`, `account_type`, `currency`, `business_entity_id`, `is_company_account`, `is_active`, and `note`; `opening_balance` and `current_balance` are fixed at `0`, and no account transaction is generated.
- Account creation does not rewrite income, expense, reimbursement, transfer, adjustment, payment request, account balance, or account transaction history. Tests used whitelisted accounts `5f75818c-1bc5-4ace-8cbc-42bd10d52b0c`, `7a724682-442d-424d-9d66-87fa26b28df6`, and `fd6d7e1e-b8b2-43c3-babb-b8d57e6fd8a2` with `codex-test / v2-test / sandbox / v2.48.0` markers; rollback left no residue, duplicate account code was rejected, browser validation covered field errors, submit-button duplicate protection, list refresh, account filter/reset, and 390px narrow layout. Account transactions for these test accounts remained `0`, and protected counts stayed unchanged for account transactions `235`, income records `17`, expense records `44`, reimbursements `6`, account transfers `3`, account adjustments `2`, and payment requests `63`.
- Wage rule readonly detail page is available at `wage-rule-detail.html` from the first column of `wage-rule.html`; it reads `school_teacher_wage_rules` plus teacher, student, subject, and business entity names through the API layer, does not add SQL/RPC, does not write DB, and does not recalculate or modify historical wage locks, payment requests, expenses, accounts, or account transactions.
- Wage rule readonly detail page has completed local browser interaction test: clicked the first-column detail entry from `wage-rule.html`, loaded `wage-rule-detail.html`, verified basic/match/rate sections and teacher, student, subject, and business entity names, confirmed the future-lock-only/no-history-recalculation notice, and returned to the list.
- Wage rule readonly detail page completed local Chrome retest on 2026-06-06: list detail entry opened `wage-rule-detail.html`, detail fields and teacher/student/subject/business entity names rendered normally, the future-lock-only/no-history-recalculation notice was visible, return-to-list navigation worked, and network audit showed no `/rpc` or non-read requests.
- Wage rule config edit completed with schema constraints, verified RPC `school_update_teacher_wage_rule_config`, frontend/API-layer implementation, rollback test, whitelisted commit test, local Chrome frontend test, SQL checkpoint `a34a6ea`, and frontend checkpoint `00621a0`.
- Wage rule config edit is limited to `settlement_type`, `hourly_rate_jpy`, `hourly_rate_cny`, `exchange_rate`, `transport_fee_jpy`, `classroom_fee_jpy`, `is_active`, and `note`; it does not edit `teacher_id`, `student_id`, `subject_id`, `business_entity_id`, wage locks, wage lock details, payment requests, expenses, account balances, or account transactions, and it does not recalculate historical wages.
- Wage rule config edit tests used whitelisted test rule `c5a7836b-538e-45ea-9e4a-a1494c5698bb` with `codex-test / v2-test / sandbox / v2.42.0` markers; historical counts stayed unchanged at wage locks `70`, wage lock details `388`, payment requests `63`, expenses `44`, and account transactions `231`.
- Wage rule creation completed with no schema change, verified RPC SQL `school_create_teacher_wage_rule_config`, rollback test, whitelisted commit test, frontend/API-layer implementation on `wage-rule.html`, local Playwright browser test, SQL checkpoint `177d167`, and frontend checkpoint `de9e059`.
- Wage rule creation is limited to future-use `school_teacher_wage_rules` config rows with explicit `teacher_id`, `student_id`, `subject_id`, `business_entity_id`, `settlement_type`, rates, exchange rate, fees, `is_active`, and `note`. The first version does not create generic student-empty rules, does not edit existing rule matching keys, and does not recalculate historical wages.
- Wage rule creation tests used whitelisted rules `8ca3d67d-886a-41b0-b480-a60264219435` and `91fb2803-6071-4a84-9b7a-ea43b1ba6a5a` with `codex-test / v2-test / sandbox / v2.49.0` markers; rollback left no residue, duplicate active teacher/student/subject/business-entity matches were rejected, browser validation covered lookup errors, negative amount field highlight, submit-button duplicate protection, list refresh, keyword filter/reset, detail link visibility, and 390px narrow layout. Protected counts stayed unchanged for wage locks `70`, wage lock details `388`, payment requests `63`, expenses `44`, and account transactions `235`.
- Wage rule soft-disable/restore completed on 2026-06-07 with verified RPC `school_set_teacher_wage_rule_active_state`, API-layer integration, `wage-rule.html` action buttons, confirmation dialog, active-state filtering/status display, browser validation, and DB comparison. The flow is the delete alternative for wage rules: it never physically deletes rows and only updates `school_teacher_wage_rules.is_active`, `note`, and `updated_at`; restore rejects another active rule with the same teacher/student/subject/business-entity match. The generic edit dialog now keeps active state read-only so status changes use the dedicated action.
- Wage rule soft-disable/restore tests used whitelisted codex wage rules only. SQL rollback test soft-disabled `8ca3d67d-886a-41b0-b480-a60264219435` and left no residue. SQL commit test soft-disabled and restored the same rule; final fields were `is_active = true`, `note = codex-test / v2-test / sandbox / v2.52.0 commit restore wage rule`, `settlement_type = jpy_hourly`, `hourly_rate_jpy = 1500`, `hourly_rate_cny = 0`, `exchange_rate = 0`, `transport_fee_jpy = 120`, `classroom_fee_jpy = 0`.
- Wage rule soft-disable/restore browser validation used rule `91fb2803-6071-4a84-9b7a-ea43b1ba6a5a`. Browser checks covered `wage-rule.html` load, confirmation-required validation and field highlight, double-click duplicate protection for disable and restore, inactive filter showing the restored action after soft-disable, active filter showing the disable action after restore, edit dialog active-state select disabled, and 390px narrow dialog layout. Only expected RPC calls appeared: two `school_set_teacher_wage_rule_active_state` calls. Console/pageerror had no critical app errors; only local static server `favicon.ico` 404 was observed. Final browser-test fields were `is_active = true`, `note = codex-test / v2-test / sandbox / v2.52.0 browser restore wage rule`, `settlement_type = jpy_hourly`, `hourly_rate_jpy = 1600`, `hourly_rate_cny = 0`, `exchange_rate = 0`, `transport_fee_jpy = 150`, and `classroom_fee_jpy = 0`.
- Wage rule soft-disable/restore DB comparison confirmed no historical or financial chain mutation: wage rules count stayed `18`, wage locks `70`, wage lock details `388`, payment requests `66`, expenses `44`, and account transactions `235`. The browser-test rule had no other active rule with the same teacher/student/subject/business-entity match.
- Write RPC run-until-gate workflow was validated through the account transfer stage, then replaced by full autopilot in `docs/workflows/write-rpc-flow.md`.
- Full autopilot default: the initial task prompt is phase-level authorization; Codex should continue through analysis, DB verification, schema/RPC work, DB execution, rollback test, whitelisted commit test, frontend work, SQL/frontend commits, feature checkpoint, current-status update, commit, and push unless a hard stop condition is hit.
- If rollback/commit test candidates do not match the test data whitelist, Codex may create clearly marked test data; real business data must not be used as an automatic test candidate.
- Account transfer and account transfer reversal frontend have static, online file, and real local browser interaction checks.
- Account transfer does not yet have a standalone detail page; the first version shows its source summary in account transaction detail.
- Profit summary readonly page is available at `profit-summary.html`, with month, business-entity, and currency filters, API-layer read queries, and no page-layer writes.
- Profit summary policy: received income and paid expense drive operating profit by currency; reversed income/expense are excluded; teacher wage expense is included in expense and marked in expense detail; reimbursement, account adjustment, and account transfer flows are audit references and do not affect operating profit.
- Profit summary drilldown shows effective income and expense detail rows behind summary numbers, with links to `income-detail.html` and `expense-detail.html`.
- Profit summary frontend completed real local Playwright/Chrome interaction verification on 2026-06-07: `profit-summary.html` opened normally, APP_VERSION showed `v2.45.0-teacher-student-create-full-autopilot-20260606`, CSS/module cache bust used `v2.35.0-profit-summary-readonly-full-autopilot-trial-20260607`, default `2026-06` filters loaded, month/business-entity/currency filters and query/reset worked, summary values rendered without NaN/undefined/blank abnormalities, income and expense drilldown links loaded `income-detail.html` and `expense-detail.html` then returned normally, narrow 390px viewport had no page-level horizontal overflow outside table scroll containers, and network/console audit showed no `/rpc`, no non-read requests, and no critical console/page errors.
- Profit summary browser verification details: default `2026-06` loaded normally with income `6 条` and expense `5 条`; filter `2026-06 / 青空进学塾 / aosora / JPY` matched read-only DB comparison with income `2 条` and expense `4 条`; query, reset, summary, income/expense drilldown, 390px narrow viewport, and network audit all passed with no critical console/page errors, no `/rpc`, and no non-read requests.
- Version/cache-bust observation from browser verification: page headers showed global APP_VERSION `v2.45.0-teacher-student-create-full-autopilot-20260606`; `profit-summary.html` CSS/module cache bust used `v2.35.0-profit-summary-readonly-full-autopilot-trial-20260607`; `account.html` CSS cache bust remained historical `v2.24.15-reimbursement-reversal-frontend-20260604`; account module cache bust used `v2.40.0-account-profile-update-full-autopilot-20260607`; account transaction detail module cache bust used `v2.34.1-account-transfer-reversal-frontend-20260606`. This is an existing historical module cache-bust inconsistency and was recorded only, not fixed.
- The write RPC workflow has been standardized as full autopilot in `docs/workflows/write-rpc-flow.md`; ordinary phase switching no longer triggers user confirmation.
- Default Codex guardrails and approval guidance in AGENTS.md now reflect full autopilot while preserving secrets, test-data, destructive-operation, non-whitelisted real-data, ambiguous-git-state, and documentation-conflict hard stops.
- Latest fallback backup for v2.45.0 teacher/student creation is outside the repository at `/Users/polariss710/Documents/aozora_school_system_v2_backups/20260606_233532_v2.45.0_teacher_student_create_final`; it contains `school_db_20260606_233532.dump` and a timestamped code copy.
- Expected repository state at the start of the next task: clean worktree.
- Next planned stage: preserve the completed/verified V1 surfaces first. Candidate backlog items are teacher wage generation, payment management follow-up enhancements, weekly plan image export, full actual import / history migration, whitelist/codex-test deferred cleanup, and DB-level linked-actual unique/index only after read-only duplicate-risk verification. Each write candidate still needs a separate guarded design because it may cross lesson, student settlement, teacher wage, payment, or historical-data boundaries.

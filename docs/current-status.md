# Current Status

Status date: 2026-06-16

This is the lightweight daily entry document. It intentionally keeps only the current system state, hard stops, safety rules, active backlog, and the latest 5 key updates. Older status history is archived in `docs/archive/current-status-history.md`.

## Current System State

- v2 is currently focused on current/future operations. Historical maintenance remains in v1 or in separately authorized guarded migration/repair phases.
- v2 master-data policy: open create/edit where safe, keep delete/merge closed unless a future audit-safe workflow explicitly opens it.
- Core business writes are DB/RPC-backed. Page modules must not call Supabase `.rpc()` directly and must not directly insert/update/delete/upsert rows; page writes go through `js/api/*-api.js` wrappers and verified RPCs.
- Student settlement, teacher wage generation/snapshots, payment requests, reimbursements, account transactions, income/expense, and locking flows are protected main chains. Master-data dialog work must not mutate these chains.
- Cash linkage policy is unified: School is the business ledger and keeps business ownership; Cash System remains the user's household/private account ledger and records actual user-controlled account movement. School initiates external Cash requests from canonical income/expense records; Cash only accepts external requests, shows them for user approve/reject, and changes Cash transactions/balances only after approve. Cash must not proactively create School business records or initiate School business requests. Teacher-wage Cash confirmation now uses `teacher_wage -> school_expense_records -> Cash request`; the old `teacher_wage -> school_payment_requests -> Cash` path is legacy only and disabled for new Cash requests. Income Cash confirmation SQL/RPC is installed, `request-cash-income-confirmation` and `sync-cash-request-result` are deployed, and real CNY whitelist tests have passed.
- Cash-side new request creation is now restricted to canonical `school_income_records` and `school_expense_records`. Legacy direct request types for `school_payment_requests` teacher wage and `school_part_time_work_income_requests` part-time income are retained only as historical read-only evidence and cannot create new Cash pending requests.
- `sync-cash-request-result` main writeback path now serves only canonical `school_income_records` and `school_expense_records`. Legacy direct Cash request types return a deprecated legacy response and no longer call old teacher-wage or part-time-work writeback RPCs.
- 已 Cash 同步的 `school_income_records` / `school_expense_records` 是已结算财务记录，不能走普通编辑、删除、撤销。后续修正必须另行设计冲正 / 调整流程；Cash pending 状态的记录也不能直接编辑核心金额、币种、账户、来源和业务归属字段。
- Income and expense records must separate business attribution month from cash movement month. `business_month` / `year_month` drives business profit, lesson/wage/income attribution, and operating statistics. `income_date` / `received_date` / `expense_date` / `paid_date` drives Cash transaction date, Cash balance, and cash-flow month. Cash must not infer business attribution month, and School must not use business month to fake Cash transaction dates.
- School must not maintain Cash account balances. Cash request account selectors should read active Cash accounts where Cash-side `home_accounts.allow_school_requests = true` and then filter by request currency. Current eligible Cash accounts are `余额宝`, `日元现金`, `日元三菱卡`, and `日元乐天卡`; `余利宝` and `医生处兑换日元先行支付` are excluded by Cash-side configuration, not by School balance truth.
- Income/teacher-wage Cash linkage statuses are not interchangeable. Legacy `pending` belongs to the old manual personal tuition / teacher-wage sync outbox and is kept only for historical RPC/tool compatibility. The current Cash request workflow uses `pending_cash_request` before Cash request submission, `awaiting_cash_confirmation` while Cash approval is pending, `synced` after Cash approve callback, `cash_rejected` after Cash reject, and `failed` / `blocked` for technical/operator failures. New UI copy should avoid presenting legacy `pending` as the recommended main-flow state.
- Current or unclosed real business months must not be used for real wage generation, snapshot generation, student settlement closing, locking, or lock-style write validation. Validation should use transaction rollback or clearly marked whitelist data such as `codex-test`, `v2-test`, `sandbox`, `测试学生`, `测试老师`, or `测试业务归属`.
- Field narrowing policy: create/edit dialogs should expose only fields actually used in current operations. Historical, reserved, low-frequency, generated, derived, financial-chain, and audit fields stay hidden/readonly but are not physically removed from DB unless a separate cleanup phase is designed. Repeated master-data dialog narrowing tasks should follow `docs/workflows/v2-master-dialog-simplification.md`.

## Hard Stops

Stop and report immediately for:

- missing required phase DB environment, unavailable `psql`, static check failure, rollback/commit test failure, abnormal git status, or unclear ownership of test data that cannot be solved by creating safe test data; School DB phases use `SCHOOL_SUPABASE_DB_URL`, Cash DB phases use `CASH_SUPABASE_DB_URL`, and secrets must not be printed or stored;
- need for non-whitelisted real business data, current/unclosed real-month write validation, broad historical-data modification, historical repair, broad backfill, destructive cleanup, `delete`, `truncate`, `drop`, broad permission changes, or irreversible production operation;
- secrets exposure risk, page-level direct DB writes, page-level direct `.rpc()`, non-target module changes, broad refactor, or documentation/request conflict that cannot be safely interpreted.

## Latest Key Updates

1. Teacher wage test detail cleanup, 2026-06-16:
   受控删除一条人工确认的历史测试老师工资明细 `school_teacher_wage_lock_details.id = f9d36502-7c80-492d-92ba-db31942a7170`，所属工资锁 `2aa849e9-4898-425d-b861-843a0dbd8001`。执行 SQL `sql/current/repair_delete_test_teacher_wage_detail_202602.sql` 前确认该 detail 无 adjustment、无 `teacher_wage` 支出记录、无旧 payment request、无 Cash linkage。由于 detail 表没有 `deleted_at`，本次物理删除目标 detail，并按剩余 2 条明细重算工资锁汇总：`lesson_count 3 -> 2`、`total_minutes 360 -> 240`、`pay_hours 6 -> 4`、`lesson_wage_jpy/total_jpy 30000 -> 20000`、`lesson_wage_cny/total_cny 1284.54 -> 856.36`、`fee_jpy = 0`。独立只读验证确认目标 detail 剩余 0，下游支出/payment/Cash/adjustment 关联仍为 0。

1. Income/expense list Cash submission and part-time export polish, 2026-06-16:
   私塾打工 locked settlement Excel 导出现在从 locked snapshot 明细导出开始时间和结束时间，仍不从当前可变课时重新计算金额。收入记录和支出记录一览页新增行内 Cash 提交入口、可提交记录 checkbox、全选和批量提交 modal；批量提交逐条复用现有 `request-cash-income-confirmation` / `request-cash-expense-confirmation`，只创建 Cash pending request，不直接创建 Cash transaction，不修改 Cash approve/reject 或 `sync-cash-request-result`。支出列表补充 Cash linkage 状态字段读取和显示，已 pending / approved / synced 或已有 Cash transaction 的记录不可再次提交。

1. Synced School record guard and Cash transaction display cleanup, 2026-06-16:
   已确认真实 2026-05 诺应教育 canonical income request `475b2b7f-2e86-415f-87a0-580759fb50a4` 为 approved，生成 Cash CNY transaction `f8bec66d-e03c-45aa-9eef-329ac604ca54`，金额 `3,670 CNY`，账户 `余额宝`，Cash transaction 日期为 `2026-05-31`。因此该流水应在 Cash 人民币流水的 `2026-05` 月份视图中显示；3 条学费 CNY 收入日期为 `2026-06-15`，显示在 `2026-06` 月份视图。School 收入/支出详情和 RPC guard 已补齐：已同步到 Cash 的收入/支出不能直接普通编辑或撤销，Cash pending 状态也禁止核心字段普通编辑。测试使用 rollback 数据，residue 为 0。

1. Legacy JPY settlement account cleanup, 2026-06-16:
   用户确认 2026-02～2026-04 旧 School 财务数据已由 Cash 历史承担，School 端只需保留课时业务事实和老师工资锁定事实。本阶段清理旧账户 `吴个人结算账户日元` / `be6e1772-879f-4438-8b24-15934bec8512` 及其 School 财务链：删除 8 条旧 `school_income_records`、15 条旧 `school_expense_records`、15 条旧 `school_payment_requests`，并随账户删除级联清理 28 条 `school_account_transactions`。删除前 rollback 验证通过，删除后确认目标账户、账户名、income/expense/payment/account_transaction/reimbursement/adjustment/transfer/student_payment/salary_payment/personal_cash_linkage 引用均为 0。保留课时记录、学生课时明细、老师课时明细、2026-02～2026-04 `school_teacher_wage_locks` / `school_teacher_wage_lock_details` 业务事实、正式账户 `GMOあおぞらネット銀行` / `包垫付金额` / `吴垫付金额`、2026-05 诺应教育 canonical 收入记录 `fc676042-663b-45c2-9e1f-51e7306d9d63`、以及 7 条 2026-05 `teacher_wage` canonical pending expense records / `492,012 JPY`。未连接 Cash DB，未删除 Cash 真实流水。

1. Canonical School / Cash flow finalized, 2026-06-16:
   第五阶段-6/7完成 School / Cash canonical flow smoke 和文档收尾。School HEAD `ce3ff67`、Cash HEAD `cdfb8a1` 是 smoke 基准。收入侧 `school_income_records -> Cash -> sync` 和支出侧 `school_expense_records -> Cash -> sync` rollback smoke 均通过，residue 为 0；Cash 外部请求 pending 为 0；真实 2026-05 诺应教育收入记录 `fc676042-663b-45c2-9e1f-51e7306d9d63` 已通过 canonical income flow 收到 `3,670 CNY`，School 原始金额仍为 `86,760 JPY`；2026-05 诺应教育 settlement 仍锁定为 `86,760 JPY`；7 条 2026-05 teacher_wage expense records 仍 pending，合计 `492,012 JPY`，等待从支出记录详情页提交 Cash 支付确认。旧 `school_payment_requests` / `teacher_wage_payment_confirm`、`teacher_wage_payment_reverse` 和 `school_part_time_work_income_requests` / `part_time_work_income_received` 均为 legacy historical only，禁止新建。

1. Legacy teacher wage payment requests deprecated, 2026-06-16:
   老师工资 canonical 支出链路第四阶段-5已完成：旧 `index.html` 老师工资支付页明确标记为旧支付请求历史，`teacher_wage` payment request 行不再显示 `提交到 Cash 确认`、`直接确认支付`、取消、恢复、撤销或重新生成等会产生新状态/新 Cash request 的操作，只保留详情查看和 legacy 状态展示。`request-cash-confirmation` Edge Function 在提交旧 payment request 前先检查 `source_type`，遇到 `teacher_wage` 返回 `teacher_wage payments must be handled through school_expense_records`，不会创建 School linkage event 或 Cash pending request。新增并执行 `sql/current/school_deprecate_teacher_wage_payment_cash_rpc.sql`，同名 RPC `school_request_cash_payment_confirmation(...)` 也已替换为拒绝旧 payment request Cash 提交。7 条旧 pending `teacher_wage` payment request 已在上一阶段迁移为 7 条 `school_expense_records` / `492,012 JPY`，旧请求保留为 `cancelled` legacy audit，未创建 Cash request 或 Cash transaction。

1. Teacher wage new payment entry routed to expense records, 2026-06-16:
   老师工资新支付入口已从旧 `school_payment_requests` 改为 canonical `school_expense_records`。`wage-detail.html` 的操作文案改为 `生成支出记录`，页面通过 API wrapper 调用既有 RPC `school_create_teacher_wage_expense_record(...)`，生成 `source_type = teacher_wage`、`expense_category = teacher_wage`、`status = pending` 的支出记录，不再从该入口调用 `school_create_teacher_wage_payment_request(...)`。工资详情会显示已生成的支出记录和支出详情链接，并把支出记录纳入工资快照调整/撤销保护；旧 `school_payment_requests` 仍在同一区域作为 legacy 只读历史展示。7 条旧 pending 请求已迁移为 canonical 支出记录，旧请求已取消并保留审计。

1. Expense-record Cash request minimum flow, 2026-06-15:
   支出侧 canonical 链路第四阶段-1已落地最小闭环：`school_expense_records` can now submit a unified Cash expense pending request through `request-cash-expense-confirmation`, Cash accepts `external_reference_type = school_expense_records`, `request_type = expense_paid`, `transaction_type = expense`, and `sync-cash-request-result` can write Cash approve/reject results back to `school_expense_records`. School SQL source is `sql/current/school_expense_cash_confirmation_workflow.sql`; it adds per-attempt Cash request tracking and actual payment amount/currency fields, plus RPCs `school_request_cash_expense_payment_confirmation(...)`, `school_mark_cash_expense_request_submitted(...)`, `school_mark_cash_expense_confirmed(...)`, and `school_mark_cash_expense_rejected(...)`. Expense detail now has a minimal `提交 Cash 支付确认` dialog for pending expense records only. This stage does not migrate the 7 pending legacy teacher_wage payment requests, does not disable the old teacher-wage payment page, does not alter teacher_wage generation, and does not touch legacy `school_payment_requests`.

1. Teacher wage expense-record route preparation, 2026-06-15:
   支出侧 canonical 链路第二阶段开始落地：老师工资未来目标链路是 teacher wage snapshot -> `school_expense_records` -> expense record Cash request -> Cash approve/reject -> expense writeback。新增准备 SQL `sql/current/school_teacher_wage_expense_record_route.sql`，为 `school_expense_records` 补齐 `source_type/source_id/payee_name_snapshot` 和 Cash linkage 状态字段，并提供专用 RPC `school_create_teacher_wage_expense_record(...)` 从 locked wage snapshot 生成一条 pending `teacher_wage` 支出记录。普通 `school_create_expense_record(...)` 仍拒绝手动创建 `teacher_wage`，避免绕过工资快照。该阶段不迁移 7 条 pending `teacher_wage` payment request，不修复旧直连造成的 paid expense 缺口，不禁用旧支付页面，不提交 Cash request，不修改 Cash repo。

1. External cram-school part-time work income routed through income records, 2026-06-15:
   私塾打工收入已按 `docs/business-flow-canonical.md` 改为 canonical 链路：part-time settlement -> `school_income_records` -> income record detail -> Cash pending request -> Cash approve/reject -> income record writeback. `sql/current/school_part_time_work_income_record_route.sql` adds `school_income_records.source_type/source_id/source_label/source_snapshot`, links `school_part_time_work_monthly_settlements.income_record_id`, adds `school_create_part_time_work_income_record(...)`, and adds `school_request_cash_income_confirmation_for_record(...)` so School can preserve the original locked JPY amount while Cash request uses actual received amount/currency. `request-cash-income-confirmation` supports existing income records with actual received amount/currency, and the old `request-cash-part-time-income-confirmation` endpoint is disabled with 410 to prevent new bypass requests. The real 2026-05 诺应教育 locked settlement `48f45ed1-7d2c-4507-8958-98dff792ad09` remains `86,760 JPY`; School income record `fc676042-663b-45c2-9e1f-51e7306d9d63` has completed Cash sync as `received` with actual Cash receipt `3,670 CNY`. The old bypass Cash request `19ba6cbd-9588-486b-8b2a-b4b7c573f252` was rejected in Cash and School part-time income request `123180f8-012a-4334-95dd-3adc3e7f5b11` is `cash_rejected`; it is retained only as historical wrong-path evidence.

1. External cram-school part-time work workflow refactored, 2026-06-15:
   Replaced the incorrect one-row wage-record V1 with the School-side 私塾打工 workflow: planned external work lessons, actual lessons generated from planned, monthly wage settlements by `year_month + workplace_name`, settlement detail snapshots at lock, and School-side income requests. The obsolete `school_part_time_work_records` table and old V1 RPCs were dropped after user confirmation that the table was newly created and did not need legacy compatibility. Current SQL source is `sql/current/school_part_time_work_workflow.sql`; page/API/nav/docs use `school_part_time_work_lessons`, `school_part_time_work_monthly_settlements`, `school_part_time_work_monthly_settlement_details`, and `school_part_time_work_income_requests`. The part-time-work UI groups records by workplace, renders planned/actual pairs inside each workplace section, keeps only the actual-generation action on empty actual slots, requires second confirmation for editing/deleting paired records, shows planned-based estimated wage cards, and stores start/end time, required lesson count, manual cumulative hours, hourly rate, and transportation fee on planned/actual lesson rows. The old course-group field has been physically removed from lesson rows and locked detail snapshots. RPCs calculate one-session hours from start/end time; `lesson_count` and `cumulative_hours` are display-only reference fields and do not enter estimated wage or actual settlement. Estimated wage uses planned hours * each planned row's hourly rate plus planned transportation fees. Monthly settlements aggregate each actual row's stored lesson wage plus actual transportation fees and settlement adjustment. There is no separate settlement save action: locking reads the current adjustment and memo, saves them, and creates the locked snapshot. Locked settlements without income requests can be unlocked with confirmation, which deletes the snapshot and returns the row to draft; income-request-created settlements cannot be unlocked. Locked Excel export reads settlement detail snapshots, not current mutable lessons. The module remains separate from students, lesson management, teacher_wage settlement, payment requests, and School expense/account transaction chains. All part-time-work page-facing RPC execute grants are authenticated-only; anon is not granted.

1. Income Cash workflow installed, tested, and documented, 2026-06-15:
   `school_income_cash_confirmation_workflow.sql` has been installed and the income Cash RPC chain is live. `request-cash-income-confirmation` and `sync-cash-request-result` have been deployed. Real whitelist CNY testing passed: 李天伦 `21,450 CNY` was created through the Cash System income path, approved in Cash, wrote `home_cny_transactions`, increased Cash balance, and synced School income to `received` / `Cash已确认`. The reversed historical income cleanup/rebuild also passed for 彭宇晗 `6,491 CNY` and 厦门吕同学 `7,740 CNY`: both were recreated as Cash System income, approved, written to `home_cny_transactions`, and reconciled with School. Cash request display text was corrected: income `description` includes student/payee plus content, teacher-wage `description` includes teacher plus wage month, and teacher-wage `note` includes student detail with fallback. Data cleanup completed for codex test accounts / business entities / students / ledgers, for `吴个人结算账户人民币`, and later for the historical `吴个人结算账户日元` School-side account/financial chain after a separate dry-run and user confirmation. Frontend filters were simplified: account management removed transaction-type filtering; income and expense filters now keep only month, student, business entity, account, and currency.

1. Personal external teaching income design refreshed, 2026-06-15:
   `docs/personal-teaching-income-module-design.md` now defines the future personal external cram-school teaching income module as a personal income module, not 青空塾 teacher wage expense. It does not enter teacher wage settlement and does not create `teacher_wage` payment requests. It uses a simplified planned + actual model: planned rows can be freely created/edited/deleted; the only workflow action is generating actual; there are no cancel / pending makeup / makeup completed states; if the lesson did not happen, the planned row is deleted; once planned and actual are paired, deletion requires a second confirmation. Month-end actual review locks a settlement, generates a `personal_teaching_income_request`, then submits a separate Cash receipt confirmation request. Cash approve writes `home_jpy_transactions` / `home_cny_transactions`, increases balance, and marks School received / settled; Cash reject creates no transaction, changes no balance, and leaves the income request pending / retryable.

1. Cash rejected retry attempt support, 2026-06-14 (historical teacher_wage direct flow, superseded):
   Implemented rejected -> resubmit -> approved retry attempts for teacher-wage Cash confirmation. School `school_personal_cash_linkage_events` now has `attempt_no`; uniqueness is per source event + attempt, with a partial active-attempt guard allowing at most one `pending_cash_request` / `awaiting_cash_confirmation` attempt at a time while preserving old `cash_rejected` history and rejected reasons. School request idempotency keys now include `:attempt:{attempt_no}` for new attempts. Cash `home_external_transaction_requests` now keeps `idempotency_key` and `external_event_id` unique per attempt, while reference uniqueness is partial for `pending` / `approved` only, so rejected requests remain terminal history and do not block a later retry. School payment UI shows `Cash已拒绝`, displays the rejection reason, and exposes `重新提交到 Cash 确认`; detail view shows request status, attempt number, rejected time, and rejected reason. Whitelist E2E used 2026-06 codex-test teacher-wage data only: attempt 1 was rejected and left the School payment request pending, attempt 2 was approved and marked the School payment request paid, duplicate active creation returned the same pending attempt, old rejected history remained. Cleanup removed 2 School linkage events, 1 payment request, 1 wage lock, 1 teacher, 1 business entity, 2 Cash requests, and 1 Cash JPY transaction; target residue is 0 and Cash `日元现金` balance returned to `202500.00`.

1. Teacher wage all-scope Cash confirmation implementation, 2026-06-14 (historical teacher_wage direct flow, superseded):
   This phase temporarily promoted the old teacher-wage Cash request path from historical personal + JPY only to all pending `teacher_wage` payment requests. It is now superseded by the canonical `teacher_wage -> school_expense_records -> Cash request` flow. The legacy `request-cash-confirmation` and `school_request_cash_payment_confirmation(...)` entry now reject `teacher_wage` submissions with `teacher_wage payments must be handled through school_expense_records`; they remain only as guardrails and historical compatibility references.

1. Cash account eligibility foundation, 2026-06-14:
   Cash System now owns a formal School account whitelist through `home_accounts.allow_school_requests`. School-side teacher-wage and income Cash request account selectors use that whitelist plus request currency instead of maintaining School-side account-name allowlists or Cash balance/account truth. Current eligible accounts are `余额宝` (`CNY`), `日元现金` (`JPY`), `日元三菱卡` (`JPY`), and `日元乐天卡` (`JPY`). Excluded accounts are `余利宝` and `医生处兑换日元先行支付`. Cash request infrastructure supports JPY/CNY approval on eligible accounts.

1. Final Cash/School business boundary and request policy, 2026-06-14:
   Fixed the final boundary: School System is the business ledger for tuition income, teacher wages, student/teacher/month, business ownership, cost attribution, corporate-account clearing records, and company expense records. Cash System remains the user's household/private account ledger, but because real school money often passes through user-controlled accounts, it records actual Alipay, JPY cash, Mitsubishi/Rakuten, RMB account, CNY/JPY allocation, corporate transfer, and corporate reimbursement movements. School records business facts; Cash records real account changes. School sends external requests; Cash displays pending requests and only approve creates a Cash transaction and changes balance. Reject creates no transaction and changes no balance. Cash does not proactively generate School requests or create School business records. Profit statistics count real operating tuition income, teacher wages, and real business expenses; they must not count Cash-to-corporate transfers, corporate reimbursements to Cash, CNY/JPY exchange, user-account transfers, entrusted tuition clearing, or wage-advance recovery as profit income/expense.

1. Cash linkage v1 unified business policy correction, 2026-06-14:
   Fixed the business policy: School records business ownership and settlement semantics; Cash System records actual account movements for all money that passes through user-controlled accounts. Tuition income enters School for business attribution whether it is personal business, 青空塾, JPY, or CNY/RMB; if the money is received in Alipay, JPY cash, or a JPY bank account, Cash records the corresponding account income. 青空塾 tuition is not excluded from Cash; Cash should label it as `青空塾代收学费`, and later corporate transfer should be recorded as `转给法人账户 / 学费提交 / 代收款清算`, not ordinary household expense. Teacher wage payments also all go through Cash when paid from user-controlled accounts, including personal, 青空塾, mixed ownership, JPY cash/bank, and RMB/Alipay payments; School records personal/青空塾/adjustment/transport/classroom cost attribution, while Cash records the actual payment account. 青空塾 wage payments are treated as `青空塾工资垫付`; later corporate reimbursement is recorded as `法人账户报销 / 青空塾工资垫付报销`. CNY/JPY exchange and account rebalancing are manual Cash operations for now, usually monthly, and are not automatically handled by School. Deprecated old policies include: only personal business enters Cash, 青空塾 does not enter Cash, only personal + `teacher_wage` + JPY links Cash, and only personal tuition JPY links Cash.

1. Cash linkage v2 UI/business direction correction, 2026-06-13 (historical, superseded where it references teacher_wage payment requests):
   Corrected the v2 product direction: School will not add a standalone sync entry. The current real School entry points are income records and expense records. The older note that teacher_wage JPY payment starts from the teacher wage payment page is superseded; teacher_wage now generates `school_expense_records`, and the expense record detail page submits Cash payment confirmation.

2. Cash linkage v2 Edge Function bridge code, 2026-06-13 (historical teacher_wage direct bridge, superseded):
   Added `supabase/functions/request-cash-confirmation/index.ts` as an early School-click backend bridge for old teacher_wage payment-request Cash confirmation. That direct bridge is now disabled for `teacher_wage` and retained only to return a clear deprecation error. Current normal request bridges are `request-cash-income-confirmation` for `school_income_records` and `request-cash-expense-confirmation` for `school_expense_records`; `sync-cash-request-result` main path now handles only those canonical records.

3. Cash linkage v2 School request lifecycle preparation, 2026-06-13:
   Added School-side formal SQL drafts for the page-driven teacher-wage payment confirmation lifecycle. `school_personal_cash_payment_confirmation_lifecycle_schema.sql` extends `school_personal_cash_linkage_events` with Cash pending-request snapshots and v2 statuses: `pending_cash_request`, `awaiting_cash_confirmation`, and `cash_rejected`, while preserving existing `pending/synced/failed/blocked` states for the zsh verification/operations tool. `school_request_personal_cash_payment_confirmation_rpc.sql` adds `school_request_personal_cash_payment_confirmation(...)` to create/reuse a v2 request event without marking the payment request paid, and `school_mark_personal_cash_payment_request_submitted(...)` to record the Cash request id and move the event to `awaiting_cash_confirmation`. `school_personal_cash_payment_request_result_rpcs.sql` adds confirmed/rejected callback RPCs: approval marks the payment request `paid` and linkage `synced` without school ledger side effects; rejection keeps payment request `pending` and marks linkage `cash_rejected`.

4. Cash linkage v2 direction: page-driven Cash confirmation, 2026-06-13:
   Reframed the long-term Cash linkage design. The current verified chain is `待支付工资 -> School 确认支付 -> payment request paid -> School linkage event pending -> zsh directly creates Cash transaction -> School synced`. This is useful for validation and operations, but it is not the final business workflow. The corrected v2 chain embeds Cash confirmation into the real School business actions: `老师工资支付页面选择 Cash 支付账户 -> 提交到 Cash 确认 -> Cash 外部待确认 approve/reject -> approve creates Cash transaction and changes balance -> School paid/Cash已确认; reject creates no transaction and leaves the payment pending/Cash已拒绝`. For tuition income, `收入记录页面选择 Cash 收款账户 -> 创建收入并提交到 Cash 确认 -> Cash approve/reject` follows the same confirmation model. Cash balance must only change after Cash-side approval. Idempotency moves forward to request creation, while Cash transactions still use the existing external/idempotency guard. The recommended bridge is a Supabase Edge Function; do not make the browser directly write the Cash project, and do not make the Cash frontend directly read the School DB.

5. Personal business Cash System linkage manual sync operations note, 2026-06-13:
   Documented the current personal Cash linkage operating model: synchronization is still manual through `scripts/sync-personal-cash-linkage.zsh`, not an automatic background job. The script supports both Phase 1 `teacher_wage` payment linkage and Phase 2 `tuition` income linkage, processes eligible pending events, and accepts `--limit`, `--event-id`, and `--dry-run`. Operators should use dry-run first, use event-id targeting for incident follow-up, use the income detail `重新同步` action before retrying failed tuition income events, avoid manual DB edits, and treat real automatic scheduling as a separate design covering cron / GitHub Actions / Supabase Edge Function / local launchd, secrets, logs/alerts, retry policy, and duplicate-run protection.

6. Personal business Cash System linkage Phase 2 Cash sync status and failed retry UI, 2026-06-13:
   Added Cash sync status display and a guarded failed retry action for personal-business tuition income. Income detail shows a Cash sync card when `school_personal_cash_income_linkage_events` exists, including pending/synced/failed status, Cash transaction id, Cash account snapshot, `synced_at`, `last_error`, `retry_count`, and idempotency key. Failed events show a `重新同步` action that calls `school_retry_personal_cash_income_linkage_event(...)` to reset only failed `tuition_income_received` events with no `cash_transaction_id` back to `pending`; it does not run the sync executor or create Cash transactions. Income list shows a compact Cash badge for linked tuition income: `Cash待同步`, `Cash已同步`, or `Cash失败`. Ordinary income edit/reverse guards remain unchanged. Linked reverse sync and automatic sync scheduling are still not implemented.

7. Personal business Cash System linkage Phase 2 completion, 2026-06-13:
   Completed the personal-business `tuition` JPY income -> Cash System JPY income linkage. The supported path is `school_income_records` -> `school_personal_cash_income_linkage_events` pending outbox -> `scripts/sync-personal-cash-linkage.zsh` -> Cash `home_jpy_transactions.transaction_type = income` -> school event `synced` writeback. Step 8 rollback whitelist testing verified create behavior, rejected 青空塾/company, CNY, non-`tuition`, amount `<= 0`, wrong mapping flow, and student/business mismatch, and confirmed edit/reverse guards reject linked income. Step 9 commit E2E verified one Cash JPY income transaction, idempotent repeat sync with Cash transaction count staying 1, no `school_account_transactions`, and no `school_accounts.current_balance` update. Step 10 cleanup removed the Phase 2 E2E `codex-test-personal-cash-tuition-e2e-20260613` data: Cash target account/transaction count is 0, school target business/student/mapping/income/event count is 0, `home_cny_transactions` marker count is 0, and no Phase 2 test residue remains.

8. Personal business Cash System linkage Phase 2 income detail guard checkpoint, 2026-06-13:
   Executed and verified the income edit/reverse guard for Cash-linked tuition income. `income-detail.html`, `js/api/income-detail-api.js`, and `js/pages/income-detail-page.js` identify `school_personal_cash_income_linkage_events` for the current income, hide ordinary edit/reverse actions, and display a Cash System linkage reason for synced/pending/failed events. `school_update_income_record_rpc.sql` and `school_reverse_income_record_rpc.sql` include backend guards that reject ordinary edit/reverse when a `tuition_income_received` linkage event exists. Phase 2 v1 still does not implement linked edit/reverse or reverse sync.

9. Personal business Cash System linkage Phase 2 income-entry UI checkpoint, 2026-06-13:
   Historical Phase 2 income-entry UI supported two creation modes: normal School account income and personal-business Cash System tuition income. That path used `school_create_personal_cash_tuition_income_record(...)` and a `pending` manual-sync outbox. The current income Cash workflow supersedes it: Cash System mode now calls `request-cash-income-confirmation` through `createCashSystemIncome(...)`, supports Cash-eligible JPY/CNY accounts by `allow_school_requests + currency`, creates School pending income first, and waits for Cash approve/reject.

10. Personal business Cash System linkage Phase 2 sync-executor checkpoint, 2026-06-13:
   `scripts/sync-personal-cash-linkage.zsh` supports both existing Phase 1 payment linkage and Phase 2 tuition income linkage. The income branch reads pending `school_personal_cash_income_linkage_events`, calls Cash `home_create_external_jpy_transaction` with `transaction_type = income`, `external_event_type = tuition_income_received`, `external_reference_type = school_income_records`, and `external_reference_id = income_record_id`, then marks the school income event `synced` with the Cash transaction id or `failed` with the error. Payment linkage behavior remains on the existing teacher-wage expense path.

11. Personal business Cash System linkage Phase 2 create-RPC checkpoint, 2026-06-13:
   Executed `school_create_personal_cash_tuition_income_record_rpc.sql`. The dedicated RPC `school_create_personal_cash_tuition_income_record(...)` creates one personal-business `tuition` JPY `school_income_records` row and one pending `school_personal_cash_income_linkage_events` row in the same school DB transaction. It requires a personal business entity, tuition category, JPY currency/payment currency, positive amount, matching active `school_personal_cash_account_mappings.flow_type = tuition_income`, and the same locked student monthly settlement guard as ordinary tuition income. It intentionally does not require a school account, does not update `school_accounts.current_balance`, does not insert `school_account_transactions`, and does not write Cash DB directly.

12. Personal business Cash System linkage Phase 2 school DB foundation checkpoint, 2026-06-13:
   Executed school-side Phase 2 DB foundation SQL for personal-business tuition JPY income -> Cash System JPY income linkage. `school_personal_cash_income_linkage_schema.sql` extends `school_personal_cash_account_mappings.flow_type` with `tuition_income` and adds independent `school_personal_cash_income_linkage_events` outbox table for `school_income_records` + `tuition_income_received`. `school_personal_cash_income_linkage_rpcs.sql` adds `school_update_personal_cash_income_linkage_event_status` for pending/synced/failed status writeback.

13. Personal business Cash System linkage Phase 1 completion and cleanup, 2026-06-13:
   Completed the Phase 1 personal-business `teacher_wage` JPY linkage. The supported path is school payment request -> `school_personal_cash_linkage_events` outbox -> `scripts/sync-personal-cash-linkage.zsh` -> Cash System `home_create_external_jpy_transaction` -> school event `synced` or `failed`. End-to-end verification created one Cash JPY transaction and synced the school event, duplicate execution did not create a second Cash transaction, and the failed path moved the event to `failed`. Company / 青空塾, CNY, reimbursement, corporate account spending, non-`teacher_wage`, personal tuition income, and part-time wage income were verified out of scope. Five temporary cleanup/rollback/delete SQL files were removed and pushed. Phase 1 DB test residue was cleaned: Cash target transaction/account counts are 0; school target linkage event/payment request/mapping/business entity counts are 0; older income-edit `codex-test` data was intentionally left untouched.

14. Personal business Cash System linkage payment-confirm outbox checkpoint, 2026-06-13:
   Completed payment confirmation UI/API/RPC integration for Phase 1 school outbox only. Added and executed `school_confirm_personal_cash_payment_request_rpc.sql`, which confirms one personal-business `teacher_wage` JPY pending payment request and creates one pending `school_personal_cash_linkage_events` outbox row in the same school DB transaction, without writing Cash DB and without creating school expense/account-transaction/account-balance side effects. Payment page now loads active `school_personal_cash_account_mappings`, shows a Cash System account selector only for `entity_type = personal` + `teacher_wage` + `JPY`, keeps company / 青空塾 on the existing school account selector and `school_confirm_payment_request`, blocks personal requests without active mapping, and hides reversal action for paid requests that already have a Cash outbox event. Payment detail page now shows read-only Cash linkage status. Rollback test residue was 0. Whitelist commit test initially left clearly marked school test data only: business entity `93000000-0000-4000-8000-000000141001`, payment request `93000000-0000-4000-8000-000000141101`, mapping `eaf3b59d-f944-441f-911f-b639ba284c78`, linkage event `11f8f9ee-cbf4-4a29-8d6a-dc56a7d2e7e4` with `sync_status = pending`; this Phase 1 test data was later cleaned. No Cash DB writes were performed in this outbox-creation phase.

15. Personal business Cash System linkage school-side mapping/outbox checkpoint, 2026-06-13:
   Completed school-side Phase 1 minimum schema/RPC/API support for `个人名义` teacher-wage JPY payment -> Cash System linkage metadata. Added and executed `school_personal_cash_linkage_schema.sql` for `school_personal_cash_account_mappings` and `school_personal_cash_linkage_events`; added and executed `school_personal_cash_linkage_rpcs.sql` for guarded mapping create/update/list, linkage event create/query, and sync status update. Added API wrappers in `js/api/personal-cash-linkage-api.js`; no page module was changed, and payment confirmation/wage/reimbursement/student-settlement main chains were not modified. Rollback tests verified personal-only guards, duplicate event idempotency, status update behavior, and 0 residue. Whitelist commit test initially left clearly marked school test data only: business entity `92000000-0000-4000-8000-000000132001`, payment request `92000000-0000-4000-8000-000000132101`, mapping `f5c02610-1b11-4353-b5de-ae5b3b60f980`, linkage event `9b95e09a-09c4-4203-bc02-07daaf1beb5b` with `sync_status = pending`; this Phase 1 test data was later cleaned. No Cash DB writes were performed in this school-side metadata phase.

16. Personal business Cash System linkage Phase 1 planning, 2026-06-13:
   Completed the earlier read-only pre-implementation planning for `个人名义` teacher-wage JPY payment confirmation -> Cash System JPY transaction linkage. Later 2026-06-13 checkpoints implemented Cash-side external JPY RPC support, school-side mapping/outbox support, payment-confirm outbox creation, and controlled manual end-to-end sync.

17. Family account whitelist cleanup checkpoint, 2026-06-13:
   Cleaned the account/family first-stage whitelist commit-test account after dry-run, rollback validation, commit delete, and residue check. Deleted only `school_accounts.id = d3734cd7-fa94-4be3-b8dc-3cdc3690f667` / `account_code = codex-test-family-app-type-commit-20260613`; pre-delete checks confirmed `app_type = family`, inactive, no business entity, non-company, clearly `codex-test`, and 0 references from account transactions, adjustments, transfers, income, expense, payment requests, reimbursements, salary/student payments, and v51 backup account columns. Residue check confirmed target ID/code/text and all checked references are 0.

## Current To-Do / Priority

1. Preserve completed V1 surfaces before adding new write scope.
2. Treat Cash System reversal sync, automatic sync scheduling, payment-management follow-up beyond the current optional exchange-rate confirmation behavior, weekly plan image export, full actual import/history migration, expanded wage-lock lifecycle, teacher wage adjustment items for transport/classroom fees, payment-request realtime exchange-rate policy, account scope/household owner expansion beyond first-stage `app_type`, personal external teaching income full workflow, private cram-school work Cash linkage, account balance adjustment / opening-balance correction, real expense attachment storage upload/preview/replace, business-entity-scoped wage generation, corporate-account clearing records, profit-stat exclusion implementation for internal clearing, SQL permission hardening for legacy `grant execute ... to anon` write RPC definitions, and DB-level linked-actual unique/index review as separate guarded phases. Do not add a standalone School sync page. Historical Phase 2 tuition income linkage is complete for personal + tuition + JPY only and remains historical; the current income Cash confirmation path now supports CNY/JPY Cash request confirmation through the installed workflow. Teacher-wage Cash confirmation is implemented and real 2026-05 small-batch trial has passed for one JPY teacher-wage payment.
3. Keep master-data create/edit narrow. Do not reopen hidden historical/reserved fields unless a concrete current-operation need and DB/API/RPC support are verified first.
4. For future master-data dialog field narrowing, reference `docs/workflows/v2-master-dialog-simplification.md` first. If touching settlement, wage, payment, reimbursement, income/expense, lesson, or account flows, use the full write-RPC workflow in `docs/workflows/write-rpc-flow.md` and verify side effects through rollback or whitelist tests.

## Daily References

- Module summary: `docs/module-status.md`
- Technical ownership map: `docs/system-map.md`
- Write/RPC workflow: `docs/workflows/write-rpc-flow.md`
- Master dialog simplification workflow: `docs/workflows/v2-master-dialog-simplification.md`
- Account/family ledger design: `docs/account-family-account-integration-design-2026-06-12.md`
- Personal business Cash System linkage design: `docs/personal-business-cash-system-linkage-design-2026-06-13.md`
- Personal external teaching income design: `docs/personal-teaching-income-module-design.md`
- Part-time wage design: `docs/part-time-wage-record-module-design-2026-06-12.md`
- Full historical current-status archive: `docs/archive/current-status-history.md`
